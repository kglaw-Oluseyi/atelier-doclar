import { test } from "@playwright/test";

test.skip("S05B omnibus smoke retired in favour of focused journeys", async () => {
  // Kept so historical names do not reappear as a passing compatibility path.
});
