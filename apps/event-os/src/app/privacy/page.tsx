export default function PrivacyPage() {
  return (
    <main id="main" className="sign-in">
      <h1>Privacy</h1>
      <p>
        Event OS stores staff identity, organisation scope and audit history. It does not store
        guest records in this release. Session cookies are HttpOnly. Production use is not
        authorised.
      </p>
    </main>
  );
}
