# MD-PR-S054 EOS-S05B Risk Protection and Continuity Command Implementation Authority

**Status:** CEO-visible proposed implementation authority — execute only after George Lawson gives this complete prompt to Cursor  
**Repository:** `kglaw-Oluseyi/atelier-doclar`  
**Branch:** `main`  
**Required starting baseline:** `f12798a28f438408527d8811be57389661c86c25`  
**Railway project/environment/service:** `atelier-doclar / production / event-os` only  
**Live URL:** `https://event-os-production-bc8d.up.railway.app`  
**Accepted implementation currently live:** EOS-S05A application SHA `50322fa5fdf7b46437dc9d62579e2e2ad918e762`  
**Production posture:** `productionAuthorised: false`; synthetic data only

## 1. Authority

George Lawson has ratified EOS-S05B documents 00–15 as the controlling requirements corpus. This prompt assigns `MD-PR-S054` and releases EOS-S05B implementation as one continuous, substantial milestone.

Cursor must implement the complete ratified corpus, including all 55 units in `14A`–`14D`, then commit, push, deploy Event OS, run the current executable evaluation and live synthetic verification, and return one consolidated evidence report. Cursor must not pause merely because one module or commit has completed.

This authority does not accept EOS-S05B, start EOS-S06, authorise production, activate external providers, use real data, send communications, book vendors, move money, submit claims or deploy another service.

## 2. Preflight and canonical document placement

1. Confirm the working directory is the repository `kglaw-Oluseyi/atelier-doclar` and branch is `main`.
2. Fetch and verify local HEAD = `origin/main` = GitHub main = `f12798a28f438408527d8811be57389661c86c25`.
3. Confirm the worktree is clean except for the CEO-supplied EOS-S05B ratification files. If unrelated or overlapping changes exist, stop and report them without altering them.
4. Locate the ratified EOS-S05B Markdown corpus. George may have placed the files at the repository root or one directory above `docs`. Do not ask him to copy their contents.
5. Move the complete corpus into `docs/control/eos-s05b/`, preserving filenames and contents:

```text
00_RISK_PROTECTION_CONTINUITY_PACK_INDEX.md
01_RATIFICATION_DOCTRINE_AND_ACCEPTED_CONTRACT_COMPATIBILITY.md
02_REGULATORY_INSURANCE_AND_LEGAL_EVIDENCE_MODEL.md
03_PRODUCT_DOMAIN_DATA_AND_STATE_SPECIFICATION.md
04_INSURANCE_POLICY_COVERAGE_AND_GAP_ENGINE.md
05_CONTRACTUAL_PROTECTION_AND_LEGAL_REVIEW_SPECIFICATION.md
06_VENDOR_RISK_AND_EVIDENCE_INTELLIGENCE.md
07_EVENT_CONTINUITY_FALLBACK_AND_INCIDENT_COMMAND.md
08_RISK_BUDGET_RESERVE_AND_FINANCIAL_BOUNDARIES.md
09_PERMISSIONS_PRIVACY_AUDIT_AND_DOCUMENT_SECURITY.md
10_COMMAND_ATELIER_UX_AND_CLIENT_ASSURANCE.md
11_AI_SAFETY_SCORING_AND_EVALUATION.md
12_INTEGRATION_NOTIFICATIONS_AND_PROVIDER_BOUNDARIES.md
13_MIGRATION_DEPLOYMENT_AND_PRODUCTION_CONFIGURATION.md
14A_CURSOR_PACK_CONTROL_FOUNDATIONS.md
14B_CURSOR_PACK_INSURANCE_CONTRACT_VENDOR.md
14C_CURSOR_PACK_CONTINUITY_BUDGET_EXPERIENCE.md
14D_CURSOR_PACK_ASSURANCE_DEPLOYMENT_REPORTING.md
15_INDEPENDENT_CLAUDE_VERIFICATION_AND_ACCEPTANCE.md
```

6. If any controlling file is missing or materially different, stop. Do not reconstruct it from memory or continue with a partial corpus.
7. Record CEO ratification and `MD-PR-S054` proportionately in the canonical authority, compatibility, successor and build records. The ratification record and implementation may be committed separately. Do not mark EOS-S05B accepted.

## 3. Authority order

Apply this order where sources differ:

1. Accepted EOS-S01–S05 and EOS-S04A–F/EOS-S05A contracts.
2. Current document-authority, execution-compatibility, deployment and technical-debt controls.
3. Ratified EOS-S05B documents 00–13 and 15.
4. Detailed Cursor instructions 14A–14D.
5. Existing implementation patterns where compatible.
6. Supplied market examples and historical material only as non-authoritative discovery input.

Do not weaken the current production-level system to preserve an outdated assumption.

## 4. Controlling product outcome

Deliver a unified organisation-wide and event-scoped protection command capability that answers:

- what protection applies;
- what evidence supports it;
- what remains exposed or indeterminate;
- what action or decision is due;
- who is authorised to make it;
- what changed since the last governed review.

The implementation must include:

1. governed legal/regulatory/venue source and rule editions;
2. organisation and event insurance-policy registry;
3. deterministic applicability, coverage matching and gap engine;
4. immutable residual-risk decisions;
5. contractual clause studio with legal/commercial review;
6. explainable vendor-risk evidence and assessment;
7. tiered primary/alternate/standby event assignments;
8. continuity-plan editions, checkpoints and escalations;
9. governed fallback authorisation without autonomous external action;
10. incident command and post-incident learning proposals;
11. risk exposure/reserve projection into accepted Budget Intelligence;
12. CEO no-blind-spots Protection Command;
13. event-lead workspace and permission-safe client/venue assurance dossier;
14. central disclosure/document security;
15. provider-neutral ports, fixture adapters and fail-closed executable evaluation.

## 5. Non-negotiable integration boundaries

- Reuse canonical organisation, event, phase, venue/layout, vendor/party, asset, Budget Intelligence, communications, identity, audit and document-store contracts.
- Do not create parallel event, vendor, spatial, guest, budget, payment, contract-party, communications or authority ledgers.
- Organisation policies apply through versioned event applicability snapshots; do not copy them into event truth.
- Risk allocations are immutable Budget Intelligence scenario drivers. They are not cash, commitments, invoices, payments or a second budget.
- A certificate is evidence, not coverage certainty. An active policy is not a guaranteed claim payment.
- Supplied values such as 5% reserve, 20–40% retention, 150–200% liquidated damages, sample premiums and named-venue requirements are governed, sourced assumptions—not hard-coded defaults.
- Legal wording remains a draft until authorised review. Never promise enforceability or describe a penalty as valid legal protection without approved evidence.
- AI remains advisory. It cannot approve a policy, rule, clause, vendor, residual risk, fallback, reserve, incident closure, dossier or governing fact.
- System Administrator has no implicit business, legal, spatial, vendor or financial authority.
- Maker/checker, event scope, optimistic concurrency, durable idempotency, append-only audit, immutable editions, truthful action results and no false success are server-enforced.

## 6. External-effect prohibitions

Do not:

- activate Brevo, Twilio, an insurer/broker interface, live OCR/AI, malware provider or source-monitoring provider without separate authority;
- send a vendor reminder or client dossier;
- contact, engage, book, replace or cancel a vendor;
- allocate or move money, release retention, pay a claim or create a bank/payment instrument;
- submit an insurance claim or represent one as submitted;
- contact emergency services or represent help as dispatched;
- use real client, vendor, policy, contract, personnel, incident or venue data;
- set `productionAuthorised` to true;
- deploy Control Tower unless its executable code is genuinely affected and this is explicitly justified in the final report;
- start or prepare EOS-S06 implementation.

Provider integrations default inactive. Workflow actions may create governed intents or proposals only. UI and audit must say exactly that.

## 7. Continuous execution route

Execute every unit in order:

- Pack A `RPC-01`–`RPC-10`: canonical reconciliation, schemas, permissions, migration, evidence, disclosure, audit and navigation.
- Pack B `RPC-11`–`RPC-25`: source/rule library, policy registry, verification, applicability, coverage/gaps, residual-risk decisions, clause studio, safeguards, vendor evidence/assessment and tiered roster.
- Pack C `RPC-26`–`RPC-43`: critical functions, continuity plans, checkpoints, escalation, fallback activation, incidents, Budget integration, CEO/event/client experiences and browser journeys.
- Pack D `RPC-44`–`RPC-55`: provider ports, security, concurrency/idempotency, executable evaluation, readiness, full gates, control records, parity, Railway deployment, live smoke and evidence report.

Focused commits are required, but commits are not review stops. Run focused tests during development. Consolidate related defects rather than issuing superficial patches.

## 8. Quality threshold

Backend and frontend receive equal implementation attention.

The UI must use Command Atelier, not a generic insurance or enterprise risk dashboard. Lead with consequence and next action. Use progressive disclosure and plain language. Unknown and indeterminate states must be designed, visible states. Avoid raw JSON, UUID-first oversight, card grids without hierarchy and red/amber/green as the only meaning.

Required interaction checks include:

- every enabled click target uses a pointer;
- disabled actions use `not-allowed`;
- inputs use a text cursor;
- visible keyboard focus and correct focus-after-result behaviour;
- reduced-motion support;
- no document horizontal scroll at 360px;
- tablet, desktop and 200% zoom usability;
- labelled mobile cards for dense matrices;
- no stale rejected value presented as saved;
- no success that hides replay, denial, conflict, pending persistence or unsent external effects.

## 9. Security and privacy threshold

Use private object storage, authenticated retrieval, `Cache-Control: private, no-store`, safe content disposition, content-signature checks, size limits, hash identity and central projection policy. Object keys/storage identifiers never enter ordinary projections.

Treat legal advice, policy numbers, personnel/medical details, bank information, incidents and internal vendor assessments as separately classified. A less-privileged assignment must not recover restricted information through Navigator, Inspector, comparison, audit, export history, cached exports, direct routes, attributes or error text.

Uploaded documents and text are untrusted. Prompt instructions, active markup, macros and URLs inside them remain inert. Do not describe in-process validation as general antivirus.

## 10. Executable evaluation

Implement `s05b-eval-v1` exactly as documents 11 and 14D require:

- cases execute production functions against isolated synthetic state;
- persisted case results and diagnostics are mandatory;
- cases cannot supply their own pass booleans;
- mutation-sensitive negative adapters prove unsafe behaviour is detected;
- UNRUN, RUNNING, STALE, INCOMPATIBLE, FAILED and ERROR all block release;
- only the current complete corpus with zero-tolerance clear is fixture-release-ready;
- corpus/model/contract changes honestly stale an earlier pass.

Before running the current corpus live, show fail-closed readiness. Run it only with the authorised CEO fixture and synthetic state. Refresh/reopen must preserve the resulting current status.

## 11. Testing and evidence gates

Before push/deploy, run:

```text
all focused EOS-S05B tests
pnpm typecheck
pnpm --filter @maison-doclar/shared-platform test
pnpm --filter @maison-doclar/event-os test
pnpm programme:validate
pnpm --filter @maison-doclar/event-os build
git diff --check
focused local Playwright
material whole-slice local browser journey
```

Test Memory and Postgres semantics where relevant. Include permission, maker/checker, cross-event, cross-organisation, unauthenticated, concurrency, replay, failure, document-security, projection, responsive and keyboard coverage.

Record every first-run failure with classification, root cause, correction and rerun. Do not erase a product defect because a retry later passes. Do not call a product failure infrastructure without proof.

## 12. GitHub and deployment

After all local gates pass:

1. commit focused changes with honest messages;
2. push normally to `main`;
3. prove local HEAD = `origin/main` = GitHub main;
4. deploy affected application code to Railway project `atelier-doclar`, environment `production`, service `event-os` only;
5. verify deployment SUCCESS and exact application SHA;
6. verify alive/ready, POSTGRES, migrations APPLIED, `productionAuthorised:false`, document/provider adapter states and S05B fail-closed evaluation;
7. run the authorised synthetic corpus and representative live smoke;
8. verify refresh persistence, one concurrency conflict, permission-safe export and no external side effect.

If a documentation stamp follows the application deployment, report both SHAs and do not manufacture drift. Do not redeploy Event OS for a documentation-only stamp.

## 13. Railway variables

Before changing variables, report the exact variable name, `event-os` service, secret/non-secret status, purpose and whether George must supply it. Cursor may set non-secret flags and use task-relevant configured credentials. Stop only when a value must remain human-controlled. Never print a secret.

## 14. Required final consolidated report

Return one report containing:

1. baseline, application, final/docs and deployed SHAs;
2. commits and principal changed files;
3. ratified document placement/control updates;
4. domain architecture and migrations;
5. delivered states and invariants;
6. permission/maker-checker matrix;
7. insurance source/rule/policy/applicability/gap evidence;
8. contractual and vendor-risk evidence;
9. continuity/checkpoint/fallback/incident evidence;
10. Budget integration, calculations, trace and governing truth preservation;
11. CEO, event-lead and client experience evidence;
12. privacy/document/export security;
13. concurrency, idempotency and truthful action results;
14. evaluation edition/hash/count, negative controls and live run;
15. every first-run failure;
16. focused and full gate results;
17. GitHub parity;
18. Railway deployment ID, SHA and readiness;
19. live synthetic smoke;
20. variables/provider states and confirmation of no external effects;
21. genuine retained debt only;
22. rollback and forward-recovery route;
23. explicit confirmation that EOS-S05B is not accepted, EOS-S06 was not started, production remains unauthorised and Control Tower was not deployed unless genuinely affected.

## 15. Meaningful stop conditions

Stop and report only if:

- repository/branch/baseline differs;
- required ratified corpus is missing or materially changed;
- the worktree contains overlapping changes;
- a destructive migration/reset would be required;
- a controlling contradiction cannot be resolved from authority order;
- a human-controlled secret is required;
- real data or an unapproved external effect could be involved;
- another repository, Railway project or service would be required.

Do not stop for module completion, routine test failure, ordinary defect remediation, a focused commit, a normal deploy wait or a recoverable Railway CLI inconvenience.

## 16. Final prohibition

Cursor does not accept EOS-S05B. Do not create `EOS_S05B_ACCEPTANCE.md`, increment the catalogue accepted-slice count, authorise production, activate external providers or begin EOS-S06.

Finish with:

`EOS-S05B IMPLEMENTATION COMPLETE — READY FOR AI CTO REVIEW AND WHOLE-SLICE CLAUDE VERIFICATION — NOT ACCEPTED — EOS-S06 NOT STARTED.`
