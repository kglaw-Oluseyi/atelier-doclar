---
pid: 26224
cwd: "/Users/maisondoclar/Desktop/atelier-doclar"
command: "cd /Users/maisondoclar/Desktop/atelier-doclar && pnpm --filter @maison-doclar/event-os test 2>&1 | tee docs/control/evidence/eos-s06-cpsat-production/governance/deploy-current-main/04_EVENT_OS_TEST.txt\n"
title: "Run event-os unit test suite"
status: succeeded
started_at: 2026-09-18T08:24:12.919Z
running_for_ms: 32207    
---

> @maison-doclar/event-os@0.1.0 test /Users/maisondoclar/Desktop/atelier-doclar/apps/event-os
> tsx --test test/*.test.ts

TAP version 13
# Subtest: Event OS Academy delta
    # Subtest: keeps academy success copy distinct from operational authorisation
    ok 1 - keeps academy success copy distinct from operational authorisation
      ---
      duration_ms: 0.556
      ...
    # Subtest: scores the planner bank without granting authority
    ok 2 - scores the planner bank without granting authority
      ---
      duration_ms: 9.333166
      ...
    1..2
ok 1 - Event OS Academy delta
  ---
  duration_ms: 10.448834
  type: 'suite'
  ...
# Subtest: ACA-S04D catalogue delivery
    # Subtest: registers ACA-S04D as routeable with version and eligible-role assignment
    ok 1 - registers ACA-S04D as routeable with version and eligible-role assignment
      ---
      duration_ms: 1.153
      ...
    # Subtest: keeps distinction at 90, pass at 80-89, retake below 80, and completion grants no authority
    ok 2 - keeps distinction at 90, pass at 80-89, retake below 80, and completion grants no authority
      ---
      duration_ms: 1.425875
      ...
    # Subtest: replays catalogue seed without duplicating ACA-S04D and leaves ACA-S04C intact
    ok 3 - replays catalogue seed without duplicating ACA-S04D and leaves ACA-S04C intact
      ---
      duration_ms: 0.427917
      ...
    1..3
ok 2 - ACA-S04D catalogue delivery
  ---
  duration_ms: 4.434167
  type: 'suite'
  ...
# Subtest: Event OS ACA-S04D delta
    # Subtest: keeps forecast success copy distinct from RSVP mutation
    ok 1 - keeps forecast success copy distinct from RSVP mutation
      ---
      duration_ms: 1.3775
      ...
    # Subtest: scores the planner bank without granting authority
    ok 2 - scores the planner bank without granting authority
      ---
      duration_ms: 0.316209
      ...
    1..2
ok 3 - Event OS ACA-S04D delta
  ---
  duration_ms: 2.957208
  type: 'suite'
  ...
# Subtest: ACA-S04E catalogue delivery
    # Subtest: registers ACA-S04E as routeable with version and eligible-role assignment
    ok 1 - registers ACA-S04E as routeable with version and eligible-role assignment
      ---
      duration_ms: 14.70925
      ...
    # Subtest: keeps distinction at 90, pass at 80-89, retake below 80, and completion grants no authority
    ok 2 - keeps distinction at 90, pass at 80-89, retake below 80, and completion grants no authority
      ---
      duration_ms: 1.436125
      ...
    # Subtest: replays catalogue seed without duplicating ACA-S04E and leaves ACA-S04D intact
    ok 3 - replays catalogue seed without duplicating ACA-S04E and leaves ACA-S04D intact
      ---
      duration_ms: 0.188959
      ...
    1..3
ok 4 - ACA-S04E catalogue delivery
  ---
  duration_ms: 17.79775
  type: 'suite'
  ...
# Subtest: ACA-S04F catalogue delivery
    # Subtest: registers ACA-S04F as routeable with version and eligible-role assignment
    ok 1 - registers ACA-S04F as routeable with version and eligible-role assignment
      ---
      duration_ms: 0.579375
      ...
    # Subtest: keeps distinction at 90, pass at 80-89, retake below 80, and completion grants no authority
    ok 2 - keeps distinction at 90, pass at 80-89, retake below 80, and completion grants no authority
      ---
      duration_ms: 0.370625
      ...
    # Subtest: replays catalogue seed without duplicating ACA-S04F and leaves ACA-S04E intact
    ok 3 - replays catalogue seed without duplicating ACA-S04F and leaves ACA-S04E intact
      ---
      duration_ms: 0.116834
      ...
    1..3
ok 5 - ACA-S04F catalogue delivery
  ---
  duration_ms: 1.713917
  type: 'suite'
  ...
# Subtest: live postgres access lifecycle probe
    # Subtest: skips unless EVENT_OS_PERSISTENCE_IT=1 and DATABASE_URL are set
    ok 1 - skips unless EVENT_OS_PERSISTENCE_IT=1 and DATABASE_URL are set
      ---
      duration_ms: 4.021084
      ...
    1..1
ok 6 - live postgres access lifecycle probe
  ---
  duration_ms: 5.233583
  type: 'suite'
  ...
# Subtest: EOS-S04D action-result lifecycle
    # Subtest: 1. a denial cannot label an unrelated later success
    ok 1 - 1. a denial cannot label an unrelated later success
      ---
      duration_ms: 33.628041
      ...
    # Subtest: 2. a denial cannot label a related later success
    ok 2 - 2. a denial cannot label a related later success
      ---
      duration_ms: 0.261333
      ...
    # Subtest: 3. a success cannot hide a later denial
    ok 3 - 3. a success cannot hide a later denial
      ---
      duration_ms: 0.12825
      ...
    # Subtest: 4. a validation failure is replaced by a corrected success
    ok 4 - 4. a validation failure is replaced by a corrected success
      ---
      duration_ms: 0.197708
      ...
    # Subtest: 5. a version conflict stays locked until reload, then a later success can present
    ok 5 - 5. a version conflict stays locked until reload, then a later success can present
      ---
      duration_ms: 18.1245
      ...
    # Subtest: 6. actor A denial cannot transfer to actor B after sign-out
    ok 6 - 6. actor A denial cannot transfer to actor B after sign-out
      ---
      duration_ms: 0.290792
      ...
    # Subtest: 7. two tabs completing different actions only show the matching correlation
    ok 7 - 7. two tabs completing different actions only show the matching correlation
      ---
      duration_ms: 0.14875
      ...
    # Subtest: 8. refresh or back navigation after consumption does not resurrect a banner
    ok 8 - 8. refresh or back navigation after consumption does not resurrect a banner
      ---
      duration_ms: 0.112042
      ...
    # Subtest: 9. UI correlation id is the same identifier bound on the result
    ok 9 - 9. UI correlation id is the same identifier bound on the result
      ---
      duration_ms: 0.320417
      ...
    # Subtest: 10. result cookies and query strings refuse secrets and raw records
    ok 10 - 10. result cookies and query strings refuse secrets and raw records
      ---
      duration_ms: 16.572667
      ...
    # Subtest: 11. consume is bound to the presented correlation id
    ok 11 - 11. consume is bound to the presented correlation id
      ---
      duration_ms: 6.904209
      ...
    # Subtest: 12. a remount cannot schedule a second consume for the same correlation
    ok 12 - 12. a remount cannot schedule a second consume for the same correlation
      ---
      duration_ms: 43.09825
      ...
    1..12
ok 7 - EOS-S04D action-result lifecycle
  ---
  duration_ms: 121.341958
  type: 'suite'
  ...
# Subtest: MD-PR-S065 dossier publication result delivery
    # Subtest: writes a new correlation onto the exact dossier redirect after publish
    ok 1 - writes a new correlation onto the exact dossier redirect after publish
      ---
      duration_ms: 13.447125
      ...
    # Subtest: keeps a replay result distinct from a stale earlier correlation
    ok 2 - keeps a replay result distinct from a stale earlier correlation
      ---
      duration_ms: 0.377042
      ...
    # Subtest: does not forget the result until the presented correlation is consumed
    ok 3 - does not forget the result until the presented correlation is consumed
      ---
      duration_ms: 0.153666
      ...
    1..3
ok 8 - MD-PR-S065 dossier publication result delivery
  ---
  duration_ms: 14.394334
  type: 'suite'
  ...
# Subtest: Event OS addressing frontend contracts
    # Subtest: keeps a blank honorific option and never requires a default title
    ok 1 - keeps a blank honorific option and never requires a default title
      ---
      duration_ms: 107.019875
      ...
    # Subtest: exposes a typed workspace to authorised operators and denies Client Lead
    ok 2 - exposes a typed workspace to authorised operators and denies Client Lead
      ---
      duration_ms: 1116.222917
      ...
    # Subtest: keeps planner confirmation and exception review off the capability projection
    ok 3 - keeps planner confirmation and exception review off the capability projection
      ---
      duration_ms: 828.361875
      ...
    # Subtest: projects party membership and unnamed entitlement without fabricating a guest
    ok 4 - projects party membership and unnamed entitlement without fabricating a guest
      ---
      duration_ms: 898.040917
      ...
    1..4
ok 9 - Event OS addressing frontend contracts
  ---
  duration_ms: 2952.065
  type: 'suite'
  ...
# Subtest: Event OS application identity
    # Subtest: exposes build identity when embedded at build time
    ok 1 - exposes build identity when embedded at build time
      ---
      duration_ms: 8.101667
      ...
    # Subtest: keeps live/ready/System on one application SHA alias
    ok 2 - keeps live/ready/System on one application SHA alias
      ---
      duration_ms: 1.039
      ...
    # Subtest: refuses stale EVENT_OS_GIT_SHA override when build SHA is known
    ok 3 - refuses stale EVENT_OS_GIT_SHA override when build SHA is known
      ---
      duration_ms: 0.200333
      ...
    # Subtest: accepts deliberate upload-deploy SHA via build embedding
    ok 4 - accepts deliberate upload-deploy SHA via build embedding
      ---
      duration_ms: 2.654791
      ...
    # Subtest: keeps documentation HEAD separately representable
    ok 5 - keeps documentation HEAD separately representable
      ---
      duration_ms: 0.571292
      ...
    # Subtest: falls back to pinned application SHA before Railway tip when embed is local-unreleased
    ok 6 - falls back to pinned application SHA before Railway tip when embed is local-unreleased
      ---
      duration_ms: 0.103
      ...
    # Subtest: keeps Railway tip as deployment source without overriding embedded application SHA
    ok 7 - keeps Railway tip as deployment source without overriding embedded application SHA
      ---
      duration_ms: 0.068167
      ...
    # Subtest: does not treat productionAuthorised as mutable via identity resolution
    ok 8 - does not treat productionAuthorised as mutable via identity resolution
      ---
      duration_ms: 0.123459
      ...
    1..8
ok 10 - Event OS application identity
  ---
  duration_ms: 14.446542
  type: 'suite'
  ...
# Subtest: Event OS formal CI e2e shard accounting
    # Subtest: assigns every listed Playwright test file exactly once with 280 tests
    not ok 1 - assigns every listed Playwright test file exactly once with 280 tests
      ---
      duration_ms: 27141.47875
      location: '/Users/maisondoclar/Desktop/atelier-doclar/apps/event-os/test/ci-e2e-shard-accounting.test.ts:12:3'
      failureType: 'testCodeFailure'
      error: |-
        Expected values to be strictly equal:
        
        143 !== 130
        
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected: 130
      actual: 143
      operator: 'strictEqual'
      stack: |-
        TestContext.<anonymous> (/Users/maisondoclar/Desktop/atelier-doclar/apps/event-os/test/ci-e2e-shard-accounting.test.ts:35:12)
        Test.runInAsyncScope (node:async_hooks:206:9)
        Test.run (node:internal/test_runner/test:796:25)
        Test.start (node:internal/test_runner/test:702:17)
        node:internal/test_runner/test:1133:71
        node:internal/per_context/primordials:482:82
        new Promise (<anonymous>)
        new SafePromise (node:internal/per_context/primordials:450:29)
        node:internal/per_context/primordials:482:9
        Array.map (<anonymous>)
      ...
    1..1
not ok 11 - Event OS formal CI e2e shard accounting
  ---
  duration_ms: 27141.981042
  type: 'suite'
  location: '/Users/maisondoclar/Desktop/atelier-doclar/apps/event-os/test/ci-e2e-shard-accounting.test.ts:11:1'
  failureType: 'subtestsFailed'
  error: '1 subtest failed'
  code: 'ERR_TEST_FAILURE'
  ...
# Subtest: synthetic cleanup CLI packaging
    # Subtest: runs a non-destructive preview without importing TypeScript workspace sources via node
    ok 1 - runs a non-destructive preview without importing TypeScript workspace sources via node
      ---
      duration_ms: 5470.427916
      ...
    # Subtest: refuses execute when not bound to Railway project atelier-doclar
    ok 2 - refuses execute when not bound to Railway project atelier-doclar
      ---
      duration_ms: 4365.849833
      ...
    1..2
ok 12 - synthetic cleanup CLI packaging
  ---
  duration_ms: 9837.77125
  type: 'suite'
  ...
# Subtest: comms display masking
    # Subtest: masks full email addresses
    ok 1 - masks full email addresses
      ---
      duration_ms: 0.440583
      ...
    # Subtest: masks full telephone numbers
    ok 2 - masks full telephone numbers
      ---
      duration_ms: 0.56025
      ...
    # Subtest: prefers masked email hints for guest selectors
    ok 3 - prefers masked email hints for guest selectors
      ---
      duration_ms: 1.321417
      ...
    # Subtest: does not expose complete contact values from eventGuestOptions
    ok 4 - does not expose complete contact values from eventGuestOptions
      ---
      duration_ms: 490.731167
      ...
    1..4
ok 13 - comms display masking
  ---
  duration_ms: 494.019667
  type: 'suite'
  ...
# Subtest: comms display role and time labels
    # Subtest: renders governed role labels without raw keys
    ok 1 - renders governed role labels without raw keys
      ---
      duration_ms: 0.324125
      ...
    # Subtest: formats proposal timestamps in Africa/Lagos without raw ISO
    ok 2 - formats proposal timestamps in Africa/Lagos without raw ISO
      ---
      duration_ms: 8.504458
      ...
    # Subtest: labels channels without raw codes as ordinary copy
    ok 3 - labels channels without raw codes as ordinary copy
      ---
      duration_ms: 0.423166
      ...
    1..3
ok 14 - comms display role and time labels
  ---
  duration_ms: 9.679458
  type: 'suite'
  ...
# Subtest: correction review presentation
    # Subtest: uses the governed review projection without leaking identifiers
    ok 1 - uses the governed review projection without leaking identifiers
      ---
      duration_ms: 54.092167
      ...
    # Subtest: keeps long names and contact values intact
    ok 2 - keeps long names and contact values intact
      ---
      duration_ms: 0.601791
      ...
    # Subtest: renders AVAILABLE evidence as an authorised same-event link
    ok 3 - renders AVAILABLE evidence as an authorised same-event link
      ---
      duration_ms: 0.301917
      ...
    # Subtest: does not render a link for NOT_LINKED evidence
    ok 4 - does not render a link for NOT_LINKED evidence
      ---
      duration_ms: 0.236708
      ...
    # Subtest: does not render a broken link for UNAVAILABLE evidence
    ok 5 - does not render a broken link for UNAVAILABLE evidence
      ---
      duration_ms: 0.197583
      ...
    # Subtest: does not reveal protected details for REDACTED evidence
    ok 6 - does not reveal protected details for REDACTED evidence
      ---
      duration_ms: 0.289625
      ...
    # Subtest: refuses a cross-event evidence path instead of rendering a link
    ok 7 - refuses a cross-event evidence path instead of rendering a link
      ---
      duration_ms: 0.06625
      ...
    # Subtest: does not offer a decision for a legacy maker-unavailable correction
    ok 8 - does not offer a decision for a legacy maker-unavailable correction
      ---
      duration_ms: 0.147833
      ...
    # Subtest: omits decision controls after the correction is decided
    ok 9 - omits decision controls after the correction is decided
      ---
      duration_ms: 0.362625
      ...
    1..9
ok 15 - correction review presentation
  ---
  duration_ms: 60.983375
  type: 'suite'
  ...
# Subtest: FileBackedSeatingV2Repository
    # Subtest: persists layout bindings across repository instances (restart)
    ok 1 - persists layout bindings across repository instances (restart)
      ---
      duration_ms: 10.8005
      ...
    # Subtest: derives sibling seating-v2 path from platform store path
    ok 2 - derives sibling seating-v2 path from platform store path
      ---
      duration_ms: 0.226625
      ...
    # Subtest: rolls back memory when a transaction throws before durable write
    ok 3 - rolls back memory when a transaction throws before durable write
      ---
      duration_ms: 2.674541
      ...
    # Subtest: refuses a second simultaneous writer (single-writer lock)
    ok 4 - refuses a second simultaneous writer (single-writer lock)
      ---
      duration_ms: 1.826375
      ...
    # Subtest: reclaims a lock left by a dead process
    ok 5 - reclaims a lock left by a dead process
      ---
      duration_ms: 1.7325
      ...
    # Subtest: fails closed on corrupt or truncated seating store
    ok 6 - fails closed on corrupt or truncated seating store
      ---
      duration_ms: 2.2435
      ...
    # Subtest: fails closed when platform-store identity drifts
    ok 7 - fails closed when platform-store identity drifts
      ---
      duration_ms: 5.099542
      ...
    # Subtest: cleanup of one temporary store does not touch a sibling store
    ok 8 - cleanup of one temporary store does not touch a sibling store
      ---
      duration_ms: 7.585708
      ...
    1..8
ok 16 - FileBackedSeatingV2Repository
  ---
  duration_ms: 33.549625
  type: 'suite'
  ...
# Subtest: S04D phase eligibility labels versus forecast centre
    # Subtest: keeps church eligible people distinct from forecast centre and does not add phases
    ok 1 - keeps church eligible people distinct from forecast centre and does not add phases
      ---
      duration_ms: 1927.051167
      ...
    1..1
ok 17 - S04D phase eligibility labels versus forecast centre
  ---
  duration_ms: 1928.206042
  type: 'suite'
  ...
# Subtest: event search filter
    # Subtest: matches CAP600, 600, capacity, stretch, 1000, and ids
    ok 1 - matches CAP600, 600, capacity, stretch, 1000, and ids
      ---
      duration_ms: 0.410125
      ...
    # Subtest: does not invent events outside the permitted set
    ok 2 - does not invent events outside the permitted set
      ---
      duration_ms: 0.069291
      ...
    # Subtest: labels synthetic qualification fixtures
    ok 3 - labels synthetic qualification fixtures
      ---
      duration_ms: 0.120542
      ...
    1..3
ok 18 - event search filter
  ---
  duration_ms: 1.283791
  type: 'suite'
  ...
# Subtest: programme posture
    # Subtest: renders accepted S06/S06A/Gate 1 and truthful S06B/C/D/S07 governance
    ok 1 - renders accepted S06/S06A/Gate 1 and truthful S06B/C/D/S07 governance
      ---
      duration_ms: 0.141
      ...
    1..1
ok 19 - programme posture
  ---
  duration_ms: 0.188292
  type: 'suite'
  ...
# Subtest: event-os production boundary
    # Subtest: keeps production authorisation false by contract
    ok 1 - keeps production authorisation false by contract
      ---
      duration_ms: 0.453417
      ...
    1..1
ok 20 - event-os production boundary
  ---
  duration_ms: 0.869875
  type: 'suite'
  ...
# Subtest: validationCounterDisplay
    # Subtest: does not show misleading zeros on completed intake after promotion
    ok 1 - does not show misleading zeros on completed intake after promotion
      ---
      duration_ms: 1.192958
      ...
    # Subtest: shows live counts while validating
    ok 2 - shows live counts while validating
      ---
      duration_ms: 1.2925
      ...
    # Subtest: shows truthful zeros when an empty completed job truly had no rows
    ok 3 - shows truthful zeros when an empty completed job truly had no rows
      ---
      duration_ms: 0.233833
      ...
    1..3
ok 21 - validationCounterDisplay
  ---
  duration_ms: 4.269584
  type: 'suite'
  ...
# Subtest: EVENT_OS_NON_PRODUCTION_STORE_PATH guards
    # Subtest: accepts an absolute path under the OS temp directory when fixtures are allowed
    ok 1 - accepts an absolute path under the OS temp directory when fixtures are allowed
      ---
      duration_ms: 1.152834
      ...
    # Subtest: refuses a non-temp path
    ok 2 - refuses a non-temp path
      ---
      duration_ms: 4.183542
      ...
    # Subtest: refuses production
    ok 3 - refuses production
      ---
      duration_ms: 1.553375
      ...
    1..3
ok 22 - EVENT_OS_NON_PRODUCTION_STORE_PATH guards
  ---
  duration_ms: 8.464458
  type: 'suite'
  ...
# Subtest: EOS-S04A operational state matrix
    # Subtest: classifies platform codes into retry-safe operator copy without secrets
    ok 1 - classifies platform codes into retry-safe operator copy without secrets
      ---
      duration_ms: 0.926417
      ...
    # Subtest: marks validation, postgres and replay states explicitly
    ok 2 - marks validation, postgres and replay states explicitly
      ---
      duration_ms: 0.152583
      ...
    # Subtest: explains session expiry, revoked assignment and permission change
    ok 3 - explains session expiry, revoked assignment and permission change
      ---
      duration_ms: 0.28125
      ...
    # Subtest: renders success, empty, partial, stale and loading without false success
    ok 4 - renders success, empty, partial, stale and loading without false success
      ---
      duration_ms: 0.155625
      ...
    # Subtest: never renders a stale submission as success or a neutral validation state
    ok 5 - never renders a stale submission as success or a neutral validation state
      ---
      duration_ms: 0.175375
      ...
    # Subtest: parses a flash conflict and rejects stack traces
    ok 6 - parses a flash conflict and rejects stack traces
      ---
      duration_ms: 0.113583
      ...
    1..6
ok 23 - EOS-S04A operational state matrix
  ---
  duration_ms: 2.624625
  type: 'suite'
  ...
# Subtest: directory structured addressing
    # Subtest: uses familiar and confirmed formal forms without concatenating a guessed title
    ok 1 - uses familiar and confirmed formal forms without concatenating a guessed title
      ---
      duration_ms: 729.296875
      ...
    1..1
ok 24 - directory structured addressing
  ---
  duration_ms: 729.514042
  type: 'suite'
  ...
# Subtest: event-os persistence policy
    # Subtest: keeps productionAuthorised false and does not treat it as memory-only
    ok 1 - keeps productionAuthorised false and does not treat it as memory-only
      ---
      duration_ms: 0.3625
      ...
    1..1
ok 25 - event-os persistence policy
  ---
  duration_ms: 0.7705
  type: 'suite'
  ...
# Subtest: S047 Budget Studio command and focus contracts
    # Subtest: parses typed 350 from FormData into the service command
    ok 1 - parses typed 350 from FormData into the service command
      ---
      duration_ms: 32.976083
      ...
    # Subtest: redirects with the persisted scenario and calculation identifiers
    ok 2 - redirects with the persisted scenario and calculation identifiers
      ---
      duration_ms: 0.378792
      ...
    # Subtest: marks the resolved contradiction heading as the focus target
    ok 3 - marks the resolved contradiction heading as the focus target
      ---
      duration_ms: 0.734125
      ...
    1..3
ok 26 - S047 Budget Studio command and focus contracts
  ---
  duration_ms: 35.921208
  type: 'suite'
  ...
# Subtest: EOS-S049 action-result truth, focus and scoped retry locks
    # Subtest: replay does not present Did data change Yes
    ok 1 - replay does not present Did data change Yes
      ---
      duration_ms: 5.045291
      ...
    # Subtest: new calculation remains APPLIED with data change
    ok 2 - new calculation remains APPLIED with data change
      ---
      duration_ms: 0.667416
      ...
    # Subtest: maker/checker denial and stale conflict focus the allowlisted heading once
    ok 3 - maker/checker denial and stale conflict focus the allowlisted heading once
      ---
      duration_ms: 0.280916
      ...
    # Subtest: unsafe target input cannot select arbitrary DOM elements
    ok 4 - unsafe target input cannot select arbitrary DOM elements
      ---
      duration_ms: 0.142042
      ...
    # Subtest: stale 340 lock does not disable unrelated actions or another subject
    ok 5 - stale 340 lock does not disable unrelated actions or another subject
      ---
      duration_ms: 0.166792
      ...
    # Subtest: consumed or refreshed results must not steal heading focus
    ok 6 - consumed or refreshed results must not steal heading focus
      ---
      duration_ms: 0.135583
      ...
    # Subtest: action result from another subject does not affect this page
    ok 7 - action result from another subject does not affect this page
      ---
      duration_ms: 0.126708
      ...
    1..7
ok 27 - EOS-S049 action-result truth, focus and scoped retry locks
  ---
  duration_ms: 8.871292
  type: 'suite'
  ...
# Subtest: EOS-S04A final acceptance II — conflict recovery decision
    # Subtest: unlocks after the recovery action even if the conflict query is still present
    ok 1 - unlocks after the recovery action even if the conflict query is still present
      ---
      duration_ms: 111.269709
      ...
    # Subtest: does not let a stale flash relock a different guest dossier
    ok 2 - does not let a stale flash relock a different guest dossier
      ---
      duration_ms: 0.221875
      ...
    # Subtest: repeats recovery without leaving the second dossier locked
    ok 3 - repeats recovery without leaving the second dossier locked
      ---
      duration_ms: 0.09375
      ...
    # Subtest: keeps a permission warning distinct from recovered conflict state
    ok 4 - keeps a permission warning distinct from recovered conflict state
      ---
      duration_ms: 0.075791
      ...
    1..4
ok 28 - EOS-S04A final acceptance II — conflict recovery decision
  ---
  duration_ms: 113.29925
  type: 'suite'
  ...
# Subtest: EOS-S04C guest-renewal conflict presentation
    # Subtest: does not present success or a new access link while a stale renewal is in conflict
    ok 1 - does not present success or a new access link while a stale renewal is in conflict
      ---
      duration_ms: 0.528333
      ...
    # Subtest: unlocks after reload even if the conflict query string is still present
    ok 2 - unlocks after reload even if the conflict query string is still present
      ---
      duration_ms: 0.10225
      ...
    # Subtest: keeps a leftover success query from masking a new conflict flash after reload
    ok 3 - keeps a leftover success query from masking a new conflict flash after reload
      ---
      duration_ms: 0.334458
      ...
    1..3
ok 29 - EOS-S04C guest-renewal conflict presentation
  ---
  duration_ms: 2.18625
  type: 'suite'
  ...
# Subtest: EOS-S051 decision-result focus sequencing
    # Subtest: new correlation focuses; stored F5 skips; same-document restore does not skip a live result
    ok 1 - new correlation focuses; stored F5 skips; same-document restore does not skip a live result
      ---
      duration_ms: 5.835291
      ...
    # Subtest: unsafe target cannot select arbitrary DOM and reduced motion avoids smooth scrolling
    ok 2 - unsafe target cannot select arbitrary DOM and reduced motion avoids smooth scrolling
      ---
      duration_ms: 0.167166
      ...
    1..2
ok 30 - EOS-S051 decision-result focus sequencing
  ---
  duration_ms: 7.35225
  type: 'suite'
  ...
# Subtest: EOS-S05B action-result scope and consumption
    # Subtest: does not recall a consumed result after unrelated navigation without a query id
    ok 1 - does not recall a consumed result after unrelated navigation without a query id
      ---
      duration_ms: 41.574083
      ...
    # Subtest: does not present a Protection result on an event workspace path without matching scope
    ok 2 - does not present a Protection result on an event workspace path without matching scope
      ---
      duration_ms: 0.310166
      ...
    # Subtest: classifies Zod payloads as expected validation, never raw JSON or a server failure
    ok 3 - classifies Zod payloads as expected validation, never raw JSON or a server failure
      ---
      duration_ms: 0.359167
      ...
    1..3
ok 31 - EOS-S05B action-result scope and consumption
  ---
  duration_ms: 43.032541
  type: 'suite'
  ...
# Subtest: EOS-S06 Claude remediation surface copy
    # Subtest: DEF-01 run cards expose stable identity fields
    ok 1 - DEF-01 run cards expose stable identity fields
      ---
      duration_ms: 0.602792
      ...
    # Subtest: DEF-01 adoptable-run e2e contract targets data-outcome FEASIBLE
    ok 2 - DEF-01 adoptable-run e2e contract targets data-outcome FEASIBLE
      ---
      duration_ms: 0.282083
      ...
    # Subtest: DEF-02 never equates seating publication with missing venue layout tables
    not ok 3 - DEF-02 never equates seating publication with missing venue layout tables
      ---
      duration_ms: 6.851083
      location: '/Users/maisondoclar/Desktop/atelier-doclar/apps/event-os/test/s06-claude-remediation-copy.test.ts:34:3'
      failureType: 'testCodeFailure'
      error: |-
        The input did not match the regular expression /remains operational until a successor is published|remains the operational seating until a successor is published/. Input:
        
        'import { AtelierPageHeader } from "../../../../../components/atelier-page-header";\n' +
          'import { AtelierOperationalState } from "../../../../../components/atelier-operational-state";\n' +
          'import { AtelierSectionTabs } from "../../../../../components/atelier-section-tabs";\n' +
          'import { AppShell } from "../../../../../components/shell";\n' +
          'import { ActionResultBanner } from "../../../../../components/action-result-banner";\n' +
          'import { CanonicalId, CanonicalTime, HistoryDisclosure } from "../../../../../components/canonical-evidence";\n' +
          'import { IdempotencyField, PendingSubmit } from "../../../../../components/atelier-pending-submit";\n' +
          'import { ProtectionMutationForm } from "../../../../../components/protection-mutation-form";\n' +
          'import { SeatingRuleAuthoringFields } from "../../../../../components/seating-rule-authoring-fields";\n' +
          'import { loadPresentedActionResult } from "../../../../../server/action-flash";\n' +
          'import { guardedActor } from "../../../../../server/guard";\n' +
          'import { getRuntime } from "../../../../../server/runtime";\n' +
          'import { preferredSeatingAssignment, seatingPermissions } from "../../../../../server/seating-scope";\n' +
          'import { operationalStateFromCode } from "../../../../../server/operational-state";\n' +
          'import {\n' +
          '  activateReservationBlockAction,\n' +
          '  adoptSeatingRunAction,\n' +
          '  adoptCpsatCandidateAction,\n' +
          '  applySeatingChangeAction,\n' +
          '  cancelSeatingRunAction,\n' +
          '  createReservationBlockAction,\n' +
          '  createSeatingConstraintAction,\n' +
          '  decideCpsatCandidateAction,\n' +
          '  decideSeatingApprovalAction,\n' +
          '  decideSeatingReviewAction,\n' +
          '  activateSeatingLayoutBindingAction,\n' +
          '  freezeSeatingInputsAction,\n' +
          '  launchSeatingRunAction,\n' +
          '  proposeSeatingLayoutBindingAction,\n' +
          '  recoverProposeSeatingLayoutBindingAction,\n' +
          '  publishSeatingPlanAction,\n' +
          '  releaseReservationBlockAction,\n' +
          '  requestSeatingExportAction,\n' +
          '  recallSeatingPlanAction,\n' +
          '  runS06EvaluationAction,\n' +
          '  submitCpsatCandidateAction,\n' +
          '  submitSeatingPlanAction,\n' +
          '  withdrawSeatingLayoutBindingAction,\n' +
          '  supersedeReservationBlockAction,\n' +
          '  withdrawReservationBlockAction,\n' +
          '} from "../../../../../server/seating-actions";\n' +
          'import { switchSeatingVerifyAsAction } from "../../../../../server/seating-verify-as-action";\n' +
          'import { eventOsVerifyAsAvailable } from "../../../../../server/seating-verify-as";\n' +
          'import { emitSettlementStage, PlatformError, retryLockApplies, seatingV2ReplacementEnabled, buildCpsatRunUiModel, WORKER_UNAVAILABLE_PUBLIC_MESSAGE, QUEUE_BUSY_PUBLIC_MESSAGE, type CpsatCandidateReviewModel } from "@maison-doclar/shared-platform";\n' +
          'import { activateSeatingRuleAction, withdrawSeatingRuleAction } from "../../../../../server/seating-actions";\n' +
          'import { CpsatRunStatusPanel } from "../../../../../components/cpsat-run-status-panel";\n' +
          'import { CpsatRunLifecyclePoller } from "../../../../../components/cpsat-run-lifecycle-poller";\n' +
          'import { CpsatCandidateReviewPanel } from "../../../../../components/cpsat-candidate-review-panel";\n' +
          'import { CpsatWorkerUnavailablePanel } from "../../../../../components/cpsat-worker-unavailable-panel";\n' +
          '\n' +
          'function visibleSeatingRuns<T extends { id: string }>(runs: T[], currentRunId: string | undefined, limit = 12): T[] {\n' +
          '  if (runs.length <= limit) return runs;\n' +
          '  const current = currentRunId ? runs.find((run) => run.id === currentRunId) : undefined;\n' +
          '  if (!current) return runs.slice(-limit);\n' +
          '  return [...runs.filter((run) => run.id !== currentRunId).slice(-(limit - 1)), current];\n' +
          '}\n' +
          '\n' +
          'function Envelope({ fields }: { fields: Record<string, string | number> }) {\n' +
          '  return (\n' +
          '    <>\n' +
          '      {Object.entries(fields).map(([name, value]) => (\n' +
          '        <input key={name} type="hidden" name={name} value={String(value)} />\n' +
          '      ))}\n' +
          '    </>\n' +
          '  );\n' +
          '}\n' +
          '\n' +
          'const TABS = [\n' +
          '  { href: "#overview", label: "Overview" },\n' +
          '  { href: "#inputs", label: "Inputs" },\n' +
          '  { href: "#rules", label: "Rules" },\n' +
          '  { href: "#reservations", label: "Reservations" },\n' +
          '  { href: "#runs", label: "Runs" },\n' +
          '  { href: "#studio", label: "Studio" },\n' +
          '  { href: "#review", label: "Review" },\n' +
          '  { href: "#publication", label: "Publication" },\n' +
          '] as const;\n' +
          '\n' +
          'export const maxDuration = 60;\n' +
          '\n' +
          'export default async function EventSeatingPage({\n' +
          '  params,\n' +
          '  searchParams,\n' +
          '}: {\n' +
          '  params: Promise<{ eventId: string }>;\n' +
          '  searchParams: Promise<Record<string, string | string[] | undefined>>;\n' +
          '}) {\n' +
          '  const { eventId } = await params;\n' +
          '  const query = await searchParams;\n' +
          '  const { actor, person } = await guardedActor();\n' +
          '  const runtime = getRuntime();\n' +
          '  const organisations = runtime.service.listOrganisations(actor);\n' +
          '  const events = organisations.flatMap((item) => {\n' +
          '    try {\n' +
          '      return runtime.service.listEvents(actor, item.id);\n' +
          '    } catch {\n' +
          '      return [];\n' +
          '    }\n' +
          '  });\n' +
          '  let event = events.find((item) => item.id === eventId);\n' +
          '  const organisation = organisations.find((item) => item.id === event?.organisationId) ?? organisations[0];\n' +
          '  if (!event && organisation) {\n' +
          '    try {\n' +
          '      event = runtime.service.getEvent(actor, organisation.id, eventId);\n' +
          '    } catch {\n' +
          '      event = undefined;\n' +
          '    }\n' +
          '  }\n' +
          '  if (!event || !organisation) {\n' +
          '    const denialOrg = organisations[0];\n' +
          '    const denied = denialOrg ? !seatingPermissions(person, denialOrg.id, eventId).view : true;\n' +
          '    return (\n' +
          '      <AppShell person={person} current="/app/events">\n' +
          '        <AtelierOperationalState\n' +
          '          state={operationalStateFromCode(\n' +
          '            denied ? "FORBIDDEN" : "NOT_FOUND",\n' +
          '            denied ? "This assignment cannot perform this seating action." : "The requested event is not available in this assignment.",\n' +
          '          )}\n' +
          '        />\n' +
          '      </AppShell>\n' +
          '    );\n' +
          '  }\n' +
          '  const permissions = seatingPermissions(person, organisation.id, event.id);\n' +
          '  if (!permissions.view) {\n' +
          '    return (\n' +
          '      <AppShell person={person} organisationName={organisation.displayName} eventName={event.name} eventId={event.id} current="/app/events">\n' +
          '        <AtelierOperationalState state={operationalStateFromCode("FORBIDDEN", "This assignment cannot perform this seating action.")} />\n' +
          '      </AppShell>\n' +
          '    );\n' +
          '  }\n' +
          '  let workspace;\n' +
          '  let publicationSource: "CPSAT" | "NONE" = "NONE";\n' +
          '  const workspaceStarted = Date.now();\n' +
          '  try {\n' +
          '    if (seatingV2ReplacementEnabled()) {\n' +
          '      workspace = await runtime.service.seatingV2Commands().projectWorkspace(actor, event.id);\n' +
          '      if (workspace.currentPublication) {\n' +
          '        publicationSource = "CPSAT";\n' +
          '      }\n' +
          '    } else {\n' +
          '      workspace = await runtime.service.seatingCommands().projectWorkspace(actor, event.id);\n' +
          '      // Legacy seating command path is not operational after M6C; treat as empty.\n' +
          '      workspace = { ...workspace, currentPublication: undefined };\n' +
          '    }\n' +
          '  } catch (error) {\n' +
          '    const message = error instanceof PlatformError ? error.publicMessage ?? error.message : "This assignment cannot perform this seating action.";\n' +
          '    return (\n' +
          '      <AppShell person={person} organisationName={organisation.displayName} eventName={event.name} eventId={event.id} current="/app/events">\n' +
          '        <AtelierOperationalState state={operationalStateFromCode("FORBIDDEN", message)} />\n' +
          '      </AppShell>\n' +
          '    );\n' +
          '  }\n' +
          '  const workspaceMs = Date.now() - workspaceStarted;\n' +
          '  const assignmentId = preferredSeatingAssignment(person.id, organisation.id, event.id)?.id ?? "";\n' +
          '  const actionResultStarted = Date.now();\n' +
          '  const presented = await loadPresentedActionResult({\n' +
          '    requestPath: `/app/events/${event.id}/seating`,\n' +
          '    resultId: typeof query.result === "string" ? query.result : undefined,\n' +
          '    actorPersonId: person.id,\n' +
          '    organisationId: organisation.id,\n' +
          '    eventId: event.id,\n' +
          '  });\n' +
          '  const actionResultMs = Date.now() - actionResultStarted;\n' +
          '  const resultId = typeof query.result === "string" ? query.result : undefined;\n' +
          '  emitSettlementStage({\n' +
          '    stage: presented.correlationId && resultId && presented.correlationId === resultId ? "RENDER_RESULT_FOUND" : "RENDER_RESULT_MISSING",\n' +
          '    commandId: resultId,\n' +
          '    resultId,\n' +
          '    eventId: event.id,\n' +
          '    durationMs: actionResultMs,\n' +
          '    reasonClass: presented.correlationId ? "PRESENTED" : "ABSENT",\n' +
          '  });\n' +
          '  const envelopeFields = { organisationId: organisation.id, eventId: event.id, assignmentId };\n' +
          '  const reviewableLifecycles = new Set([\n' +
          '    "READY_FOR_REVIEW",\n' +
          '    "PENDING_APPROVAL",\n' +
          '    "APPROVED",\n' +
          '    "REJECTED",\n' +
          '    "ADOPTED",\n' +
          '    "SUPERSEDED",\n' +
          '  ]);\n' +
          '  const cpsatReviewRun = workspace.runs.find((run) =>\n' +
          '    reviewableLifecycles.has(String((run as { lifecycle?: string }).lifecycle ?? run.status ?? "")),\n' +
          '  );\n' +
          '  let cpsatReview: CpsatCandidateReviewModel | null = null;\n' +
          '  if (cpsatReviewRun && seatingV2ReplacementEnabled()) {\n' +
          '    try {\n' +
          '      cpsatReview = await runtime.service.seatingV2Commands().getCpsatCandidateReview(actor, event.id, cpsatReviewRun.id);\n' +
          '    } catch {\n' +
          '      cpsatReview = null;\n' +
          '    }\n' +
          '  }\n' +
          '  const working = workspace.workingEdition as {\n' +
          '    id?: string;\n' +
          '    contentHash?: string;\n' +
          '    status?: string;\n' +
          '    version?: number;\n' +
          '    stale?: boolean;\n' +
          '    sourceRunId?: string;\n' +
          '  } | undefined;\n' +
          '  const input = workspace.inputEdition as { id?: string; contentHash?: string; layoutContentHash?: string } | undefined;\n' +
          '  const publication = workspace.currentPublication as {\n' +
          '    id?: string;\n' +
          '    publicationNumber?: number;\n' +
          '    editionHash?: string;\n' +
          '    adoptionId?: string;\n' +
          '    runId?: string;\n' +
          '    productResult?: string | null;\n' +
          '    evidenceGrade?: string | null;\n' +
          '    seatedCount?: number;\n' +
          '    unseatedCount?: number;\n' +
          '    source?: string;\n' +
          '  } | undefined;\n' +
          '  const verifyAs = eventOsVerifyAsAvailable() && permissions.fixtureVerifyAs;\n' +
          '  const seatedDisplay = publication?.seatedCount ?? workspace.counts.seated;\n' +
          '  const unseatedDisplay = publication?.unseatedCount ?? workspace.counts.unseated;\n' +
          '  const eligibleDisplay = Math.max(workspace.counts.eligibleGuests, seatedDisplay + unseatedDisplay);\n' +
          '  return (\n' +
          '    <AppShell person={person} organisationName={organisation.displayName} eventName={event.name} eventId={event.id} current="/app/events">\n' +
          '      <AtelierPageHeader\n' +
          '        eyebrow="Seating Command"\n' +
          '        title={`${event.name} seating`}\n' +
          '        lede="The solver recommends. Authorised people decide. Published seating does not send messages, issue credentials or change check-in."\n' +
          '      />\n' +
          '      <p data-testid="sea'... 59507 more characters
        
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected:
      actual: |-
        import { AtelierPageHeader } from "../../../../../components/atelier-page-header";
        import { AtelierOperationalState } from "../../../../../components/atelier-operational-state";
        import { AtelierSectionTabs } from "../../../../../components/atelier-section-tabs";
        import { AppShell } from "../../../../../components/shell";
        import { ActionResultBanner } from "../../../../../components/action-result-banner";
        import { CanonicalId, CanonicalTime, HistoryDisclosure } from "../../../../../components/canonical-evidence";
        import { IdempotencyField, PendingSubmit } from "../../../../../components/atelier-pending-submit";
        import { ProtectionMutationForm } from "../../../../../components/protection-mutation-form";
        import { SeatingRuleAuthoringFields } from "../../../../../components/seating-rule-authoring-fields";
        import { loadPresentedActionResult } from "../../../../../server/action-flash";
        import { guardedActor } from "../../../../../server/guard";
        import { getRuntime } from "../../../../../server/runtime";
        import { preferredSeatingAssignment, seatingPermissions } from "../../../../../server/seating-scope";
        import { operationalStateFromCode } from "../../../../../server/operational-state";
        import {
          activateReservationBlockAction,
          adoptSeatingRunAction,
          adoptCpsatCandidateAction,
          applySeatingChangeAction,
          cancelSeatingRunAction,
          createReservationBlockAction,
          createSeatingConstraintAction,
          decideCpsatCandidateAction,
          decideSeatingApprovalAction,
          decideSeatingReviewAction,
          activateSeatingLayoutBindingAction,
          freezeSeatingInputsAction,
          launchSeatingRunAction,
          proposeSeatingLayoutBindingAction,
          recoverProposeSeatingLayoutBindingAction,
          publishSeatingPlanAction,
          releaseReservationBlockAction,
          requestSeatingExportAction,
          recallSeatingPlanAction,
          runS06EvaluationAction,
          submitCpsatCandidateAction,
          submitSeatingPlanAction,
          withdrawSeatingLayoutBindingAction,
          supersedeReservationBlockAction,
          withdrawReservationBlockAction,
        } from "../../../../../server/seating-actions";
        import { switchSeatingVerifyAsAction } from "../../../../../server/seating-verify-as-action";
        import { eventOsVerifyAsAvailable } from "../../../../../server/seating-verify-as";
        import { emitSettlementStage, PlatformError, retryLockApplies, seatingV2ReplacementEnabled, buildCpsatRunUiModel, WORKER_UNAVAILABLE_PUBLIC_MESSAGE, QUEUE_BUSY_PUBLIC_MESSAGE, type CpsatCandidateReviewModel } from "@maison-doclar/shared-platform";
        import { activateSeatingRuleAction, withdrawSeatingRuleAction } from "../../../../../server/seating-actions";
        import { CpsatRunStatusPanel } from "../../../../../components/cpsat-run-status-panel";
        import { CpsatRunLifecyclePoller } from "../../../../../components/cpsat-run-lifecycle-poller";
        import { CpsatCandidateReviewPanel } from "../../../../../components/cpsat-candidate-review-panel";
        import { CpsatWorkerUnavailablePanel } from "../../../../../components/cpsat-worker-unavailable-panel";
        
        function visibleSeatingRuns<T extends { id: string }>(runs: T[], currentRunId: string | undefined, limit = 12): T[] {
          if (runs.length <= limit) return runs;
          const current = currentRunId ? runs.find((run) => run.id === currentRunId) : undefined;
          if (!current) return runs.slice(-limit);
          return [...runs.filter((run) => run.id !== currentRunId).slice(-(limit - 1)), current];
        }
        
        function Envelope({ fields }: { fields: Record<string, string | number> }) {
          return (
            <>
              {Object.entries(fields).map(([name, value]) => (
                <input key={name} type="hidden" name={name} value={String(value)} />
              ))}
            </>
          );
        }
        
        const TABS = [
          { href: "#overview", label: "Overview" },
          { href: "#inputs", label: "Inputs" },
          { href: "#rules", label: "Rules" },
          { href: "#reservations", label: "Reservations" },
          { href: "#runs", label: "Runs" },
          { href: "#studio", label: "Studio" },
          { href: "#review", label: "Review" },
          { href: "#publication", label: "Publication" },
        ] as const;
        
        export const maxDuration = 60;
        
        export default async function EventSeatingPage({
          params,
          searchParams,
        }: {
          params: Promise<{ eventId: string }>;
          searchParams: Promise<Record<string, string | string[] | undefined>>;
        }) {
          const { eventId } = await params;
          const query = await searchParams;
          const { actor, person } = await guardedActor();
          const runtime = getRuntime();
          const organisations = runtime.service.listOrganisations(actor);
          const events = organisations.flatMap((item) => {
            try {
              return runtime.service.listEvents(actor, item.id);
            } catch {
              return [];
            }
          });
          let event = events.find((item) => item.id === eventId);
          const organisation = organisations.find((item) => item.id === event?.organisationId) ?? organisations[0];
          if (!event && organisation) {
            try {
              event = runtime.service.getEvent(actor, organisation.id, eventId);
            } catch {
              event = undefined;
            }
          }
          if (!event || !organisation) {
            const denialOrg = organisations[0];
            const denied = denialOrg ? !seatingPermissions(person, denialOrg.id, eventId).view : true;
            return (
              <AppShell person={person} current="/app/events">
                <AtelierOperationalState
                  state={operationalStateFromCode(
                    denied ? "FORBIDDEN" : "NOT_FOUND",
                    denied ? "This assignment cannot perform this seating action." : "The requested event is not available in this assignment.",
                  )}
                />
              </AppShell>
            );
          }
          const permissions = seatingPermissions(person, organisation.id, event.id);
          if (!permissions.view) {
            return (
              <AppShell person={person} organisationName={organisation.displayName} eventName={event.name} eventId={event.id} current="/app/events">
                <AtelierOperationalState state={operationalStateFromCode("FORBIDDEN", "This assignment cannot perform this seating action.")} />
              </AppShell>
            );
          }
          let workspace;
          let publicationSource: "CPSAT" | "NONE" = "NONE";
          const workspaceStarted = Date.now();
          try {
            if (seatingV2ReplacementEnabled()) {
              workspace = await runtime.service.seatingV2Commands().projectWorkspace(actor, event.id);
              if (workspace.currentPublication) {
                publicationSource = "CPSAT";
              }
            } else {
              workspace = await runtime.service.seatingCommands().projectWorkspace(actor, event.id);
              // Legacy seating command path is not operational after M6C; treat as empty.
              workspace = { ...workspace, currentPublication: undefined };
            }
          } catch (error) {
            const message = error instanceof PlatformError ? error.publicMessage ?? error.message : "This assignment cannot perform this seating action.";
            return (
              <AppShell person={person} organisationName={organisation.displayName} eventName={event.name} eventId={event.id} current="/app/events">
                <AtelierOperationalState state={operationalStateFromCode("FORBIDDEN", message)} />
              </AppShell>
            );
          }
          const workspaceMs = Date.now() - workspaceStarted;
          const assignmentId = preferredSeatingAssignment(person.id, organisation.id, event.id)?.id ?? "";
          const actionResultStarted = Date.now();
          const presented = await loadPresentedActionResult({
            requestPath: `/app/events/${event.id}/seating`,
            resultId: typeof query.result === "string" ? query.result : undefined,
            actorPersonId: person.id,
            organisationId: organisation.id,
            eventId: event.id,
          });
          const actionResultMs = Date.now() - actionResultStarted;
          const resultId = typeof query.result === "string" ? query.result : undefined;
          emitSettlementStage({
            stage: presented.correlationId && resultId && presented.correlationId === resultId ? "RENDER_RESULT_FOUND" : "RENDER_RESULT_MISSING",
            commandId: resultId,
            resultId,
            eventId: event.id,
            durationMs: actionResultMs,
            reasonClass: presented.correlationId ? "PRESENTED" : "ABSENT",
          });
          const envelopeFields = { organisationId: organisation.id, eventId: event.id, assignmentId };
          const reviewableLifecycles = new Set([
            "READY_FOR_REVIEW",
            "PENDING_APPROVAL",
            "APPROVED",
            "REJECTED",
            "ADOPTED",
            "SUPERSEDED",
          ]);
          const cpsatReviewRun = workspace.runs.find((run) =>
            reviewableLifecycles.has(String((run as { lifecycle?: string }).lifecycle ?? run.status ?? "")),
          );
          let cpsatReview: CpsatCandidateReviewModel | null = null;
          if (cpsatReviewRun && seatingV2ReplacementEnabled()) {
            try {
              cpsatReview = await runtime.service.seatingV2Commands().getCpsatCandidateReview(actor, event.id, cpsatReviewRun.id);
            } catch {
              cpsatReview = null;
            }
          }
          const working = workspace.workingEdition as {
            id?: string;
            contentHash?: string;
            status?: string;
            version?: number;
            stale?: boolean;
            sourceRunId?: string;
          } | undefined;
          const input = workspace.inputEdition as { id?: string; contentHash?: string; layoutContentHash?: string } | undefined;
          const publication = workspace.currentPublication as {
            id?: string;
            publicationNumber?: number;
            editionHash?: string;
            adoptionId?: string;
            runId?: string;
            productResult?: string | null;
            evidenceGrade?: string | null;
            seatedCount?: number;
            unseatedCount?: number;
            source?: string;
          } | undefined;
          const verifyAs = eventOsVerifyAsAvailable() && permissions.fixtureVerifyAs;
          const seatedDisplay = publication?.seatedCount ?? workspace.counts.seated;
          const unseatedDisplay = publication?.unseatedCount ?? workspace.counts.unseated;
          const eligibleDisplay = Math.max(workspace.counts.eligibleGuests, seatedDisplay + unseatedDisplay);
          return (
            <AppShell person={person} organisationName={organisation.displayName} eventName={event.name} eventId={event.id} current="/app/events">
              <AtelierPageHeader
                eyebrow="Seating Command"
                title={`${event.name} seating`}
                lede="The solver recommends. Authorised people decide. Published seating does not send messages, issue credentials or change check-in."
              />
              <p data-testid="seating-publication-badge">
                {publication
                  ? `Current operational publication: adoption ${(publication.adoptionId ?? publication.id ?? "").slice(0, 8)} · ${seatedDisplay}/${eligibleDisplay} seated`
                  : "No current operational publication"}
                {publication?.productResult ? ` · Solver result: ${publication.productResult}` : ""}
                {publication?.evidenceGrade ? ` · Evidence: ${publication.evidenceGrade}` : ""}
              </p>
              <p data-testid="seating-freshness-badge">Freshness: {workspace.freshnessCopy ?? workspace.inputFreshness}</p>
              <p data-testid="seating-worker-badge">
                Worker availability: {(workspace as { cpsatWorkerReadyCount?: number }).cpsatWorkerReadyCount != null
                  ? `${(workspace as { cpsatWorkerReadyCount?: number }).cpsatWorkerReadyCount} ready`
                  : "unknown"}
              </p>
              <p
                data-testid="seating-settlement"
                data-workspace-ms={workspaceMs}
                data-action-result-ms={actionResultMs}
                data-result-id={typeof query.result === "string" ? query.result : ""}
              >
                Render {workspaceMs}ms · action result {actionResultMs}ms
              </p>
              <ActionResultBanner presented={presented} />
              {(() => {
                const msg = presented.view?.message ?? "";
                if (msg === QUEUE_BUSY_PUBLIC_MESSAGE || msg.includes("No run was started")) {
                  return <CpsatWorkerUnavailablePanel reason="queue_busy" overviewHref="#overview" />;
                }
                if (
                  msg === WORKER_UNAVAILABLE_PUBLIC_MESSAGE ||
                  msg.includes("solver service is not ready") ||
                  msg.includes("temporarily unavailable")
                ) {
                  return <CpsatWorkerUnavailablePanel reason="unavailable" overviewHref="#overview" />;
                }
                return null;
              })()}
              <div className="seating-tabs">
                <AtelierSectionTabs label="Seating Command views" items={TABS} />
              </div>
        
              <section id="overview" className="atelier-panel" data-testid="seating-overview">
                <h2>Overview</h2>
                <ul className="seating-metric-cards">
                  <li><a href="#inputs">Input readiness · {workspace.inputFreshness}</a></li>
                  <li><a href="#studio">Eligible guests · {workspace.counts.eligibleGuests}</a></li>
                  <li><a href="#studio">Seated · {seatedDisplay}</a></li>
                  <li><a href="#studio">Unseated · {unseatedDisplay}</a></li>
                  <li><a href="#rules">Hard blockers · {workspace.counts.hardBlockers}</a></li>
                  <li>
                    <a href="#publication">
                      Current operational publication · {publication ? `adoption ${(publication.adoptionId ?? publication.id ?? "").slice(0, 8)}` : "None"}
                    </a>
                  </li>
                  <li>
                    <a href="#runs">
                      Workflow · {publicationSource === "CPSAT" ? "CP-SAT authority" : "No CP-SAT publication"}
                    </a>
                  </li>
                  <li>
                    <a href="#publication">
                      Working edition · {working ? `${working.status}${working.status === "PUBLISHED" ? "" : " / unpublished"}` : "None"}
                    </a>
                  </li>
                </ul>
                <h3>What needs attention</h3>
                {workspace.attention.length ? (
                  <ol>
                    {workspace.attention.map((item) => (
                      <li key={`${item.kind}-${item.message}`}>
                        <a href={item.href}>{item.message}</a>
                      </li>
                    ))}
                  </ol>
                ) : (
                  <p>No outstanding seating blockers.</p>
                )}
                <p data-testid="seating-next-action">Next authorised action: {workspace.nextAction}</p>
                {permissions.evaluate ? (
                  <ProtectionMutationForm action={runS06EvaluationAction.bind(null, event.id)} className="actions" testId="seating-evaluate">
                    <Envelope fields={envelopeFields} />
                    <IdempotencyField />
                    <button type="submit" className="button">
                      Run seating evaluation
                    </button>
                  </ProtectionMutationForm>
                ) : null}
                {workspace.evaluation ? (
                  <p data-testid="seating-evaluation-status">
                    Last evaluation {workspace.evaluation.corpusEdition}: {workspace.evaluation.status}
                    {typeof workspace.evaluation.passedCount === "number"
                      ? ` · ${workspace.evaluation.passedCount}/${workspace.evaluation.caseCount} passed`
                      : ` · ${workspace.evaluation.caseCount} cases`}
                    {workspace.evaluation.readinessResult ? ` · ${workspace.evaluation.readinessResult}` : ""}
                    {workspace.evaluation.safeFailureCodes?.length
                      ? ` · codes ${workspace.evaluation.safeFailureCodes.join(", ")}`
                      : ""}
                  </p>
                ) : null}
                {verifyAs ? (
                  <section className="atelier-panel" data-testid="seating-verify-as">
                    <h3>Synthetic verification</h3>
                    <p>This switches the signed-in fixture role. It is not Access Administration and cannot be used under production authorisation.</p>
                    <ProtectionMutationForm action={switchSeatingVerifyAsAction.bind(null, event.id)} className="atelier-form" testId="seating-verify-as-form">
                      <Envelope fields={envelopeFields} />
                      <IdempotencyField />
                      <label>
                        Fixture role
                        <select name="symbolicRole" required>
                          <option value="planner">Planner</option>
                          <option value="reviewer">Specialist reviewer</option>
                          <option value="director">Event Director</option>
                          <option value="ceo">CEO</option>
                          <option value="auditor">Auditor</option>
                        </select>
                      </label>
                      <button type="submit" className="button">
                        Switch fixture role
                      </button>
                    </ProtectionMutationForm>
                  </section>
                ) : null}
              </section>
        
              <section id="inputs" className="atelier-panel" data-testid="seating-inputs">
                <h2>Inputs</h2>
                <article>
                  <h3>Guest cohort</h3>
                  <p>{workspace.guests.length} governed guests. Eligible {workspace.counts.eligibleGuests}.</p>
                </article>
                <article>
                  <h3>RSVP truth</h3>
                  <p>Attendance intent is RSVP truth. Forecast does not overwrite it.</p>
                </article>
                <article data-testid="seating-layout-publication">
                  <h3>Venue layout tables</h3>
                  <p>
                    {workspace.tables.length
                      ? `${workspace.tables.length} bound published tables available for seating${workspace.tables.some((table) => table.mismatch) ? " · physical and declared capacity disagree" : ""}`
                      : workspace.seatingLayoutBinding?.status === "BOUND"
                        ? "The bound layout publication has no usable tables yet."
                        : "No venue layout tables are available for seating until a seating layout binding is active against a current layout publication. This is separate from the operational seating publication."}
                  </p>
                </article>
                <article data-testid="seating-layout-binding">
                  <h3>Seating layout binding</h3>
                  <p
                    data-testid="seating-layout-binding-status"
                    data-binding-status={workspace.seatingLayoutBinding?.status ?? "ABSENT"}
                    data-publication-number={workspace.seatingLayoutBinding?.publicationNumber ?? ""}
                    data-content-hash-prefix={workspace.seatingLayoutBinding?.contentHashPrefix ?? ""}
                    data-freeze-disabled={workspace.seatingLayoutBinding?.freezeDisabled ? "true" : "false"}
                  >
                    {workspace.seatingLayoutBinding?.status === "BOUND"
                      ? `${workspace.seatingLayoutBinding.layoutLabel ?? "Bound layout"} · CURRENT publication ${workspace.seatingLayoutBinding.publicationNumber} · hash ${workspace.seatingLayoutBinding.contentHashPrefix} · ${workspace.seatingLayoutBinding.tableCount ?? 0} tables · ${workspace.seatingLayoutBinding.physicalCapacity ?? 0} physical / ${workspace.seatingLayoutBinding.declaredCapacity ?? 0} declared`
                      : workspace.seatingLayoutBinding?.status === "AMBIGUOUS"
                        ? "More than one seating layout binding is active for this event."
                        : workspace.seatingLayoutBinding?.status === "STALE"
                          ? "The seating layout binding is stale."
                          : workspace.seatingLayoutBinding?.status === "MISMATCH"
                            ? "The seating layout binding could not be verified."
                            : "No active seating layout binding."}
                  </p>
                  {permissions.prepare && (workspace.seatingLayoutBindingCandidates?.length ?? 0) > 0 ? (
                    <ProtectionMutationForm
                      key={`seating-layout-binding-propose-${presented.correlationId ?? "idle"}`}
                      action={proposeSeatingLayoutBindingAction.bind(null, event.id)}
                      recoverAction={recoverProposeSeatingLayoutBindingAction.bind(null, event.id)}
                      className="atelier-form"
                      testId="seating-layout-binding-propose"
                    >
                      <Envelope
                        fields={{
                          ...envelopeFields,
                          // Server-minted key: stable for this form instance so transport failure
                          // retry can recover a durable commit via idempotent replay.
                          idempotencyKey: crypto.randomUUID(),
                        }}
                      />
                      <label>
                        Current layout publication
                        <select name="layoutPublicationId" required>
                          {workspace.seatingLayoutBindingCandidates?.map((candidate) => (
                            <option key={candidate.publicationId} value={candidate.publicationId}>
                              {candidate.layoutLabel} · CURRENT publication {candidate.publicationNumber} · hash {candidate.contentHash.slice(0, 12)} · {candidate.tableCount} tables
                            </option>
                          ))}
                        </select>
                      </label>
                      <PendingSubmit className="button" locked={presented.mutationLocked}>
                        Propose seating layout binding
                      </PendingSubmit>
                    </ProtectionMutationForm>
                  ) : null}
                  {permissions.ruleActivate && workspace.seatingLayoutBinding?.draftId ? (
                    <ProtectionMutationForm action={activateSeatingLayoutBindingAction.bind(null, event.id)} className="actions" testId="seating-layout-binding-activate">
                      <Envelope
                        fields={{
                          ...envelopeFields,
                          bindingId: workspace.seatingLayoutBinding.draftId,
                          expectedVersion: workspace.seatingLayoutBinding.draftVersion ?? 1,
                          layoutPublicationId: workspace.seatingLayoutBinding.draftPublicationId ?? "",
                          layoutContentHash: workspace.seatingLayoutBinding.draftContentHash ?? "",
                        }}
                      />
                      <IdempotencyField />
                      <div data-testid="seating-layout-binding-activate-identity">
                        <p data-testid="seating-layout-binding-proposal-review">
                          Pending proposal {workspace.seatingLayoutBinding.draftId.slice(0, 8)} · {workspace.seatingLayoutBinding.draftStatus ?? "DRAFT"} ·{" "}
                          {workspace.seatingLayoutBinding.draftLayoutLabel ?? "Proposed layout"} · CURRENT publication{" "}
                          {workspace.seatingLayoutBinding.draftPublicationNumber} · hash {workspace.seatingLayoutBinding.draftContentHashPrefix}
                        </p>
                        <p data-testid="seating-layout-binding-proposal-proposer">
                          Proposed by {workspace.seatingLayoutBinding.draftProposedByLabel ?? "Planner"}
                          {workspace.seatingLayoutBinding.draftProposedAt ? ` · ${workspace.seatingLayoutBinding.draftProposedAt}` : ""}
                        </p>
                        <p data-testid="seating-layout-binding-proposal-current">
                          {workspace.seatingLayoutBinding.status === "BOUND"
                            ? `Current authoritative binding: ${workspace.seatingLayoutBinding.activeLayoutLabel ?? "Bound layout"} · hash ${workspace.seatingLayoutBinding.activeContentHashPrefix ?? workspace.seatingLayoutBinding.contentHashPrefix ?? "—"}`
                            : "Current authoritative binding: none"}
                        </p>
                        <p data-testid="seating-layout-binding-proposal-target">
                          Proposed replacement: {workspace.seatingLayoutBinding.draftLayoutLabel ?? "Proposed layout"} · hash{" "}
                          {workspace.seatingLayoutBinding.draftContentHashPrefix}
                        </p>
                      </div>
                      <button type="submit" className="button">Activate seating layout binding</button>
                    </ProtectionMutationForm>
                  ) : null}
                  {permissions.prepare && workspace.seatingLayoutBinding?.draftId ? (
                    <ProtectionMutationForm action={withdrawSeatingLayoutBindingAction.bind(null, event.id)} className="actions" testId="seating-layout-binding-withdraw-draft">
                      <Envelope fields={{ ...envelopeFields, bindingId: workspace.seatingLayoutBinding.draftId, expectedVersion: workspace.seatingLayoutBinding.draftVersion ?? 1 }} />
                      <IdempotencyField />
                      <button type="submit" className="button">Withdraw draft binding</button>
                    </ProtectionMutationForm>
                  ) : null}
                  {permissions.ruleActivate && workspace.seatingLayoutBinding?.status === "BOUND" && workspace.seatingLayoutBinding.activeId ? (
                    <ProtectionMutationForm action={withdrawSeatingLayoutBindingAction.bind(null, event.id)} className="actions" testId="seating-layout-binding-withdraw">
                      <Envelope fields={{ ...envelopeFields, bindingId: workspace.seatingLayoutBinding.activeId, expectedVersion: workspace.seatingLayoutBinding.activeVersion ?? 1 }} />
                      <IdempotencyField />
                      <button type="submit" className="button">Withdraw active binding</button>
                    </ProtectionMutationForm>
                  ) : null}
                  <HistoryDisclosure
                    summary="Earlier seating layout bindings"
                    count={workspace.seatingLayoutBindingHistory?.length ?? 0}
                    testId="seating-layout-binding-history"
                  >
                    {(workspace.seatingLayoutBindingHistory ?? []).map((item, index) => (
                      <p key={`${item.state}-${item.contentHashPrefix ?? index}`}>
                        {item.state}
                        {item.publicationNumber != null ? ` · publication ${item.publicationNumber}` : ""}
                        {item.contentHashPrefix ? ` · hash ${item.contentHashPrefix}` : ""}
                      </p>
                    ))}
                  </HistoryDisclosure>
                </article>
                <article>
                  <h3>Event Brief</h3>
                  <p>Optional published brief facts only.</p>
                </article>
                <article>
                  <h3>Protection snapshot</h3>
                  <p>Optional coded protection constraints only.</p>
                </article>
                {permissions.prepare ? (
                  <ProtectionMutationForm action={freezeSeatingInputsAction.bind(null, event.id)} className="actions" testId="seating-freeze">
                    <Envelope fields={envelopeFields} />
                    <IdempotencyField />
                    <button
                      type="submit"
                      className="button"
                      disabled={
                        workspace.seatingLayoutBinding?.status !== "BOUND" || workspace.tables.some((table) => table.mismatch)
                      }
                    >
                      Freeze new input edition
                    </button>
                  </ProtectionMutationForm>
                ) : null}
                {input ? (
                  <details>
                    <summary>Input provenance</summary>
                    <p
                      data-testid="seating-input-hash"
                      data-hash={input.contentHash ?? ""}
                      data-layout-hash={input.layoutContentHash ?? ""}
                    >
                      Hash {input.contentHash}
                    </p>
                  </details>
                ) : (
                  <p data-testid="seating-input-hash" data-hash="" data-layout-hash="">
                    No frozen input edition.
                  </p>
                )}
                <HistoryDisclosure
                  summary="Earlier frozen input packages"
                  count={(workspace.inputPackageHistory ?? []).filter((item) => !item.current).length}
                  testId="seating-package-history"
                >
                  {(workspace.inputPackageHistory ?? [])
                    .filter((item) => !item.current)
                    .map((item) => (
                      <p key={item.contentHash || item.layoutContentHash}>
                        Hash {item.contentHash} · layout {item.layoutContentHash}
                      </p>
                    ))}
                </HistoryDisclosure>
              </section>
        
              <section id="rules" className="atelier-panel" data-testid="seating-rules">
                <h2>Rules</h2>
                {(["Governing", "Draft", "Historical"] as const).map((group) => {
                  const items = workspace.constraints.filter((item) => {
                    if (group === "Governing") return item.status === "ACTIVE" && item.duplicateRole !== "REDUNDANT_HISTORICAL";
                    if (group === "Draft") return item.status === "DRAFT";
                    return (
                      (item.status !== "ACTIVE" && item.status !== "DRAFT") ||
                      item.duplicateRole === "REDUNDANT_HISTORICAL"
                    );
                  }).slice(group === "Historical" ? -24 : undefined);
                  return (
                    <div key={group}>
                      <h3>{group}</h3>
                      <ul>
                        {items.map((item) => {
                          const softDraft = item.status === "DRAFT" && item.kind !== "HARD";
                          const hardDraft = item.status === "DRAFT" && item.kind === "HARD";
                          const alreadyActiveDraft = item.duplicateRole === "ALREADY_ACTIVE_DRAFT";
                          const hardConflict = Boolean(item.hardConflictEditionId);
                          const canActivate =
                            !alreadyActiveDraft &&
                            ((hardDraft && permissions.ruleActivate) || (softDraft && permissions.constraintManage));
                          const canWithdraw =
                            item.duplicateRole !== "REDUNDANT_HISTORICAL" &&
                            (item.status === "DRAFT" || item.status === "ACTIVE") &&
                            permissions.constraintManage;
                          return (
                            <li
                              key={item.id}
                              data-testid={
                                item.duplicateRole === "AUTHORITATIVE"
                                  ? "seating-rule-authoritative"
                                  : item.duplicateRole === "REDUNDANT_HISTORICAL"
                                    ? "seating-rule-redundant-historical"
                                    : item.duplicateRole === "ALREADY_ACTIVE_DRAFT"
                                      ? "seating-rule-already-active-draft"
                                      : hardConflict
                                        ? "seating-rule-hard-conflict-draft"
                                        : "seating-rule-item"
                              }
                              data-duplicate-role={item.duplicateRole ?? ""}
                              data-authoritative-id={item.authoritativeEditionId ?? ""}
                              data-hard-conflict-id={item.hardConflictEditionId ?? ""}
                            >
                              {item.preview}
                              {item.duplicateRole === "AUTHORITATIVE" && (item.redundantActiveCount ?? 0) > 0 ? (
                                <p data-testid="seating-rule-authoritative-note">
                                  Authoritative governing rule for this semantic content. {item.redundantActiveCount} redundant ACTIVE
                                  duplicate{item.redundantActiveCount === 1 ? "" : "s"} predate the uniqueness invariant and are listed under Historical.
                                </p>
                              ) : null}
                              {item.duplicateRole === "REDUNDANT_HISTORICAL" ? (
                                <p data-testid="seating-rule-redundant-note">
                                  Redundant historical duplicate of authoritative rule {item.authoritativeEditionId?.slice(0, 8)}. Not separately governing.
                                  Retained for audit/replay; predates semantic uniqueness enforcement.
                                </p>
                              ) : null}
                              {alreadyActiveDraft ? (
                                <p data-testid="seating-rule-already-active">
                                  ALREADY ACTIVE — equivalent rule {item.authoritativeEditionId?.slice(0, 8)} already governs this scope. Activation would make no data change.
                                </p>
                              ) : null}
                              {hardConflict ? (
                                <p data-testid="seating-rule-hard-conflict">
                                  Conflicts with ACTIVE HARD {(item.hardConflictKind ?? "rule").replaceAll("_", " ")} rule
                                  edition {item.hardConflictEditionId?.slice(0, 8)}
                                  {item.hardConflictContentHashPrefix
                                    ? ` · content hash ${item.hardConflictContentHashPrefix}`
                                    : ""}
                                  . Withdraw or supersede that governing rule through a governed action before activating this
                                  replacement. Activation is blocked while the contradiction remains.
                                </p>
                              ) : null}
                              {canActivate ? (
                                <ProtectionMutationForm action={activateSeatingRuleAction.bind(null, event.id)} className="actions">
                                  <Envelope
                                    fields={{
                                      ...envelopeFields,
                                      editionId: item.id,
                                      expectedVersion: item.editionNo ?? 1,
                                      expectedContentHash: item.contentHash ?? "",
                                    }}
                                  />
                                  <IdempotencyField />
                                  <button type="submit" className="button secondary">Activate</button>
                                </ProtectionMutationForm>
                              ) : null}
                              {canWithdraw ? (
                                <ProtectionMutationForm action={withdrawSeatingRuleAction.bind(null, event.id)} className="actions">
                                  <Envelope
                                    fields={{
                                      ...envelopeFields,
                                      editionId: item.id,
                                      expectedVersion: item.editionNo ?? 1,
                                      expectedContentHash: item.contentHash ?? "",
                                      reason: "Withdrawn from governing set",
                                    }}
                                  />
                                  <IdempotencyField />
                                  <button type="submit" className="button secondary">Withdraw</button>
                                </ProtectionMutationForm>
                              ) : null}
                            </li>
                          );
                        })}
                      </ul>
                    </div>
                  );
                })}
                {permissions.constraintManage ? (
                  <ProtectionMutationForm
                    key={`seating-constraint-form-${presented.correlationId ?? "idle"}`}
                    action={createSeatingConstraintAction.bind(null, event.id)}
                    className="atelier-form seating-form"
                    testId="seating-constraint-form"
                  >
                    <Envelope
                      fields={{
                        ...envelopeFields,
                        // Server-minted key: stable for this form instance so transport failure
                        // retry can recover a durable commit via idempotent replay.
                        idempotencyKey: crypto.randomUUID(),
                      }}
                    />
                    <fieldset>
                      <legend>Create a seating rule</legend>
                      <label>
                        Name
                        <input name="name" required defaultValue="Keep these guests together" />
                      </label>
                      <label>
                        Kind
                        <select name="kind" required>
                          <option value="HARD">Hard rule</option>
                          <option value="WEIGHTED">Weighted preference</option>
                          <option value="INFORMATION">Information only</option>
                        </select>
                      </label>
                      <SeatingRuleAuthoringFields guests={workspace.guests} tables={workspace.tables} />
                      <label>
                        Weight
                        <input name="weight" type="number" min={1} defaultValue={1} />
                      </label>
                      <label>
                        Evidence
                        <input name="evidence" defaultValue="Planner note" />
                      </label>
                      <label>
                        Review domain
                        <select name="reviewDomain">
                          <option value="">None</option>
                          <option value="PROTOCOL">Protocol</option>
                          <option value="ACCESSIBILITY">Accessibility</option>
                          <option value="SECURITY">Security</option>
                        </select>
                      </label>
                    </fieldset>
                    <PendingSubmit className="button" locked={presented.mutationLocked}>
                      Save rule
                    </PendingSubmit>
                  </ProtectionMutationForm>
                ) : null}
              </section>
        
              <section id="reservations" className="atelier-panel" data-testid="seating-reservations">
                <h2>Reservations</h2>
                <p>Reserved does not mean seated.</p>
                <p data-testid="seating-capacity-ledger">
                  Capacity {workspace.capacityLedger.total} · generally available {workspace.capacityLedger.generallyAvailable} · reserved minima {workspace.capacityLedger.reservedMin} · reserved maxima {workspace.capacityLedger.reservedMax}
                  {workspace.capacityLedger.overbooked ? " · unresolved overbooking" : ""}
                </p>
                <ul>
                  {workspace.reservations.map((item) => (
                    <li key={item.id} data-testid={`seating-reservation-${item.releaseState}`}>
                      {item.preview ?? `${item.setCode} · ${item.releaseState}`}. Reserved does not mean seated.
                      {item.editionNo != null ? ` Edition ${item.editionNo}.` : ""}
                      {permissions.ruleActivate && item.releaseState === "DRAFT" ? (
                        <ProtectionMutationForm action={activateReservationBlockAction.bind(null, event.id)} className="actions">
                          <Envelope fields={{ ...envelopeFields, blockId: item.id, expectedVersion: item.editionNo ?? 1, expectedContentHash: item.contentHash ?? "" }} />
                          <IdempotencyField />
                          <button type="submit" className="button secondary">Activate reservation</button>
                        </ProtectionMutationForm>
                      ) : null}
                      {permissions.reservationManage && (item.releaseState === "DRAFT" || item.releaseState === "ACTIVE") ? (
                        <ProtectionMutationForm action={withdrawReservationBlockAction.bind(null, event.id)} className="actions">
                          <Envelope fields={{ ...envelopeFields, blockId: item.id, expectedVersion: item.editionNo ?? 1, expectedContentHash: item.contentHash ?? "", reason: "Withdrawn from governing set" }} />
                          <IdempotencyField />
                          <button type="submit" className="button secondary">Withdraw reservation</button>
                        </ProtectionMutationForm>
                      ) : null}
                      {permissions.reservationManage && item.releaseState === "ACTIVE" ? (
                        <>
                          <ProtectionMutationForm action={supersedeReservationBlockAction.bind(null, event.id)} className="actions" testId={`seating-reservation-successor-${item.id}`}>
                            <Envelope
                              fields={{
                                ...envelopeFields,
                                blockId: item.id,
                                expectedVersion: item.editionNo ?? 1,
                                expectedContentHash: item.contentHash ?? "",
                                eligibleGuestIds: workspace.guests.filter((guest) => guest.eligible).map((guest) => guest.id).join(","),
                                exactCount: item.exact ?? 2,
                              }}
                            />
                            <IdempotencyField />
                            <button type="submit" className="button secondary">Create successor reservation</button>
                          </ProtectionMutationForm>
                          <ProtectionMutationForm action={releaseReservationBlockAction.bind(null, event.id)} className="actions">
                            <Envelope fields={{ ...envelopeFields, blockId: item.id, expectedVersion: item.editionNo ?? 1, expectedContentHash: item.contentHash ?? "" }} />
                            <IdempotencyField />
                            <button type="submit" className="button secondary">Release reservation</button>
                          </ProtectionMutationForm>
                        </>
                      ) : null}
                    </li>
                  ))}
                </ul>
                {permissions.reservationManage ? (
                  <ProtectionMutationForm action={createReservationBlockAction.bind(null, event.id)} className="atelier-form seating-form" testId="seating-reservation-form">
                    <Envelope fields={envelopeFields} />
                    <IdempotencyField />
                    <fieldset>
                      <legend>Create a reservation</legend>
                      <label>
                        Guest set
                        <select name="eligibleSetCode" required>
                          <option value="ELIGIBLE_ATTENDING">Eligible attending guests</option>
                          <option value="PROTOCOL_PRIORITY">Protocol priority guests</option>
                        </select>
                      </label>
                      <input type="hidden" name="eligibleGuestIds" value={workspace.guests.filter((item) => item.eligible).map((item) => item.id).join(",")} />
                      <label>
                        Table
                        <select name="tableId">
                          <option value="">Any published table</option>
                          {workspace.tables.map((table) => (
                            <option key={table.id} value={table.id}>{table.label}</option>
                          ))}
                        </select>
                      </label>
                      <label>
                        Exact count
                        <input name="exactCount" type="number" min={1} defaultValue={2} />
                      </label>
                      <label>
                        Priority
                        <input name="priority" type="number" min={1} defaultValue={10} />
                      </label>
                    </fieldset>
                    <button type="submit" className="button">
                      Save reservation
                    </button>
                  </ProtectionMutationForm>
                ) : null}
              </section>
        
              <section id="runs" className="atelier-panel" data-testid="seating-runs">
                <h2>Runs</h2>
                <p>The solver recommends. Authorised people decide. Cancellation asks the worker to stop; a running attempt is not instantly erased.</p>
                {permissions.run && input ? (
                  <ProtectionMutationForm action={launchSeatingRunAction.bind(null, event.id)} className="atelier-form seating-form" testId="seating-run-form">
                    <Envelope fields={{ ...envelopeFields, inputEditionId: input.id ?? "" }} />
                    <IdempotencyField />
                    <fieldset>
                      <legend>Launch a seating run</legend>
                      <p className="help">
                        Generate seating plan freezes the current governed layout, rules, guests and objectives,
                        then queues a durable CP-SAT run for the solver service.
                      </p>
                      <label>
                        Deterministic seed
                        <input name="seed" defaultValue={`s06-${event.id.slice(-4)}`} readOnly />
                      </label>
                    </fieldset>
                    <button type="submit" className="button" style={{ cursor: "pointer" }}>
                      Generate seating plan
                    </button>
                  </ProtectionMutationForm>
                ) : null}
                {!workspace.runs.length ? (
                  <CpsatRunStatusPanel
                    model={buildCpsatRunUiModel({
                      operatorLifecycle: "NONE",
                      lifecycle: "NONE",
                      productResult: "",
                      resultStatus: null,
                      freshness: "CURRENT",
                      evidenceGrade: null,
                    })}
                  />
                ) : null}
                {(() => {
                  const pollTargets = workspace.runs.filter((run) => {
                    const lifecycle = String(run.lifecycle ?? run.status ?? "");
                    return (
                      ["QUEUED", "CLAIMED", "RUNNING", "BUILDING", "SEARCHING", "VERIFYING", "EXPLAINING", "PERSISTING"].includes(
                        lifecycle,
                      ) || Boolean(run.cancelRequested && !["CANCELLED", "READY_FOR_REVIEW", "FAILED", "CLOSED_NO_PLAN"].includes(lifecycle))
                    );
                  });
                  const primary = pollTargets[0];
                  return (
                    <CpsatRunLifecyclePoller
                      active={pollTargets.length > 0}
                      lifecycle={primary ? String(primary.lifecycle ?? primary.status) : null}
                      intervalMs={5_000}
                    />
                  );
                })()}
                <ul>
                  {visibleSeatingRuns(workspace.runs, workspace.currentRunId).map((run) => {
                    const isCurrent = run.id === workspace.currentRunId || Boolean(run.current);
                    const outcome = run.validatorVerdict ?? run.resultStatus ?? run.status;
                    const cancelRequested = Boolean(run.cancelRequested);
                    const queued = run.status === "QUEUED" || run.lifecycle === "QUEUED";
                    const lifecycle = String(run.lifecycle ?? run.status);
                    const phase =
                      run.progressPhase ??
                      (lifecycle === "RUNNING"
                        ? "Generating seating plan"
                        : queued
                          ? "queued"
                          : lifecycle === "READY_FOR_REVIEW"
                            ? "sealed"
                            : "settled");
                    return (
                      <li key={run.id} data-testid={`seating-run-${run.status}`}>
                        <article
                          data-testid="seating-run-card"
                          data-run-id={run.id}
                          data-current={isCurrent ? "true" : "false"}
                          data-stale={run.stale ? "true" : "false"}
                          data-outcome={outcome}
                          data-cancel-requested={cancelRequested ? "true" : "false"}
                        >
                          {isCurrent ? (
                            <CpsatRunStatusPanel
                              model={buildCpsatRunUiModel({
                                productResult: String(outcome === "QUEUED" || outcome === "CLAIMED" || outcome === "RUNNING" ? "" : outcome),
                                lifecycle,
                                resultStatus: run.resultStatus ?? (queued || lifecycle === "RUNNING" || lifecycle === "CLAIMED" ? null : String(outcome)),
                                phase,
                                elapsedMs: 0,
                                seated: run.seated ?? 0,
                                eligible: (run.seated ?? 0) + (run.unseated ?? 0),
                                occupiedTables: 0,
                                tableCapacity: 0,
                                freshness: run.stale ? "STALE" : (run.freshnessGrade as "CURRENT" | "FRESH" | "STALE" | undefined) ?? "CURRENT",
                                evidenceGrade: run.evidenceGrade ?? null,
                                purpose: run.purpose,
                                mode: run.mode,
                                createdAt: run.startedAt,
                                completedAt: run.completedAt ?? null,
                                cancelRequested,
                                assignmentHash: run.assignmentsHash ?? null,
                                hasCompleteIncumbent: lifecycle === "READY_FOR_REVIEW" || outcome === "FEASIBLE" || outcome === "OPTIMAL",
                                faultCode: run.faultCode ?? null,
                              })}
                            />
                          ) : null}
                          <p data-testid="seating-run-identity">
                            Run <code>{run.id.slice(0, 8)}</code>
                            {isCurrent ? " · Current" : " · Not current"}
                            {run.stale ? " · Stale" : " · Fresh"}
                            {" · "}
                            {cancelRequested ? "Cancellation requested" : outcome}
                          </p>
                          <CanonicalId id={run.id} label="Full immutable run ID" testId="seating-run-full-id" />
                          <div data-testid="seating-run-started">
                            Started{" "}
                            {run.startedAt ? <CanonicalTime iso={run.startedAt} /> : "unavailable"}
                            {" · Initiating actor: "}
                            {run.initiatingActorLabel ?? "unavailable"}
                          </div>
                          <p data-testid="seating-run-counts">
                            Seated {run.seated ?? 0} · Unseated {run.unseated ?? 0}
                          </p>
                          {isCurrent && run.stale ? (
                            <p data-testid="seating-run-current-stale">
                              This is the selected/current run, and it is also stale because upstream seating inputs changed after it was produced. Review and run again before treating it as fresh authority.
                            </p>
                          ) : null}
                          {run.stale && !isCurrent ? (
                            <p>Upstream event information changed after this run. Review and run again.</p>
                          ) : null}
                          {run.validatorVerdict === "INFEASIBLE" || run.status === "INFEASIBLE" ? (
                            <p>No safe seating plan satisfies every hard rule.</p>
                          ) : null}
                          {run.violatedSummary ? <p>Violated: {run.violatedSummary}</p> : null}
                          {permissions.edit &&
                          !seatingV2ReplacementEnabled() &&
                          !run.stale &&
                          (run.validatorVerdict === "FEASIBLE" || run.status === "FEASIBLE" || run.status === "INFEASIBLE") ? (
                            <ProtectionMutationForm action={adoptSeatingRunAction.bind(null, event.id)} className="actions">
                              <Envelope fields={{ ...envelopeFields, runId: run.id }} />
                              <IdempotencyField />
                              <button type="submit" className="button secondary" style={{ cursor: "pointer" }}>Adopt run</button>
                            </ProtectionMutationForm>
                          ) : null}
                          {permissions.run && queued && !cancelRequested ? (
                            <ProtectionMutationForm action={cancelSeatingRunAction.bind(null, event.id)} className="actions">
                              <Envelope fields={{ ...envelopeFields, runId: run.id, expectedVersion: 0 }} />
                              <IdempotencyField />
                              <button type="submit" className="button secondary" style={{ cursor: "pointer" }}>Cancel run</button>
                            </ProtectionMutationForm>
                          ) : null}
                        </article>
                      </li>
                    );
                  })}
                </ul>
              </section>
        
              <section id="studio" className="atelier-panel" data-testid="seating-studio">
                <h2>Studio</h2>
                <div className="seating-studio-grid">
                  <div>
                    <h3>Tables</h3>
                    {workspace.tables.length ? (
                      <ul>
                        {workspace.tables.map((table) => (
                          <li key={table.id} data-testid="seating-table-capacity">
                            {table.label} · {table.seated}/{table.capacity}
                            {table.mismatch ? " · capacity mismatch" : ""}
                          </li>
                        ))}
                      </ul>
                    ) : (
                      <div data-testid="seating-tables-empty" className="atelier-empty">
                        <p>No table layout is configured for seating placements.</p>
                        <p>
                          Placements cannot be displayed until a current venue layout publication is bound for this event.
                          {permissions.prepare
                            ? " Propose and activate a seating layout binding under Inputs."
                            : " Ask an authorised planner or director to bind a current layout publication. This assignment cannot change the binding."}
                        </p>
                        <p>
                          <a href="#inputs">Open Inputs · seating layout binding</a>
                        </p>
                      </div>
                    )}
                  </div>
                  <div>
                    <h3>Guests</h3>
                    <ul>
                      {workspace.workingAssignments.length
                        ? workspace.workingAssignments.map((item) => (
                            <li key={item.guestId}>
                              {item.guestLabel} · {item.state === "UNSEATED" ? "This guest remains unseated; the system did not invent a placement." : item.tableId} · {item.lockState}
                            </li>
                          ))
                        : workspace.guests.map((guest) => (
                            <li key={guest.id}>{guest.label} · {guest.eligible ? "Eligible" : guest.eligibilityCode}</li>
                          ))}
                    </ul>
                  </div>
                </div>
                {permissions.edit && (working?.status === "DRAFT" || working?.status === "WORKING") ? (
                  <ProtectionMutationForm action={applySeatingChangeAction.bind(null, event.id)} className="atelier-form seating-form" testId="seating-edit-form">
                    <div
                      hidden
                      data-testid="seating-edit-identity"
                      data-edition-id={working.id ?? ""}
                      data-edition-version={String(working.version ?? 0)}
                      data-edition-hash={working.contentHash ?? ""}
                    />
                    <Envelope
                      fields={{
                        ...envelopeFields,
                        editionId: working.id ?? "",
                        expectedVersion: working.version ?? 0,
                        expectedContentHash: working.contentHash ?? "",
                      }}
                    />
                    <IdempotencyField />
                    <fieldset>
                      <legend>Apply a seating change</legend>
                      <label>
                        Guest
                        <select name="guestId" required>
                          {workspace.workingAssignments.map((item) => (
                            <option key={item.guestId} value={item.guestId}>{item.guestLabel}</option>
                          ))}
                        </select>
                      </label>
                      <label>
                        Action
                        <select name="command" required>
                          <option value="ASSIGN_UNSEATED">Assign unseated</option>
                          <option value="MOVE">Move</option>
                          <option value="SWAP">Swap</option>
                          <option value="UNSEAT">Unseat</option>
                          <option value="LOCK">Lock</option>
                          <option value="UNLOCK">Unlock</option>
                        </select>
                      </label>
                      <label>
                        Target seat
                        <select name="targetPositionId">
                          <option value="">Choose seat</option>
                          {workspace.positions.map((position) => {
                            const occupied = workspace.workingAssignments.some(
                              (item) => item.state === "SEATED" && item.positionId === position.positionToken,
                            );
                            return (
                              <option key={position.id} value={position.positionToken} data-occupied={occupied ? "true" : "false"}>
                                {position.positionToken}
                              </option>
                            );
                          })}
                        </select>
                      </label>
                      <label>
                        Other guest
                        <select name="otherGuestId">
                          <option value="">None</option>
                          {workspace.workingAssignments.map((item) => (
                            <option key={`other-${item.guestId}`} value={item.guestId}>{item.guestLabel}</option>
                          ))}
                        </select>
                      </label>
                    </fieldset>
                    <label>
                      Unseat reason
                      <select name="reasonCode" defaultValue="MANUAL_UNSEAT">
                        <option value="MANUAL_UNSEAT">Manual unseat</option>
                        <option value="GOVERNED_UNSEATED">Governed exception</option>
                      </select>
                    </label>
                    <PendingSubmit
                      locked={
                        Boolean(resultId) &&
                        (presented.mutationLocked ||
                          presented.retryLock?.actionScope === "seating.plan.edit" ||
                          retryLockApplies(presented.retryLock, {
                            actionScope: "seating.plan.edit",
                            subjectId: working.id ?? "",
                            attemptedVersion: working.version,
                          }))
                      }
                    >
                      Apply seating change
                    </PendingSubmit>
                  </ProtectionMutationForm>
                ) : (
                  <p>Adopt a run before editing seats.</p>
                )}
              </section>
        
              <section id="review" className="atelier-panel" data-testid="seating-review">
                <h2>Review</h2>
                <p data-testid="seating-review-lineage">Package → Run → Validation → Plan edition</p>
                {cpsatReview ? (
                  <>
                    <CpsatCandidateReviewPanel
                      review={cpsatReview}
                      runHref={`#runs`}
                      showMutationControls={Boolean(permissions.submit || permissions.approve || permissions.publish)}
                    />
                    {/* Role-accurate mutation controls: Auditor never sees these forms. */}
                    {permissions.submit && cpsatReview.actions.canSubmit && cpsatReview.freshness !== "STALE" ? (
                      <ProtectionMutationForm
                        action={submitCpsatCandidateAction.bind(null, event.id)}
                        className="actions"
                        testId="cpsat-submit-for-approval"
                      >
                        <Envelope
                          fields={{
                            ...envelopeFields,
                            runId: cpsatReview.runId,
                            candidateId: cpsatReview.candidateId,
                            assignmentHash: cpsatReview.assignmentHash,
                          }}
                        />
                        <IdempotencyField />
                        <button type="submit" className="button primary" style={{ cursor: "pointer" }}>
                          Submit for approval
                        </button>
                      </ProtectionMutationForm>
                    ) : null}
                    {permissions.approve && cpsatReview.actions.canApprove && cpsatReview.approvalHistory.proposalId && cpsatReview.freshness !== "STALE" ? (
                      <ProtectionMutationForm
                        action={decideCpsatCandidateAction.bind(null, event.id)}
                        className="actions"
                        testId="cpsat-approve-candidate"
                      >
                        <Envelope
                          fields={{
                            ...envelopeFields,
                            runId: cpsatReview.runId,
                            candidateId: cpsatReview.candidateId,
                            assignmentHash: cpsatReview.assignmentHash,
                            proposalId: cpsatReview.approvalHistory.proposalId,
                            decision: "APPROVED",
                          }}
                        />
                        <IdempotencyField />
                        <button type="submit" className="button primary" style={{ cursor: "pointer" }}>
                          Approve seating plan
                        </button>
                      </ProtectionMutationForm>
                    ) : null}
                    {permissions.approve && cpsatReview.actions.canReject && cpsatReview.approvalHistory.proposalId ? (
                      <ProtectionMutationForm
                        action={decideCpsatCandidateAction.bind(null, event.id)}
                        className="actions"
                        testId="cpsat-reject-candidate"
                      >
                        <Envelope
                          fields={{
                            ...envelopeFields,
                            runId: cpsatReview.runId,
                            candidateId: cpsatReview.candidateId,
                            assignmentHash: cpsatReview.assignmentHash,
                            proposalId: cpsatReview.approvalHistory.proposalId,
                            decision: "REJECTED",
                          }}
                        />
                        <IdempotencyField />
                        <label>
                          Rejection reason
                          <input name="reason" required minLength={3} data-testid="cpsat-reject-reason" />
                        </label>
                        <button type="submit" className="button secondary" style={{ cursor: "pointer" }}>
                          Reject seating plan
                        </button>
                      </ProtectionMutationForm>
                    ) : null}
                    {permissions.publish && cpsatReview.actions.canAdopt && cpsatReview.approvalHistory.proposalId && cpsatReview.freshness !== "STALE" ? (
                      <ProtectionMutationForm
                        action={adoptCpsatCandidateAction.bind(null, event.id)}
                        className="actions"
                        testId="cpsat-adopt-candidate"
                      >
                        <Envelope
                          fields={{
                            ...envelopeFields,
                            runId: cpsatReview.runId,
                            candidateId: cpsatReview.candidateId,
                            assignmentHash: cpsatReview.assignmentHash,
                            approvalId: cpsatReview.approvalHistory.proposalId,
                          }}
                        />
                        <IdempotencyField />
                        <button type="submit" className="button primary" style={{ cursor: "pointer" }}>
                          Adopt seating plan
                        </button>
                      </ProtectionMutationForm>
                    ) : null}
                  </>
                ) : null}
                <p data-testid="seating-review-event">
                  {event.name}
                  {" · "}
                  {publication
                    ? `Current operational publication: adoption ${(publication.adoptionId ?? publication.id ?? "").slice(0, 8)} · ${seatedDisplay}/${eligibleDisplay} seated`
                    : "No current operational publication"}
                  {" · "}
                  {working ? `Current working edition: ${working.status}${working.status === "PUBLISHED" ? "" : " / unpublished"}` : "No working edition"}
                </p>
                <p data-testid="seating-review-requirement">{workspace.reviewRequirementCopy}</p>
                <ul data-testid="seating-review-evidence">
                  {workspace.constraints.filter((item) => item.reviewDomain && workspace.implicatedReviewDomains.includes(item.reviewDomain as "PROTOCOL" | "ACCESSIBILITY" | "SECURITY")).map((item) => (
                    <li key={item.id}>{item.reviewDomain} · {item.preview} · {item.status}</li>
                  ))}
                </ul>
                <details>
                  <summary>Plan hash provenance</summary>
                  <p data-testid="seating-plan-hash">Working edition hash: {working?.contentHash ?? "None"}</p>
                </details>
                <h3>Manual decisions</h3>
                <ul>{workspace.decisions.map((item) => <li key={item.id}>{item.command} · {item.reasonCode}</li>)}</ul>
                <h3>Specialist reviews</h3>
                <ul>{workspace.reviews.map((item) => <li key={item.id}>{item.domain} · {item.reviewerLabel} · {item.decision} · {item.reason}</li>)}</ul>
                {permissions.submit && (working?.status === "DRAFT" || working?.status === "WORKING") ? (
                  <ProtectionMutationForm action={submitSeatingPlanAction.bind(null, event.id)} className="actions">
                    <Envelope
                      fields={{
                        ...envelopeFields,
                        editionId: working.id ?? "",
                        expectedVersion: working.version ?? 0,
                        expectedContentHash: working.contentHash ?? "",
                      }}
                    />
                    <IdempotencyField />
                    <button type="submit" className="button">Submit seating plan</button>
                  </ProtectionMutationForm>
                ) : null}
                {permissions.submit && working?.status === "SUBMITTED" ? (
                  <ProtectionMutationForm action={recallSeatingPlanAction.bind(null, event.id)} className="actions" testId="seating-recall">
                    <Envelope fields={{ ...envelopeFields, editionId: working.id ?? "", expectedVersion: working.version ?? 0, expectedContentHash: working.contentHash ?? "" }} />
                    <IdempotencyField />
                    <button type="submit" className="button secondary">Recall submitted plan</button>
                  </ProtectionMutationForm>
                ) : null}
                {(permissions.reviewProtocol || permissions.reviewAccessibility || permissions.reviewSecurity) && working?.status === "SUBMITTED" && workspace.implicatedReviewDomains.length ? (
                  <ProtectionMutationForm action={decideSeatingReviewAction.bind(null, event.id)} className="atelier-form seating-form" testId="seating-review-form">
                    <Envelope fields={{ ...envelopeFields, editionId: working.id ?? "", editionHash: working.contentHash ?? "", expectedVersion: working.version ?? 0, expectedContentHash: working.contentHash ?? "" }} />
                    <IdempotencyField />
                    <fieldset>
                      <legend>Record specialist review</legend>
                      <label>
                        Domain
                        <select name="domain" required>
                          {workspace.implicatedReviewDomains.includes("PROTOCOL") && permissions.reviewProtocol ? <option value="PROTOCOL">Protocol</option> : null}
                          {workspace.implicatedReviewDomains.includes("ACCESSIBILITY") && permissions.reviewAccessibility ? <option value="ACCESSIBILITY">Accessibility</option> : null}
                          {workspace.implicatedReviewDomains.includes("SECURITY") && permissions.reviewSecurity ? <option value="SECURITY">Security</option> : null}
                        </select>
                      </label>
                      <label>
                        Decision
                        <select name="decision" required>
                          <option value="APPROVED">Approve review</option>
                          <option value="REJECTED">Request correction</option>
                        </select>
                      </label>
                      <label>
                        Reason
                        <input name="reason" required defaultValue="Reviewed against coded constraints" />
                      </label>
                    </fieldset>
                    <button type="submit" className="button">Record review</button>
                  </ProtectionMutationForm>
                ) : null}
                {permissions.approve && working?.status === "SUBMITTED" ? (
                  <ProtectionMutationForm action={decideSeatingApprovalAction.bind(null, event.id)} className="actions" testId="seating-approve">
                    <Envelope fields={{ ...envelopeFields, editionId: working.id ?? "", editionHash: working.contentHash ?? "", expectedVersion: working.version ?? 0, expectedContentHash: working.contentHash ?? "" }} />
                    <IdempotencyField />
                    <input type="hidden" name="decision" value="APPROVED" />
                    <button type="submit" className="button">Approve seating plan</button>
                  </ProtectionMutationForm>
                ) : null}
              </section>
        
              <section id="publication" className="atelier-panel" data-testid="seating-publication">
                <h2>Publication</h2>
                <p>Published without sending messages, issuing credentials or changing check-in.</p>
                <article data-testid="seating-current-publication">
                  <h3>Current operational publication</h3>
                  <p>
                    {publication
                      ? `CP-SAT adoption ${publication.adoptionId ?? publication.id} is the operational seating (${seatedDisplay}/${eligibleDisplay} seated). Solver result: ${publication.productResult ?? "n/a"}. Evidence: ${publication.evidenceGrade ?? "n/a"}.`
                      : "No current operational publication."}
                  </p>
                  {publication?.runId ? (
                    <p data-testid="seating-publication-run">
                      Adopted run <code>{publication.runId}</code>
                    </p>
                  ) : null}
                </article>
                <article data-testid="seating-working-edition">
                  <h3>Current working edition</h3>
                  <p>
                    {working
                      ? working.status === "PUBLISHED"
                        ? `PUBLISHED · ${working.contentHash}`
                        : `${working.status} / unpublished · ${working.contentHash}`
                      : "No working edition."}
                  </p>
                  {publication && working && working.status !== "PUBLISHED" ? (
                    <p data-testid="seating-publication-dual-truth">
                      Operational publication remains current while this working edition stays unpublished.
                    </p>
                  ) : null}
                </article>
                <article>
                  <h3>Operational approval</h3>
                  <ul>{workspace.approvals.map((item) => <li key={item.id}>{item.decision}</li>)}</ul>
                </article>
                <article>
                  <h3>History</h3>
                  <ul>{workspace.publications.map((item) => <li key={item.id}>{item.status} · {item.publicationNumber} · {item.editionHash}</li>)}</ul>
                </article>
                {permissions.publish && !seatingV2ReplacementEnabled() && working?.status === "APPROVED" ? (
                  <ProtectionMutationForm action={publishSeatingPlanAction.bind(null, event.id)} className="actions" testId="seating-publish">
                    <Envelope fields={{ ...envelopeFields, editionId: working.id ?? "", editionHash: working.contentHash ?? "", expectedVersion: working.version ?? 0, expectedContentHash: working.contentHash ?? "" }} />
                    <IdempotencyField />
                    <p>Published without sending messages, issuing credentials or changing check-in.</p>
                    <button type="submit" className="button">Publish seating plan</button>
                  </ProtectionMutationForm>
                ) : null}
                {permissions.exportJob ? (
                  <ProtectionMutationForm
                    key={`seating-export-${presented.correlationId ?? "idle"}`}
                    action={requestSeatingExportAction.bind(null, event.id)}
                    className="atelier-form seating-form"
                    testId="seating-export"
                  >
                    <Envelope
                      fields={{
                        ...envelopeFields,
                        publicationId: publication?.id ?? "",
                        editionId: working?.id ?? "",
                        // Server-minted key: avoids client IdempotencyField SSR/reuse mismatch on
                        // identical READY-export replay navigations (React #418).
                        idempotencyKey: crypto.randomUUID(),
                      }}
                    />
                    <fieldset>
                      <legend>Request a seating export</legend>
                      <label>
                        Format
                        <select name="format" required>
                          <option value="PDF">PDF</option>
                          <option value="PNG">PNG</option>
                          <option value="JSON">JSON</option>
                        </select>
                      </label>
                      <label>
                        Projection
                        <select name="projectionClass" required>
                          <option value="PLANNER">Planner</option>
                          <option value="DIRECTOR">Director</option>
                          <option value="CEO">CEO</option>
                          <option value="AUDITOR">Auditor</option>
                          <option value="DOWNSTREAM">Downstream</option>
                        </select>
                      </label>
                    </fieldset>
                    <button type="submit" className="button">Request export</button>
                  </ProtectionMutationForm>
                ) : null}
                <ul data-testid="seating-export-list">
                  {workspace.exports.map((item) => (
                    <li key={item.id} data-testid="seating-export-item" data-export-id={item.id}>
                      {item.format} · {item.status} · {item.projectionClass === "FULL" ? "CEO" : item.projectionClass}
                    </li>
                  ))}
                </ul>
              </section>
            </AppShell>
          );
        }
        
      operator: 'match'
      stack: |-
        TestContext.<anonymous> (/Users/maisondoclar/Desktop/atelier-doclar/apps/event-os/test/s06-claude-remediation-copy.test.ts:38:12)
        Test.runInAsyncScope (node:async_hooks:206:9)
        Test.run (node:internal/test_runner/test:796:25)
        Suite.processPendingSubtests (node:internal/test_runner/test:526:18)
        Test.postRun (node:internal/test_runner/test:889:19)
        Test.run (node:internal/test_runner/test:835:12)
        async Suite.processPendingSubtests (node:internal/test_runner/test:526:7)
      ...
    # Subtest: DEF-03 tables empty state covers role-safe next action
    ok 4 - DEF-03 tables empty state covers role-safe next action
      ---
      duration_ms: 0.149625
      ...
    # Subtest: DEF-04 nav is permission-gated for Access, Audit and Event Command
    ok 5 - DEF-04 nav is permission-gated for Access, Audit and Event Command
      ---
      duration_ms: 0.094791
      ...
    1..5
not ok 32 - EOS-S06 Claude remediation surface copy
  ---
  duration_ms: 8.73725
  type: 'suite'
  location: '/Users/maisondoclar/Desktop/atelier-doclar/apps/event-os/test/s06-claude-remediation-copy.test.ts:12:1'
  failureType: 'subtestsFailed'
  error: '1 subtest failed'
  code: 'ERR_TEST_FAILURE'
  ...
# Subtest: EOS-S06 HARD conflict identity copy
    # Subtest: labels conflictingEditionId as a rule edition, not a content hash
    ok 1 - labels conflictingEditionId as a rule edition, not a content hash
      ---
      duration_ms: 0.920583
      ...
    1..1
ok 33 - EOS-S06 HARD conflict identity copy
  ---
  duration_ms: 1.33925
  type: 'suite'
  ...
# Subtest: EOS-S06 HARD-rule conflict refusal copy
    # Subtest: renders controlled business wording for CONFLICTING_ACTIVE_HARD_RULE / SEATING_HARD_RULE_CONFLICT
    ok 1 - renders controlled business wording for CONFLICTING_ACTIVE_HARD_RULE / SEATING_HARD_RULE_CONFLICT
      ---
      duration_ms: 0.793083
      ...
    # Subtest: reserves unexpected server failure for genuine technical faults
    ok 2 - reserves unexpected server failure for genuine technical faults
      ---
      duration_ms: 0.101375
      ...
    1..2
ok 34 - EOS-S06 HARD-rule conflict refusal copy
  ---
  duration_ms: 1.344375
  type: 'suite'
  ...
# Subtest: EOS-S06 mutation recoverable UI boundary
    # Subtest: ProtectionMutationForm catches non-redirect action failures into a contained failure state
    ok 1 - ProtectionMutationForm catches non-redirect action failures into a contained failure state
      ---
      duration_ms: 0.910958
      ...
    # Subtest: propose and rule forms server-mint idempotency keys instead of client IdempotencyField
    ok 2 - propose and rule forms server-mint idempotency keys instead of client IdempotencyField
      ---
      duration_ms: 0.334917
      ...
    # Subtest: seating error boundary keeps an explicit retry/reload surface
    ok 3 - seating error boundary keeps an explicit retry/reload surface
      ---
      duration_ms: 0.374792
      ...
    # Subtest: transportFailureFormState stays operator-safe
    ok 4 - transportFailureFormState stays operator-safe
      ---
      duration_ms: 0.168458
      ...
    1..4
ok 35 - EOS-S06 mutation recoverable UI boundary
  ---
  duration_ms: 2.439834
  type: 'suite'
  ...
# Subtest: Security remediation — Audit Executive Ledger gate
    # Subtest: Audit page and shell gate on platform.audit.read_all only
    ok 1 - Audit page and shell gate on platform.audit.read_all only
      ---
      duration_ms: 0.4635
      ...
    # Subtest: CEO and Auditor may authorise Executive Ledger; Director and Planner may not
    ok 2 - CEO and Auditor may authorise Executive Ledger; Director and Planner may not
      ---
      duration_ms: 528.696792
      ...
    # Subtest: Director operational audit must not be treated as Executive Ledger data on the page path
    ok 3 - Director operational audit must not be treated as Executive Ledger data on the page path
      ---
      duration_ms: 606.905542
      ...
    # Subtest: refused roles surface zero ledger rows contract in page source
    ok 4 - refused roles surface zero ledger rows contract in page source
      ---
      duration_ms: 2.655792
      ...
    1..4
ok 36 - Security remediation — Audit Executive Ledger gate
  ---
  duration_ms: 1139.686041
  type: 'suite'
  ...
# Subtest: Security remediation — Executive Event Command home visibility
    # Subtest: Home card uses executiveCommand.view
    ok 1 - Home card uses executiveCommand.view
      ---
      duration_ms: 1.222125
      ...
    1..1
ok 37 - Security remediation — Executive Event Command home visibility
  ---
  duration_ms: 1.32525
  type: 'suite'
  ...
# Subtest: Security remediation — seating export replay hydration
    # Subtest: export form uses server-minted idempotency key without client IdempotencyField
    ok 1 - export form uses server-minted idempotency key without client IdempotencyField
      ---
      duration_ms: 1.2495
      ...
    # Subtest: REPLAYED seating.export feedback states no new export was created
    ok 2 - REPLAYED seating.export feedback states no new export was created
      ---
      duration_ms: 0.432166
      ...
    1..2
ok 38 - Security remediation — seating export replay hydration
  ---
  duration_ms: 1.994041
  type: 'suite'
  ...
# Subtest: MD-PR-S060 persistence/redirect boundary
    # Subtest: does not call redirect inside withDurable in runProtectionFormAction
    ok 1 - does not call redirect inside withDurable in runProtectionFormAction
      ---
      duration_ms: 0.744167
      ...
    # Subtest: maps a stale Budget version to VERSION_CONFLICT, never HTTP 503
    ok 2 - maps a stale Budget version to VERSION_CONFLICT, never HTTP 503
      ---
      duration_ms: 3.375209
      ...
    # Subtest: finishes durable work before a truthful action-result write
    ok 3 - finishes durable work before a truthful action-result write
      ---
      duration_ms: 0.565292
      ...
    1..3
ok 39 - MD-PR-S060 persistence/redirect boundary
  ---
  duration_ms: 7.604208
  type: 'suite'
  ...
# Subtest: S073 diagnostic gates
    # Subtest: is unavailable when the temporary token is unset
    ok 1 - is unavailable when the temporary token is unset
      ---
      duration_ms: 0.439125
      ...
    # Subtest: denies a missing or unequal token and rate-limits a valid token
    ok 2 - denies a missing or unequal token and rate-limits a valid token
      ---
      duration_ms: 0.388542
      ...
    # Subtest: returns 404 for the retired diagnostic surface
    ok 3 - returns 404 for the retired diagnostic surface
      ---
      duration_ms: 15.474
      ...
    1..3
ok 40 - S073 diagnostic gates
  ---
  duration_ms: 16.886375
  type: 'suite'
  ...
# Subtest: MD-PR-S075 Packet 2 Event OS trusted action boundary (red)
    # Subtest: 1/13/14. every Seating action binds route eventId and does not take authority from FormData
    ok 1 - 1/13/14. every Seating action binds route eventId and does not take authority from FormData
      ---
      duration_ms: 1.036875
      ...
    # Subtest: 13/14. seating mutations authenticate once and do not reread FormData scope after a trusted context exists
    ok 2 - 13/14. seating mutations authenticate once and do not reread FormData scope after a trusted context exists
      ---
      duration_ms: 0.287
      ...
    # Subtest: 16. Verify-as requires seating.fixture_verify_as, bound event and the one-way CEO fallback
    ok 3 - 16. Verify-as requires seating.fixture_verify_as, bound event and the one-way CEO fallback
      ---
      duration_ms: 0.070375
      ...
    # Subtest: 20. Protection and Dossier shared-form behaviour remains FormData-scoped
    ok 4 - 20. Protection and Dossier shared-form behaviour remains FormData-scoped
      ---
      duration_ms: 0.140209
      ...
    1..4
ok 41 - MD-PR-S075 Packet 2 Event OS trusted action boundary (red)
  ---
  duration_ms: 2.080917
  type: 'suite'
  ...
# Subtest: MD-PR-S075 V2 replacement fail-closed mutation surface
    # Subtest: flag defaults on; off means CAPABILITY_NOT_ENABLED without reactivating V1 writers
    ok 1 - flag defaults on; off means CAPABILITY_NOT_ENABLED without reactivating V1 writers
      ---
      duration_ms: 1.028458
      ...
    # Subtest: every Seating Server Action mutation requires V2 writable and never calls seatingCommands()
    ok 2 - every Seating Server Action mutation requires V2 writable and never calls seatingCommands()
      ---
      duration_ms: 0.605
      ...
    # Subtest: does not fall back from a V2 path to V1 writers; obsolete V1 freeze remains snapshot-based but unused by actions
    ok 3 - does not fall back from a V2 path to V1 writers; obsolete V1 freeze remains snapshot-based but unused by actions
      ---
      duration_ms: 0.290166
      ...
    # Subtest: page retains historic V1 publication read compatibility without mutation authority
    not ok 4 - page retains historic V1 publication read compatibility without mutation authority
      ---
      duration_ms: 2.040833
      location: '/Users/maisondoclar/Desktop/atelier-doclar/apps/event-os/test/seating-v2-replacement-fail-closed.test.ts:49:3'
      failureType: 'testCodeFailure'
      error: |-
        The input did not match the regular expression /projectCurrentPublication/. Input:
        
        'import { AtelierPageHeader } from "../../../../../components/atelier-page-header";\n' +
          'import { AtelierOperationalState } from "../../../../../components/atelier-operational-state";\n' +
          'import { AtelierSectionTabs } from "../../../../../components/atelier-section-tabs";\n' +
          'import { AppShell } from "../../../../../components/shell";\n' +
          'import { ActionResultBanner } from "../../../../../components/action-result-banner";\n' +
          'import { CanonicalId, CanonicalTime, HistoryDisclosure } from "../../../../../components/canonical-evidence";\n' +
          'import { IdempotencyField, PendingSubmit } from "../../../../../components/atelier-pending-submit";\n' +
          'import { ProtectionMutationForm } from "../../../../../components/protection-mutation-form";\n' +
          'import { SeatingRuleAuthoringFields } from "../../../../../components/seating-rule-authoring-fields";\n' +
          'import { loadPresentedActionResult } from "../../../../../server/action-flash";\n' +
          'import { guardedActor } from "../../../../../server/guard";\n' +
          'import { getRuntime } from "../../../../../server/runtime";\n' +
          'import { preferredSeatingAssignment, seatingPermissions } from "../../../../../server/seating-scope";\n' +
          'import { operationalStateFromCode } from "../../../../../server/operational-state";\n' +
          'import {\n' +
          '  activateReservationBlockAction,\n' +
          '  adoptSeatingRunAction,\n' +
          '  adoptCpsatCandidateAction,\n' +
          '  applySeatingChangeAction,\n' +
          '  cancelSeatingRunAction,\n' +
          '  createReservationBlockAction,\n' +
          '  createSeatingConstraintAction,\n' +
          '  decideCpsatCandidateAction,\n' +
          '  decideSeatingApprovalAction,\n' +
          '  decideSeatingReviewAction,\n' +
          '  activateSeatingLayoutBindingAction,\n' +
          '  freezeSeatingInputsAction,\n' +
          '  launchSeatingRunAction,\n' +
          '  proposeSeatingLayoutBindingAction,\n' +
          '  recoverProposeSeatingLayoutBindingAction,\n' +
          '  publishSeatingPlanAction,\n' +
          '  releaseReservationBlockAction,\n' +
          '  requestSeatingExportAction,\n' +
          '  recallSeatingPlanAction,\n' +
          '  runS06EvaluationAction,\n' +
          '  submitCpsatCandidateAction,\n' +
          '  submitSeatingPlanAction,\n' +
          '  withdrawSeatingLayoutBindingAction,\n' +
          '  supersedeReservationBlockAction,\n' +
          '  withdrawReservationBlockAction,\n' +
          '} from "../../../../../server/seating-actions";\n' +
          'import { switchSeatingVerifyAsAction } from "../../../../../server/seating-verify-as-action";\n' +
          'import { eventOsVerifyAsAvailable } from "../../../../../server/seating-verify-as";\n' +
          'import { emitSettlementStage, PlatformError, retryLockApplies, seatingV2ReplacementEnabled, buildCpsatRunUiModel, WORKER_UNAVAILABLE_PUBLIC_MESSAGE, QUEUE_BUSY_PUBLIC_MESSAGE, type CpsatCandidateReviewModel } from "@maison-doclar/shared-platform";\n' +
          'import { activateSeatingRuleAction, withdrawSeatingRuleAction } from "../../../../../server/seating-actions";\n' +
          'import { CpsatRunStatusPanel } from "../../../../../components/cpsat-run-status-panel";\n' +
          'import { CpsatRunLifecyclePoller } from "../../../../../components/cpsat-run-lifecycle-poller";\n' +
          'import { CpsatCandidateReviewPanel } from "../../../../../components/cpsat-candidate-review-panel";\n' +
          'import { CpsatWorkerUnavailablePanel } from "../../../../../components/cpsat-worker-unavailable-panel";\n' +
          '\n' +
          'function visibleSeatingRuns<T extends { id: string }>(runs: T[], currentRunId: string | undefined, limit = 12): T[] {\n' +
          '  if (runs.length <= limit) return runs;\n' +
          '  const current = currentRunId ? runs.find((run) => run.id === currentRunId) : undefined;\n' +
          '  if (!current) return runs.slice(-limit);\n' +
          '  return [...runs.filter((run) => run.id !== currentRunId).slice(-(limit - 1)), current];\n' +
          '}\n' +
          '\n' +
          'function Envelope({ fields }: { fields: Record<string, string | number> }) {\n' +
          '  return (\n' +
          '    <>\n' +
          '      {Object.entries(fields).map(([name, value]) => (\n' +
          '        <input key={name} type="hidden" name={name} value={String(value)} />\n' +
          '      ))}\n' +
          '    </>\n' +
          '  );\n' +
          '}\n' +
          '\n' +
          'const TABS = [\n' +
          '  { href: "#overview", label: "Overview" },\n' +
          '  { href: "#inputs", label: "Inputs" },\n' +
          '  { href: "#rules", label: "Rules" },\n' +
          '  { href: "#reservations", label: "Reservations" },\n' +
          '  { href: "#runs", label: "Runs" },\n' +
          '  { href: "#studio", label: "Studio" },\n' +
          '  { href: "#review", label: "Review" },\n' +
          '  { href: "#publication", label: "Publication" },\n' +
          '] as const;\n' +
          '\n' +
          'export const maxDuration = 60;\n' +
          '\n' +
          'export default async function EventSeatingPage({\n' +
          '  params,\n' +
          '  searchParams,\n' +
          '}: {\n' +
          '  params: Promise<{ eventId: string }>;\n' +
          '  searchParams: Promise<Record<string, string | string[] | undefined>>;\n' +
          '}) {\n' +
          '  const { eventId } = await params;\n' +
          '  const query = await searchParams;\n' +
          '  const { actor, person } = await guardedActor();\n' +
          '  const runtime = getRuntime();\n' +
          '  const organisations = runtime.service.listOrganisations(actor);\n' +
          '  const events = organisations.flatMap((item) => {\n' +
          '    try {\n' +
          '      return runtime.service.listEvents(actor, item.id);\n' +
          '    } catch {\n' +
          '      return [];\n' +
          '    }\n' +
          '  });\n' +
          '  let event = events.find((item) => item.id === eventId);\n' +
          '  const organisation = organisations.find((item) => item.id === event?.organisationId) ?? organisations[0];\n' +
          '  if (!event && organisation) {\n' +
          '    try {\n' +
          '      event = runtime.service.getEvent(actor, organisation.id, eventId);\n' +
          '    } catch {\n' +
          '      event = undefined;\n' +
          '    }\n' +
          '  }\n' +
          '  if (!event || !organisation) {\n' +
          '    const denialOrg = organisations[0];\n' +
          '    const denied = denialOrg ? !seatingPermissions(person, denialOrg.id, eventId).view : true;\n' +
          '    return (\n' +
          '      <AppShell person={person} current="/app/events">\n' +
          '        <AtelierOperationalState\n' +
          '          state={operationalStateFromCode(\n' +
          '            denied ? "FORBIDDEN" : "NOT_FOUND",\n' +
          '            denied ? "This assignment cannot perform this seating action." : "The requested event is not available in this assignment.",\n' +
          '          )}\n' +
          '        />\n' +
          '      </AppShell>\n' +
          '    );\n' +
          '  }\n' +
          '  const permissions = seatingPermissions(person, organisation.id, event.id);\n' +
          '  if (!permissions.view) {\n' +
          '    return (\n' +
          '      <AppShell person={person} organisationName={organisation.displayName} eventName={event.name} eventId={event.id} current="/app/events">\n' +
          '        <AtelierOperationalState state={operationalStateFromCode("FORBIDDEN", "This assignment cannot perform this seating action.")} />\n' +
          '      </AppShell>\n' +
          '    );\n' +
          '  }\n' +
          '  let workspace;\n' +
          '  let publicationSource: "CPSAT" | "NONE" = "NONE";\n' +
          '  const workspaceStarted = Date.now();\n' +
          '  try {\n' +
          '    if (seatingV2ReplacementEnabled()) {\n' +
          '      workspace = await runtime.service.seatingV2Commands().projectWorkspace(actor, event.id);\n' +
          '      if (workspace.currentPublication) {\n' +
          '        publicationSource = "CPSAT";\n' +
          '      }\n' +
          '    } else {\n' +
          '      workspace = await runtime.service.seatingCommands().projectWorkspace(actor, event.id);\n' +
          '      // Legacy seating command path is not operational after M6C; treat as empty.\n' +
          '      workspace = { ...workspace, currentPublication: undefined };\n' +
          '    }\n' +
          '  } catch (error) {\n' +
          '    const message = error instanceof PlatformError ? error.publicMessage ?? error.message : "This assignment cannot perform this seating action.";\n' +
          '    return (\n' +
          '      <AppShell person={person} organisationName={organisation.displayName} eventName={event.name} eventId={event.id} current="/app/events">\n' +
          '        <AtelierOperationalState state={operationalStateFromCode("FORBIDDEN", message)} />\n' +
          '      </AppShell>\n' +
          '    );\n' +
          '  }\n' +
          '  const workspaceMs = Date.now() - workspaceStarted;\n' +
          '  const assignmentId = preferredSeatingAssignment(person.id, organisation.id, event.id)?.id ?? "";\n' +
          '  const actionResultStarted = Date.now();\n' +
          '  const presented = await loadPresentedActionResult({\n' +
          '    requestPath: `/app/events/${event.id}/seating`,\n' +
          '    resultId: typeof query.result === "string" ? query.result : undefined,\n' +
          '    actorPersonId: person.id,\n' +
          '    organisationId: organisation.id,\n' +
          '    eventId: event.id,\n' +
          '  });\n' +
          '  const actionResultMs = Date.now() - actionResultStarted;\n' +
          '  const resultId = typeof query.result === "string" ? query.result : undefined;\n' +
          '  emitSettlementStage({\n' +
          '    stage: presented.correlationId && resultId && presented.correlationId === resultId ? "RENDER_RESULT_FOUND" : "RENDER_RESULT_MISSING",\n' +
          '    commandId: resultId,\n' +
          '    resultId,\n' +
          '    eventId: event.id,\n' +
          '    durationMs: actionResultMs,\n' +
          '    reasonClass: presented.correlationId ? "PRESENTED" : "ABSENT",\n' +
          '  });\n' +
          '  const envelopeFields = { organisationId: organisation.id, eventId: event.id, assignmentId };\n' +
          '  const reviewableLifecycles = new Set([\n' +
          '    "READY_FOR_REVIEW",\n' +
          '    "PENDING_APPROVAL",\n' +
          '    "APPROVED",\n' +
          '    "REJECTED",\n' +
          '    "ADOPTED",\n' +
          '    "SUPERSEDED",\n' +
          '  ]);\n' +
          '  const cpsatReviewRun = workspace.runs.find((run) =>\n' +
          '    reviewableLifecycles.has(String((run as { lifecycle?: string }).lifecycle ?? run.status ?? "")),\n' +
          '  );\n' +
          '  let cpsatReview: CpsatCandidateReviewModel | null = null;\n' +
          '  if (cpsatReviewRun && seatingV2ReplacementEnabled()) {\n' +
          '    try {\n' +
          '      cpsatReview = await runtime.service.seatingV2Commands().getCpsatCandidateReview(actor, event.id, cpsatReviewRun.id);\n' +
          '    } catch {\n' +
          '      cpsatReview = null;\n' +
          '    }\n' +
          '  }\n' +
          '  const working = workspace.workingEdition as {\n' +
          '    id?: string;\n' +
          '    contentHash?: string;\n' +
          '    status?: string;\n' +
          '    version?: number;\n' +
          '    stale?: boolean;\n' +
          '    sourceRunId?: string;\n' +
          '  } | undefined;\n' +
          '  const input = workspace.inputEdition as { id?: string; contentHash?: string; layoutContentHash?: string } | undefined;\n' +
          '  const publication = workspace.currentPublication as {\n' +
          '    id?: string;\n' +
          '    publicationNumber?: number;\n' +
          '    editionHash?: string;\n' +
          '    adoptionId?: string;\n' +
          '    runId?: string;\n' +
          '    productResult?: string | null;\n' +
          '    evidenceGrade?: string | null;\n' +
          '    seatedCount?: number;\n' +
          '    unseatedCount?: number;\n' +
          '    source?: string;\n' +
          '  } | undefined;\n' +
          '  const verifyAs = eventOsVerifyAsAvailable() && permissions.fixtureVerifyAs;\n' +
          '  const seatedDisplay = publication?.seatedCount ?? workspace.counts.seated;\n' +
          '  const unseatedDisplay = publication?.unseatedCount ?? workspace.counts.unseated;\n' +
          '  const eligibleDisplay = Math.max(workspace.counts.eligibleGuests, seatedDisplay + unseatedDisplay);\n' +
          '  return (\n' +
          '    <AppShell person={person} organisationName={organisation.displayName} eventName={event.name} eventId={event.id} current="/app/events">\n' +
          '      <AtelierPageHeader\n' +
          '        eyebrow="Seating Command"\n' +
          '        title={`${event.name} seating`}\n' +
          '        lede="The solver recommends. Authorised people decide. Published seating does not send messages, issue credentials or change check-in."\n' +
          '      />\n' +
          '      <p data-testid="sea'... 59507 more characters
        
      code: 'ERR_ASSERTION'
      name: 'AssertionError'
      expected:
      actual: |-
        import { AtelierPageHeader } from "../../../../../components/atelier-page-header";
        import { AtelierOperationalState } from "../../../../../components/atelier-operational-state";
        import { AtelierSectionTabs } from "../../../../../components/atelier-section-tabs";
        import { AppShell } from "../../../../../components/shell";
        import { ActionResultBanner } from "../../../../../components/action-result-banner";
        import { CanonicalId, CanonicalTime, HistoryDisclosure } from "../../../../../components/canonical-evidence";
        import { IdempotencyField, PendingSubmit } from "../../../../../components/atelier-pending-submit";
        import { ProtectionMutationForm } from "../../../../../components/protection-mutation-form";
        import { SeatingRuleAuthoringFields } from "../../../../../components/seating-rule-authoring-fields";
        import { loadPresentedActionResult } from "../../../../../server/action-flash";
        import { guardedActor } from "../../../../../server/guard";
        import { getRuntime } from "../../../../../server/runtime";
        import { preferredSeatingAssignment, seatingPermissions } from "../../../../../server/seating-scope";
        import { operationalStateFromCode } from "../../../../../server/operational-state";
        import {
          activateReservationBlockAction,
          adoptSeatingRunAction,
          adoptCpsatCandidateAction,
          applySeatingChangeAction,
          cancelSeatingRunAction,
          createReservationBlockAction,
          createSeatingConstraintAction,
          decideCpsatCandidateAction,
          decideSeatingApprovalAction,
          decideSeatingReviewAction,
          activateSeatingLayoutBindingAction,
          freezeSeatingInputsAction,
          launchSeatingRunAction,
          proposeSeatingLayoutBindingAction,
          recoverProposeSeatingLayoutBindingAction,
          publishSeatingPlanAction,
          releaseReservationBlockAction,
          requestSeatingExportAction,
          recallSeatingPlanAction,
          runS06EvaluationAction,
          submitCpsatCandidateAction,
          submitSeatingPlanAction,
          withdrawSeatingLayoutBindingAction,
          supersedeReservationBlockAction,
          withdrawReservationBlockAction,
        } from "../../../../../server/seating-actions";
        import { switchSeatingVerifyAsAction } from "../../../../../server/seating-verify-as-action";
        import { eventOsVerifyAsAvailable } from "../../../../../server/seating-verify-as";
        import { emitSettlementStage, PlatformError, retryLockApplies, seatingV2ReplacementEnabled, buildCpsatRunUiModel, WORKER_UNAVAILABLE_PUBLIC_MESSAGE, QUEUE_BUSY_PUBLIC_MESSAGE, type CpsatCandidateReviewModel } from "@maison-doclar/shared-platform";
        import { activateSeatingRuleAction, withdrawSeatingRuleAction } from "../../../../../server/seating-actions";
        import { CpsatRunStatusPanel } from "../../../../../components/cpsat-run-status-panel";
        import { CpsatRunLifecyclePoller } from "../../../../../components/cpsat-run-lifecycle-poller";
        import { CpsatCandidateReviewPanel } from "../../../../../components/cpsat-candidate-review-panel";
        import { CpsatWorkerUnavailablePanel } from "../../../../../components/cpsat-worker-unavailable-panel";
        
        function visibleSeatingRuns<T extends { id: string }>(runs: T[], currentRunId: string | undefined, limit = 12): T[] {
          if (runs.length <= limit) return runs;
          const current = currentRunId ? runs.find((run) => run.id === currentRunId) : undefined;
          if (!current) return runs.slice(-limit);
          return [...runs.filter((run) => run.id !== currentRunId).slice(-(limit - 1)), current];
        }
        
        function Envelope({ fields }: { fields: Record<string, string | number> }) {
          return (
            <>
              {Object.entries(fields).map(([name, value]) => (
                <input key={name} type="hidden" name={name} value={String(value)} />
              ))}
            </>
          );
        }
        
        const TABS = [
          { href: "#overview", label: "Overview" },
          { href: "#inputs", label: "Inputs" },
          { href: "#rules", label: "Rules" },
          { href: "#reservations", label: "Reservations" },
          { href: "#runs", label: "Runs" },
          { href: "#studio", label: "Studio" },
          { href: "#review", label: "Review" },
          { href: "#publication", label: "Publication" },
        ] as const;
        
        export const maxDuration = 60;
        
        export default async function EventSeatingPage({
          params,
          searchParams,
        }: {
          params: Promise<{ eventId: string }>;
          searchParams: Promise<Record<string, string | string[] | undefined>>;
        }) {
          const { eventId } = await params;
          const query = await searchParams;
          const { actor, person } = await guardedActor();
          const runtime = getRuntime();
          const organisations = runtime.service.listOrganisations(actor);
          const events = organisations.flatMap((item) => {
            try {
              return runtime.service.listEvents(actor, item.id);
            } catch {
              return [];
            }
          });
          let event = events.find((item) => item.id === eventId);
          const organisation = organisations.find((item) => item.id === event?.organisationId) ?? organisations[0];
          if (!event && organisation) {
            try {
              event = runtime.service.getEvent(actor, organisation.id, eventId);
            } catch {
              event = undefined;
            }
          }
          if (!event || !organisation) {
            const denialOrg = organisations[0];
            const denied = denialOrg ? !seatingPermissions(person, denialOrg.id, eventId).view : true;
            return (
              <AppShell person={person} current="/app/events">
                <AtelierOperationalState
                  state={operationalStateFromCode(
                    denied ? "FORBIDDEN" : "NOT_FOUND",
                    denied ? "This assignment cannot perform this seating action." : "The requested event is not available in this assignment.",
                  )}
                />
              </AppShell>
            );
          }
          const permissions = seatingPermissions(person, organisation.id, event.id);
          if (!permissions.view) {
            return (
              <AppShell person={person} organisationName={organisation.displayName} eventName={event.name} eventId={event.id} current="/app/events">
                <AtelierOperationalState state={operationalStateFromCode("FORBIDDEN", "This assignment cannot perform this seating action.")} />
              </AppShell>
            );
          }
          let workspace;
          let publicationSource: "CPSAT" | "NONE" = "NONE";
          const workspaceStarted = Date.now();
          try {
            if (seatingV2ReplacementEnabled()) {
              workspace = await runtime.service.seatingV2Commands().projectWorkspace(actor, event.id);
              if (workspace.currentPublication) {
                publicationSource = "CPSAT";
              }
            } else {
              workspace = await runtime.service.seatingCommands().projectWorkspace(actor, event.id);
              // Legacy seating command path is not operational after M6C; treat as empty.
              workspace = { ...workspace, currentPublication: undefined };
            }
          } catch (error) {
            const message = error instanceof PlatformError ? error.publicMessage ?? error.message : "This assignment cannot perform this seating action.";
            return (
              <AppShell person={person} organisationName={organisation.displayName} eventName={event.name} eventId={event.id} current="/app/events">
                <AtelierOperationalState state={operationalStateFromCode("FORBIDDEN", message)} />
              </AppShell>
            );
          }
          const workspaceMs = Date.now() - workspaceStarted;
          const assignmentId = preferredSeatingAssignment(person.id, organisation.id, event.id)?.id ?? "";
          const actionResultStarted = Date.now();
          const presented = await loadPresentedActionResult({
            requestPath: `/app/events/${event.id}/seating`,
            resultId: typeof query.result === "string" ? query.result : undefined,
            actorPersonId: person.id,
            organisationId: organisation.id,
            eventId: event.id,
          });
          const actionResultMs = Date.now() - actionResultStarted;
          const resultId = typeof query.result === "string" ? query.result : undefined;
          emitSettlementStage({
            stage: presented.correlationId && resultId && presented.correlationId === resultId ? "RENDER_RESULT_FOUND" : "RENDER_RESULT_MISSING",
            commandId: resultId,
            resultId,
            eventId: event.id,
            durationMs: actionResultMs,
            reasonClass: presented.correlationId ? "PRESENTED" : "ABSENT",
          });
          const envelopeFields = { organisationId: organisation.id, eventId: event.id, assignmentId };
          const reviewableLifecycles = new Set([
            "READY_FOR_REVIEW",
            "PENDING_APPROVAL",
            "APPROVED",
            "REJECTED",
            "ADOPTED",
            "SUPERSEDED",
          ]);
          const cpsatReviewRun = workspace.runs.find((run) =>
            reviewableLifecycles.has(String((run as { lifecycle?: string }).lifecycle ?? run.status ?? "")),
          );
          let cpsatReview: CpsatCandidateReviewModel | null = null;
          if (cpsatReviewRun && seatingV2ReplacementEnabled()) {
            try {
              cpsatReview = await runtime.service.seatingV2Commands().getCpsatCandidateReview(actor, event.id, cpsatReviewRun.id);
            } catch {
              cpsatReview = null;
            }
          }
          const working = workspace.workingEdition as {
            id?: string;
            contentHash?: string;
            status?: string;
            version?: number;
            stale?: boolean;
            sourceRunId?: string;
          } | undefined;
          const input = workspace.inputEdition as { id?: string; contentHash?: string; layoutContentHash?: string } | undefined;
          const publication = workspace.currentPublication as {
            id?: string;
            publicationNumber?: number;
            editionHash?: string;
            adoptionId?: string;
            runId?: string;
            productResult?: string | null;
            evidenceGrade?: string | null;
            seatedCount?: number;
            unseatedCount?: number;
            source?: string;
          } | undefined;
          const verifyAs = eventOsVerifyAsAvailable() && permissions.fixtureVerifyAs;
          const seatedDisplay = publication?.seatedCount ?? workspace.counts.seated;
          const unseatedDisplay = publication?.unseatedCount ?? workspace.counts.unseated;
          const eligibleDisplay = Math.max(workspace.counts.eligibleGuests, seatedDisplay + unseatedDisplay);
          return (
            <AppShell person={person} organisationName={organisation.displayName} eventName={event.name} eventId={event.id} current="/app/events">
              <AtelierPageHeader
                eyebrow="Seating Command"
                title={`${event.name} seating`}
                lede="The solver recommends. Authorised people decide. Published seating does not send messages, issue credentials or change check-in."
              />
              <p data-testid="seating-publication-badge">
                {publication
                  ? `Current operational publication: adoption ${(publication.adoptionId ?? publication.id ?? "").slice(0, 8)} · ${seatedDisplay}/${eligibleDisplay} seated`
                  : "No current operational publication"}
                {publication?.productResult ? ` · Solver result: ${publication.productResult}` : ""}
                {publication?.evidenceGrade ? ` · Evidence: ${publication.evidenceGrade}` : ""}
              </p>
              <p data-testid="seating-freshness-badge">Freshness: {workspace.freshnessCopy ?? workspace.inputFreshness}</p>
              <p data-testid="seating-worker-badge">
                Worker availability: {(workspace as { cpsatWorkerReadyCount?: number }).cpsatWorkerReadyCount != null
                  ? `${(workspace as { cpsatWorkerReadyCount?: number }).cpsatWorkerReadyCount} ready`
                  : "unknown"}
              </p>
              <p
                data-testid="seating-settlement"
                data-workspace-ms={workspaceMs}
                data-action-result-ms={actionResultMs}
                data-result-id={typeof query.result === "string" ? query.result : ""}
              >
                Render {workspaceMs}ms · action result {actionResultMs}ms
              </p>
              <ActionResultBanner presented={presented} />
              {(() => {
                const msg = presented.view?.message ?? "";
                if (msg === QUEUE_BUSY_PUBLIC_MESSAGE || msg.includes("No run was started")) {
                  return <CpsatWorkerUnavailablePanel reason="queue_busy" overviewHref="#overview" />;
                }
                if (
                  msg === WORKER_UNAVAILABLE_PUBLIC_MESSAGE ||
                  msg.includes("solver service is not ready") ||
                  msg.includes("temporarily unavailable")
                ) {
                  return <CpsatWorkerUnavailablePanel reason="unavailable" overviewHref="#overview" />;
                }
                return null;
              })()}
              <div className="seating-tabs">
                <AtelierSectionTabs label="Seating Command views" items={TABS} />
              </div>
        
              <section id="overview" className="atelier-panel" data-testid="seating-overview">
                <h2>Overview</h2>
                <ul className="seating-metric-cards">
                  <li><a href="#inputs">Input readiness · {workspace.inputFreshness}</a></li>
                  <li><a href="#studio">Eligible guests · {workspace.counts.eligibleGuests}</a></li>
                  <li><a href="#studio">Seated · {seatedDisplay}</a></li>
                  <li><a href="#studio">Unseated · {unseatedDisplay}</a></li>
                  <li><a href="#rules">Hard blockers · {workspace.counts.hardBlockers}</a></li>
                  <li>
                    <a href="#publication">
                      Current operational publication · {publication ? `adoption ${(publication.adoptionId ?? publication.id ?? "").slice(0, 8)}` : "None"}
                    </a>
                  </li>
                  <li>
                    <a href="#runs">
                      Workflow · {publicationSource === "CPSAT" ? "CP-SAT authority" : "No CP-SAT publication"}
                    </a>
                  </li>
                  <li>
                    <a href="#publication">
                      Working edition · {working ? `${working.status}${working.status === "PUBLISHED" ? "" : " / unpublished"}` : "None"}
                    </a>
                  </li>
                </ul>
                <h3>What needs attention</h3>
                {workspace.attention.length ? (
                  <ol>
                    {workspace.attention.map((item) => (
                      <li key={`${item.kind}-${item.message}`}>
                        <a href={item.href}>{item.message}</a>
                      </li>
                    ))}
                  </ol>
                ) : (
                  <p>No outstanding seating blockers.</p>
                )}
                <p data-testid="seating-next-action">Next authorised action: {workspace.nextAction}</p>
                {permissions.evaluate ? (
                  <ProtectionMutationForm action={runS06EvaluationAction.bind(null, event.id)} className="actions" testId="seating-evaluate">
                    <Envelope fields={envelopeFields} />
                    <IdempotencyField />
                    <button type="submit" className="button">
                      Run seating evaluation
                    </button>
                  </ProtectionMutationForm>
                ) : null}
                {workspace.evaluation ? (
                  <p data-testid="seating-evaluation-status">
                    Last evaluation {workspace.evaluation.corpusEdition}: {workspace.evaluation.status}
                    {typeof workspace.evaluation.passedCount === "number"
                      ? ` · ${workspace.evaluation.passedCount}/${workspace.evaluation.caseCount} passed`
                      : ` · ${workspace.evaluation.caseCount} cases`}
                    {workspace.evaluation.readinessResult ? ` · ${workspace.evaluation.readinessResult}` : ""}
                    {workspace.evaluation.safeFailureCodes?.length
                      ? ` · codes ${workspace.evaluation.safeFailureCodes.join(", ")}`
                      : ""}
                  </p>
                ) : null}
                {verifyAs ? (
                  <section className="atelier-panel" data-testid="seating-verify-as">
                    <h3>Synthetic verification</h3>
                    <p>This switches the signed-in fixture role. It is not Access Administration and cannot be used under production authorisation.</p>
                    <ProtectionMutationForm action={switchSeatingVerifyAsAction.bind(null, event.id)} className="atelier-form" testId="seating-verify-as-form">
                      <Envelope fields={envelopeFields} />
                      <IdempotencyField />
                      <label>
                        Fixture role
                        <select name="symbolicRole" required>
                          <option value="planner">Planner</option>
                          <option value="reviewer">Specialist reviewer</option>
                          <option value="director">Event Director</option>
                          <option value="ceo">CEO</option>
                          <option value="auditor">Auditor</option>
                        </select>
                      </label>
                      <button type="submit" className="button">
                        Switch fixture role
                      </button>
                    </ProtectionMutationForm>
                  </section>
                ) : null}
              </section>
        
              <section id="inputs" className="atelier-panel" data-testid="seating-inputs">
                <h2>Inputs</h2>
                <article>
                  <h3>Guest cohort</h3>
                  <p>{workspace.guests.length} governed guests. Eligible {workspace.counts.eligibleGuests}.</p>
                </article>
                <article>
                  <h3>RSVP truth</h3>
                  <p>Attendance intent is RSVP truth. Forecast does not overwrite it.</p>
                </article>
                <article data-testid="seating-layout-publication">
                  <h3>Venue layout tables</h3>
                  <p>
                    {workspace.tables.length
                      ? `${workspace.tables.length} bound published tables available for seating${workspace.tables.some((table) => table.mismatch) ? " · physical and declared capacity disagree" : ""}`
                      : workspace.seatingLayoutBinding?.status === "BOUND"
                        ? "The bound layout publication has no usable tables yet."
                        : "No venue layout tables are available for seating until a seating layout binding is active against a current layout publication. This is separate from the operational seating publication."}
                  </p>
                </article>
                <article data-testid="seating-layout-binding">
                  <h3>Seating layout binding</h3>
                  <p
                    data-testid="seating-layout-binding-status"
                    data-binding-status={workspace.seatingLayoutBinding?.status ?? "ABSENT"}
                    data-publication-number={workspace.seatingLayoutBinding?.publicationNumber ?? ""}
                    data-content-hash-prefix={workspace.seatingLayoutBinding?.contentHashPrefix ?? ""}
                    data-freeze-disabled={workspace.seatingLayoutBinding?.freezeDisabled ? "true" : "false"}
                  >
                    {workspace.seatingLayoutBinding?.status === "BOUND"
                      ? `${workspace.seatingLayoutBinding.layoutLabel ?? "Bound layout"} · CURRENT publication ${workspace.seatingLayoutBinding.publicationNumber} · hash ${workspace.seatingLayoutBinding.contentHashPrefix} · ${workspace.seatingLayoutBinding.tableCount ?? 0} tables · ${workspace.seatingLayoutBinding.physicalCapacity ?? 0} physical / ${workspace.seatingLayoutBinding.declaredCapacity ?? 0} declared`
                      : workspace.seatingLayoutBinding?.status === "AMBIGUOUS"
                        ? "More than one seating layout binding is active for this event."
                        : workspace.seatingLayoutBinding?.status === "STALE"
                          ? "The seating layout binding is stale."
                          : workspace.seatingLayoutBinding?.status === "MISMATCH"
                            ? "The seating layout binding could not be verified."
                            : "No active seating layout binding."}
                  </p>
                  {permissions.prepare && (workspace.seatingLayoutBindingCandidates?.length ?? 0) > 0 ? (
                    <ProtectionMutationForm
                      key={`seating-layout-binding-propose-${presented.correlationId ?? "idle"}`}
                      action={proposeSeatingLayoutBindingAction.bind(null, event.id)}
                      recoverAction={recoverProposeSeatingLayoutBindingAction.bind(null, event.id)}
                      className="atelier-form"
                      testId="seating-layout-binding-propose"
                    >
                      <Envelope
                        fields={{
                          ...envelopeFields,
                          // Server-minted key: stable for this form instance so transport failure
                          // retry can recover a durable commit via idempotent replay.
                          idempotencyKey: crypto.randomUUID(),
                        }}
                      />
                      <label>
                        Current layout publication
                        <select name="layoutPublicationId" required>
                          {workspace.seatingLayoutBindingCandidates?.map((candidate) => (
                            <option key={candidate.publicationId} value={candidate.publicationId}>
                              {candidate.layoutLabel} · CURRENT publication {candidate.publicationNumber} · hash {candidate.contentHash.slice(0, 12)} · {candidate.tableCount} tables
                            </option>
                          ))}
                        </select>
                      </label>
                      <PendingSubmit className="button" locked={presented.mutationLocked}>
                        Propose seating layout binding
                      </PendingSubmit>
                    </ProtectionMutationForm>
                  ) : null}
                  {permissions.ruleActivate && workspace.seatingLayoutBinding?.draftId ? (
                    <ProtectionMutationForm action={activateSeatingLayoutBindingAction.bind(null, event.id)} className="actions" testId="seating-layout-binding-activate">
                      <Envelope
                        fields={{
                          ...envelopeFields,
                          bindingId: workspace.seatingLayoutBinding.draftId,
                          expectedVersion: workspace.seatingLayoutBinding.draftVersion ?? 1,
                          layoutPublicationId: workspace.seatingLayoutBinding.draftPublicationId ?? "",
                          layoutContentHash: workspace.seatingLayoutBinding.draftContentHash ?? "",
                        }}
                      />
                      <IdempotencyField />
                      <div data-testid="seating-layout-binding-activate-identity">
                        <p data-testid="seating-layout-binding-proposal-review">
                          Pending proposal {workspace.seatingLayoutBinding.draftId.slice(0, 8)} · {workspace.seatingLayoutBinding.draftStatus ?? "DRAFT"} ·{" "}
                          {workspace.seatingLayoutBinding.draftLayoutLabel ?? "Proposed layout"} · CURRENT publication{" "}
                          {workspace.seatingLayoutBinding.draftPublicationNumber} · hash {workspace.seatingLayoutBinding.draftContentHashPrefix}
                        </p>
                        <p data-testid="seating-layout-binding-proposal-proposer">
                          Proposed by {workspace.seatingLayoutBinding.draftProposedByLabel ?? "Planner"}
                          {workspace.seatingLayoutBinding.draftProposedAt ? ` · ${workspace.seatingLayoutBinding.draftProposedAt}` : ""}
                        </p>
                        <p data-testid="seating-layout-binding-proposal-current">
                          {workspace.seatingLayoutBinding.status === "BOUND"
                            ? `Current authoritative binding: ${workspace.seatingLayoutBinding.activeLayoutLabel ?? "Bound layout"} · hash ${workspace.seatingLayoutBinding.activeContentHashPrefix ?? workspace.seatingLayoutBinding.contentHashPrefix ?? "—"}`
                            : "Current authoritative binding: none"}
                        </p>
                        <p data-testid="seating-layout-binding-proposal-target">
                          Proposed replacement: {workspace.seatingLayoutBinding.draftLayoutLabel ?? "Proposed layout"} · hash{" "}
                          {workspace.seatingLayoutBinding.draftContentHashPrefix}
                        </p>
                      </div>
                      <button type="submit" className="button">Activate seating layout binding</button>
                    </ProtectionMutationForm>
                  ) : null}
                  {permissions.prepare && workspace.seatingLayoutBinding?.draftId ? (
                    <ProtectionMutationForm action={withdrawSeatingLayoutBindingAction.bind(null, event.id)} className="actions" testId="seating-layout-binding-withdraw-draft">
                      <Envelope fields={{ ...envelopeFields, bindingId: workspace.seatingLayoutBinding.draftId, expectedVersion: workspace.seatingLayoutBinding.draftVersion ?? 1 }} />
                      <IdempotencyField />
                      <button type="submit" className="button">Withdraw draft binding</button>
                    </ProtectionMutationForm>
                  ) : null}
                  {permissions.ruleActivate && workspace.seatingLayoutBinding?.status === "BOUND" && workspace.seatingLayoutBinding.activeId ? (
                    <ProtectionMutationForm action={withdrawSeatingLayoutBindingAction.bind(null, event.id)} className="actions" testId="seating-layout-binding-withdraw">
                      <Envelope fields={{ ...envelopeFields, bindingId: workspace.seatingLayoutBinding.activeId, expectedVersion: workspace.seatingLayoutBinding.activeVersion ?? 1 }} />
                      <IdempotencyField />
                      <button type="submit" className="button">Withdraw active binding</button>
                    </ProtectionMutationForm>
                  ) : null}
                  <HistoryDisclosure
                    summary="Earlier seating layout bindings"
                    count={workspace.seatingLayoutBindingHistory?.length ?? 0}
                    testId="seating-layout-binding-history"
                  >
                    {(workspace.seatingLayoutBindingHistory ?? []).map((item, index) => (
                      <p key={`${item.state}-${item.contentHashPrefix ?? index}`}>
                        {item.state}
                        {item.publicationNumber != null ? ` · publication ${item.publicationNumber}` : ""}
                        {item.contentHashPrefix ? ` · hash ${item.contentHashPrefix}` : ""}
                      </p>
                    ))}
                  </HistoryDisclosure>
                </article>
                <article>
                  <h3>Event Brief</h3>
                  <p>Optional published brief facts only.</p>
                </article>
                <article>
                  <h3>Protection snapshot</h3>
                  <p>Optional coded protection constraints only.</p>
                </article>
                {permissions.prepare ? (
                  <ProtectionMutationForm action={freezeSeatingInputsAction.bind(null, event.id)} className="actions" testId="seating-freeze">
                    <Envelope fields={envelopeFields} />
                    <IdempotencyField />
                    <button
                      type="submit"
                      className="button"
                      disabled={
                        workspace.seatingLayoutBinding?.status !== "BOUND" || workspace.tables.some((table) => table.mismatch)
                      }
                    >
                      Freeze new input edition
                    </button>
                  </ProtectionMutationForm>
                ) : null}
                {input ? (
                  <details>
                    <summary>Input provenance</summary>
                    <p
                      data-testid="seating-input-hash"
                      data-hash={input.contentHash ?? ""}
                      data-layout-hash={input.layoutContentHash ?? ""}
                    >
                      Hash {input.contentHash}
                    </p>
                  </details>
                ) : (
                  <p data-testid="seating-input-hash" data-hash="" data-layout-hash="">
                    No frozen input edition.
                  </p>
                )}
                <HistoryDisclosure
                  summary="Earlier frozen input packages"
                  count={(workspace.inputPackageHistory ?? []).filter((item) => !item.current).length}
                  testId="seating-package-history"
                >
                  {(workspace.inputPackageHistory ?? [])
                    .filter((item) => !item.current)
                    .map((item) => (
                      <p key={item.contentHash || item.layoutContentHash}>
                        Hash {item.contentHash} · layout {item.layoutContentHash}
                      </p>
                    ))}
                </HistoryDisclosure>
              </section>
        
              <section id="rules" className="atelier-panel" data-testid="seating-rules">
                <h2>Rules</h2>
                {(["Governing", "Draft", "Historical"] as const).map((group) => {
                  const items = workspace.constraints.filter((item) => {
                    if (group === "Governing") return item.status === "ACTIVE" && item.duplicateRole !== "REDUNDANT_HISTORICAL";
                    if (group === "Draft") return item.status === "DRAFT";
                    return (
                      (item.status !== "ACTIVE" && item.status !== "DRAFT") ||
                      item.duplicateRole === "REDUNDANT_HISTORICAL"
                    );
                  }).slice(group === "Historical" ? -24 : undefined);
                  return (
                    <div key={group}>
                      <h3>{group}</h3>
                      <ul>
                        {items.map((item) => {
                          const softDraft = item.status === "DRAFT" && item.kind !== "HARD";
                          const hardDraft = item.status === "DRAFT" && item.kind === "HARD";
                          const alreadyActiveDraft = item.duplicateRole === "ALREADY_ACTIVE_DRAFT";
                          const hardConflict = Boolean(item.hardConflictEditionId);
                          const canActivate =
                            !alreadyActiveDraft &&
                            ((hardDraft && permissions.ruleActivate) || (softDraft && permissions.constraintManage));
                          const canWithdraw =
                            item.duplicateRole !== "REDUNDANT_HISTORICAL" &&
                            (item.status === "DRAFT" || item.status === "ACTIVE") &&
                            permissions.constraintManage;
                          return (
                            <li
                              key={item.id}
                              data-testid={
                                item.duplicateRole === "AUTHORITATIVE"
                                  ? "seating-rule-authoritative"
                                  : item.duplicateRole === "REDUNDANT_HISTORICAL"
                                    ? "seating-rule-redundant-historical"
                                    : item.duplicateRole === "ALREADY_ACTIVE_DRAFT"
                                      ? "seating-rule-already-active-draft"
                                      : hardConflict
                                        ? "seating-rule-hard-conflict-draft"
                                        : "seating-rule-item"
                              }
                              data-duplicate-role={item.duplicateRole ?? ""}
                              data-authoritative-id={item.authoritativeEditionId ?? ""}
                              data-hard-conflict-id={item.hardConflictEditionId ?? ""}
                            >
                              {item.preview}
                              {item.duplicateRole === "AUTHORITATIVE" && (item.redundantActiveCount ?? 0) > 0 ? (
                                <p data-testid="seating-rule-authoritative-note">
                                  Authoritative governing rule for this semantic content. {item.redundantActiveCount} redundant ACTIVE
                                  duplicate{item.redundantActiveCount === 1 ? "" : "s"} predate the uniqueness invariant and are listed under Historical.
                                </p>
                              ) : null}
                              {item.duplicateRole === "REDUNDANT_HISTORICAL" ? (
                                <p data-testid="seating-rule-redundant-note">
                                  Redundant historical duplicate of authoritative rule {item.authoritativeEditionId?.slice(0, 8)}. Not separately governing.
                                  Retained for audit/replay; predates semantic uniqueness enforcement.
                                </p>
                              ) : null}
                              {alreadyActiveDraft ? (
                                <p data-testid="seating-rule-already-active">
                                  ALREADY ACTIVE — equivalent rule {item.authoritativeEditionId?.slice(0, 8)} already governs this scope. Activation would make no data change.
                                </p>
                              ) : null}
                              {hardConflict ? (
                                <p data-testid="seating-rule-hard-conflict">
                                  Conflicts with ACTIVE HARD {(item.hardConflictKind ?? "rule").replaceAll("_", " ")} rule
                                  edition {item.hardConflictEditionId?.slice(0, 8)}
                                  {item.hardConflictContentHashPrefix
                                    ? ` · content hash ${item.hardConflictContentHashPrefix}`
                                    : ""}
                                  . Withdraw or supersede that governing rule through a governed action before activating this
                                  replacement. Activation is blocked while the contradiction remains.
                                </p>
                              ) : null}
                              {canActivate ? (
                                <ProtectionMutationForm action={activateSeatingRuleAction.bind(null, event.id)} className="actions">
                                  <Envelope
                                    fields={{
                                      ...envelopeFields,
                                      editionId: item.id,
                                      expectedVersion: item.editionNo ?? 1,
                                      expectedContentHash: item.contentHash ?? "",
                                    }}
                                  />
                                  <IdempotencyField />
                                  <button type="submit" className="button secondary">Activate</button>
                                </ProtectionMutationForm>
                              ) : null}
                              {canWithdraw ? (
                                <ProtectionMutationForm action={withdrawSeatingRuleAction.bind(null, event.id)} className="actions">
                                  <Envelope
                                    fields={{
                                      ...envelopeFields,
                                      editionId: item.id,
                                      expectedVersion: item.editionNo ?? 1,
                                      expectedContentHash: item.contentHash ?? "",
                                      reason: "Withdrawn from governing set",
                                    }}
                                  />
                                  <IdempotencyField />
                                  <button type="submit" className="button secondary">Withdraw</button>
                                </ProtectionMutationForm>
                              ) : null}
                            </li>
                          );
                        })}
                      </ul>
                    </div>
                  );
                })}
                {permissions.constraintManage ? (
                  <ProtectionMutationForm
                    key={`seating-constraint-form-${presented.correlationId ?? "idle"}`}
                    action={createSeatingConstraintAction.bind(null, event.id)}
                    className="atelier-form seating-form"
                    testId="seating-constraint-form"
                  >
                    <Envelope
                      fields={{
                        ...envelopeFields,
                        // Server-minted key: stable for this form instance so transport failure
                        // retry can recover a durable commit via idempotent replay.
                        idempotencyKey: crypto.randomUUID(),
                      }}
                    />
                    <fieldset>
                      <legend>Create a seating rule</legend>
                      <label>
                        Name
                        <input name="name" required defaultValue="Keep these guests together" />
                      </label>
                      <label>
                        Kind
                        <select name="kind" required>
                          <option value="HARD">Hard rule</option>
                          <option value="WEIGHTED">Weighted preference</option>
                          <option value="INFORMATION">Information only</option>
                        </select>
                      </label>
                      <SeatingRuleAuthoringFields guests={workspace.guests} tables={workspace.tables} />
                      <label>
                        Weight
                        <input name="weight" type="number" min={1} defaultValue={1} />
                      </label>
                      <label>
                        Evidence
                        <input name="evidence" defaultValue="Planner note" />
                      </label>
                      <label>
                        Review domain
                        <select name="reviewDomain">
                          <option value="">None</option>
                          <option value="PROTOCOL">Protocol</option>
                          <option value="ACCESSIBILITY">Accessibility</option>
                          <option value="SECURITY">Security</option>
                        </select>
                      </label>
                    </fieldset>
                    <PendingSubmit className="button" locked={presented.mutationLocked}>
                      Save rule
                    </PendingSubmit>
                  </ProtectionMutationForm>
                ) : null}
              </section>
        
              <section id="reservations" className="atelier-panel" data-testid="seating-reservations">
                <h2>Reservations</h2>
                <p>Reserved does not mean seated.</p>
                <p data-testid="seating-capacity-ledger">
                  Capacity {workspace.capacityLedger.total} · generally available {workspace.capacityLedger.generallyAvailable} · reserved minima {workspace.capacityLedger.reservedMin} · reserved maxima {workspace.capacityLedger.reservedMax}
                  {workspace.capacityLedger.overbooked ? " · unresolved overbooking" : ""}
                </p>
                <ul>
                  {workspace.reservations.map((item) => (
                    <li key={item.id} data-testid={`seating-reservation-${item.releaseState}`}>
                      {item.preview ?? `${item.setCode} · ${item.releaseState}`}. Reserved does not mean seated.
                      {item.editionNo != null ? ` Edition ${item.editionNo}.` : ""}
                      {permissions.ruleActivate && item.releaseState === "DRAFT" ? (
                        <ProtectionMutationForm action={activateReservationBlockAction.bind(null, event.id)} className="actions">
                          <Envelope fields={{ ...envelopeFields, blockId: item.id, expectedVersion: item.editionNo ?? 1, expectedContentHash: item.contentHash ?? "" }} />
                          <IdempotencyField />
                          <button type="submit" className="button secondary">Activate reservation</button>
                        </ProtectionMutationForm>
                      ) : null}
                      {permissions.reservationManage && (item.releaseState === "DRAFT" || item.releaseState === "ACTIVE") ? (
                        <ProtectionMutationForm action={withdrawReservationBlockAction.bind(null, event.id)} className="actions">
                          <Envelope fields={{ ...envelopeFields, blockId: item.id, expectedVersion: item.editionNo ?? 1, expectedContentHash: item.contentHash ?? "", reason: "Withdrawn from governing set" }} />
                          <IdempotencyField />
                          <button type="submit" className="button secondary">Withdraw reservation</button>
                        </ProtectionMutationForm>
                      ) : null}
                      {permissions.reservationManage && item.releaseState === "ACTIVE" ? (
                        <>
                          <ProtectionMutationForm action={supersedeReservationBlockAction.bind(null, event.id)} className="actions" testId={`seating-reservation-successor-${item.id}`}>
                            <Envelope
                              fields={{
                                ...envelopeFields,
                                blockId: item.id,
                                expectedVersion: item.editionNo ?? 1,
                                expectedContentHash: item.contentHash ?? "",
                                eligibleGuestIds: workspace.guests.filter((guest) => guest.eligible).map((guest) => guest.id).join(","),
                                exactCount: item.exact ?? 2,
                              }}
                            />
                            <IdempotencyField />
                            <button type="submit" className="button secondary">Create successor reservation</button>
                          </ProtectionMutationForm>
                          <ProtectionMutationForm action={releaseReservationBlockAction.bind(null, event.id)} className="actions">
                            <Envelope fields={{ ...envelopeFields, blockId: item.id, expectedVersion: item.editionNo ?? 1, expectedContentHash: item.contentHash ?? "" }} />
                            <IdempotencyField />
                            <button type="submit" className="button secondary">Release reservation</button>
                          </ProtectionMutationForm>
                        </>
                      ) : null}
                    </li>
                  ))}
                </ul>
                {permissions.reservationManage ? (
                  <ProtectionMutationForm action={createReservationBlockAction.bind(null, event.id)} className="atelier-form seating-form" testId="seating-reservation-form">
                    <Envelope fields={envelopeFields} />
                    <IdempotencyField />
                    <fieldset>
                      <legend>Create a reservation</legend>
                      <label>
                        Guest set
                        <select name="eligibleSetCode" required>
                          <option value="ELIGIBLE_ATTENDING">Eligible attending guests</option>
                          <option value="PROTOCOL_PRIORITY">Protocol priority guests</option>
                        </select>
                      </label>
                      <input type="hidden" name="eligibleGuestIds" value={workspace.guests.filter((item) => item.eligible).map((item) => item.id).join(",")} />
                      <label>
                        Table
                        <select name="tableId">
                          <option value="">Any published table</option>
                          {workspace.tables.map((table) => (
                            <option key={table.id} value={table.id}>{table.label}</option>
                          ))}
                        </select>
                      </label>
                      <label>
                        Exact count
                        <input name="exactCount" type="number" min={1} defaultValue={2} />
                      </label>
                      <label>
                        Priority
                        <input name="priority" type="number" min={1} defaultValue={10} />
                      </label>
                    </fieldset>
                    <button type="submit" className="button">
                      Save reservation
                    </button>
                  </ProtectionMutationForm>
                ) : null}
              </section>
        
              <section id="runs" className="atelier-panel" data-testid="seating-runs">
                <h2>Runs</h2>
                <p>The solver recommends. Authorised people decide. Cancellation asks the worker to stop; a running attempt is not instantly erased.</p>
                {permissions.run && input ? (
                  <ProtectionMutationForm action={launchSeatingRunAction.bind(null, event.id)} className="atelier-form seating-form" testId="seating-run-form">
                    <Envelope fields={{ ...envelopeFields, inputEditionId: input.id ?? "" }} />
                    <IdempotencyField />
                    <fieldset>
                      <legend>Launch a seating run</legend>
                      <p className="help">
                        Generate seating plan freezes the current governed layout, rules, guests and objectives,
                        then queues a durable CP-SAT run for the solver service.
                      </p>
                      <label>
                        Deterministic seed
                        <input name="seed" defaultValue={`s06-${event.id.slice(-4)}`} readOnly />
                      </label>
                    </fieldset>
                    <button type="submit" className="button" style={{ cursor: "pointer" }}>
                      Generate seating plan
                    </button>
                  </ProtectionMutationForm>
                ) : null}
                {!workspace.runs.length ? (
                  <CpsatRunStatusPanel
                    model={buildCpsatRunUiModel({
                      operatorLifecycle: "NONE",
                      lifecycle: "NONE",
                      productResult: "",
                      resultStatus: null,
                      freshness: "CURRENT",
                      evidenceGrade: null,
                    })}
                  />
                ) : null}
                {(() => {
                  const pollTargets = workspace.runs.filter((run) => {
                    const lifecycle = String(run.lifecycle ?? run.status ?? "");
                    return (
                      ["QUEUED", "CLAIMED", "RUNNING", "BUILDING", "SEARCHING", "VERIFYING", "EXPLAINING", "PERSISTING"].includes(
                        lifecycle,
                      ) || Boolean(run.cancelRequested && !["CANCELLED", "READY_FOR_REVIEW", "FAILED", "CLOSED_NO_PLAN"].includes(lifecycle))
                    );
                  });
                  const primary = pollTargets[0];
                  return (
                    <CpsatRunLifecyclePoller
                      active={pollTargets.length > 0}
                      lifecycle={primary ? String(primary.lifecycle ?? primary.status) : null}
                      intervalMs={5_000}
                    />
                  );
                })()}
                <ul>
                  {visibleSeatingRuns(workspace.runs, workspace.currentRunId).map((run) => {
                    const isCurrent = run.id === workspace.currentRunId || Boolean(run.current);
                    const outcome = run.validatorVerdict ?? run.resultStatus ?? run.status;
                    const cancelRequested = Boolean(run.cancelRequested);
                    const queued = run.status === "QUEUED" || run.lifecycle === "QUEUED";
                    const lifecycle = String(run.lifecycle ?? run.status);
                    const phase =
                      run.progressPhase ??
                      (lifecycle === "RUNNING"
                        ? "Generating seating plan"
                        : queued
                          ? "queued"
                          : lifecycle === "READY_FOR_REVIEW"
                            ? "sealed"
                            : "settled");
                    return (
                      <li key={run.id} data-testid={`seating-run-${run.status}`}>
                        <article
                          data-testid="seating-run-card"
                          data-run-id={run.id}
                          data-current={isCurrent ? "true" : "false"}
                          data-stale={run.stale ? "true" : "false"}
                          data-outcome={outcome}
                          data-cancel-requested={cancelRequested ? "true" : "false"}
                        >
                          {isCurrent ? (
                            <CpsatRunStatusPanel
                              model={buildCpsatRunUiModel({
                                productResult: String(outcome === "QUEUED" || outcome === "CLAIMED" || outcome === "RUNNING" ? "" : outcome),
                                lifecycle,
                                resultStatus: run.resultStatus ?? (queued || lifecycle === "RUNNING" || lifecycle === "CLAIMED" ? null : String(outcome)),
                                phase,
                                elapsedMs: 0,
                                seated: run.seated ?? 0,
                                eligible: (run.seated ?? 0) + (run.unseated ?? 0),
                                occupiedTables: 0,
                                tableCapacity: 0,
                                freshness: run.stale ? "STALE" : (run.freshnessGrade as "CURRENT" | "FRESH" | "STALE" | undefined) ?? "CURRENT",
                                evidenceGrade: run.evidenceGrade ?? null,
                                purpose: run.purpose,
                                mode: run.mode,
                                createdAt: run.startedAt,
                                completedAt: run.completedAt ?? null,
                                cancelRequested,
                                assignmentHash: run.assignmentsHash ?? null,
                                hasCompleteIncumbent: lifecycle === "READY_FOR_REVIEW" || outcome === "FEASIBLE" || outcome === "OPTIMAL",
                                faultCode: run.faultCode ?? null,
                              })}
                            />
                          ) : null}
                          <p data-testid="seating-run-identity">
                            Run <code>{run.id.slice(0, 8)}</code>
                            {isCurrent ? " · Current" : " · Not current"}
                            {run.stale ? " · Stale" : " · Fresh"}
                            {" · "}
                            {cancelRequested ? "Cancellation requested" : outcome}
                          </p>
                          <CanonicalId id={run.id} label="Full immutable run ID" testId="seating-run-full-id" />
                          <div data-testid="seating-run-started">
                            Started{" "}
                            {run.startedAt ? <CanonicalTime iso={run.startedAt} /> : "unavailable"}
                            {" · Initiating actor: "}
                            {run.initiatingActorLabel ?? "unavailable"}
                          </div>
                          <p data-testid="seating-run-counts">
                            Seated {run.seated ?? 0} · Unseated {run.unseated ?? 0}
                          </p>
                          {isCurrent && run.stale ? (
                            <p data-testid="seating-run-current-stale">
                              This is the selected/current run, and it is also stale because upstream seating inputs changed after it was produced. Review and run again before treating it as fresh authority.
                            </p>
                          ) : null}
                          {run.stale && !isCurrent ? (
                            <p>Upstream event information changed after this run. Review and run again.</p>
                          ) : null}
                          {run.validatorVerdict === "INFEASIBLE" || run.status === "INFEASIBLE" ? (
                            <p>No safe seating plan satisfies every hard rule.</p>
                          ) : null}
                          {run.violatedSummary ? <p>Violated: {run.violatedSummary}</p> : null}
                          {permissions.edit &&
                          !seatingV2ReplacementEnabled() &&
                          !run.stale &&
                          (run.validatorVerdict === "FEASIBLE" || run.status === "FEASIBLE" || run.status === "INFEASIBLE") ? (
                            <ProtectionMutationForm action={adoptSeatingRunAction.bind(null, event.id)} className="actions">
                              <Envelope fields={{ ...envelopeFields, runId: run.id }} />
                              <IdempotencyField />
                              <button type="submit" className="button secondary" style={{ cursor: "pointer" }}>Adopt run</button>
                            </ProtectionMutationForm>
                          ) : null}
                          {permissions.run && queued && !cancelRequested ? (
                            <ProtectionMutationForm action={cancelSeatingRunAction.bind(null, event.id)} className="actions">
                              <Envelope fields={{ ...envelopeFields, runId: run.id, expectedVersion: 0 }} />
                              <IdempotencyField />
                              <button type="submit" className="button secondary" style={{ cursor: "pointer" }}>Cancel run</button>
                            </ProtectionMutationForm>
                          ) : null}
                        </article>
                      </li>
                    );
                  })}
                </ul>
              </section>
        
              <section id="studio" className="atelier-panel" data-testid="seating-studio">
                <h2>Studio</h2>
                <div className="seating-studio-grid">
                  <div>
                    <h3>Tables</h3>
                    {workspace.tables.length ? (
                      <ul>
                        {workspace.tables.map((table) => (
                          <li key={table.id} data-testid="seating-table-capacity">
                            {table.label} · {table.seated}/{table.capacity}
                            {table.mismatch ? " · capacity mismatch" : ""}
                          </li>
                        ))}
                      </ul>
                    ) : (
                      <div data-testid="seating-tables-empty" className="atelier-empty">
                        <p>No table layout is configured for seating placements.</p>
                        <p>
                          Placements cannot be displayed until a current venue layout publication is bound for this event.
                          {permissions.prepare
                            ? " Propose and activate a seating layout binding under Inputs."
                            : " Ask an authorised planner or director to bind a current layout publication. This assignment cannot change the binding."}
                        </p>
                        <p>
                          <a href="#inputs">Open Inputs · seating layout binding</a>
                        </p>
                      </div>
                    )}
                  </div>
                  <div>
                    <h3>Guests</h3>
                    <ul>
                      {workspace.workingAssignments.length
                        ? workspace.workingAssignments.map((item) => (
                            <li key={item.guestId}>
                              {item.guestLabel} · {item.state === "UNSEATED" ? "This guest remains unseated; the system did not invent a placement." : item.tableId} · {item.lockState}
                            </li>
                          ))
                        : workspace.guests.map((guest) => (
                            <li key={guest.id}>{guest.label} · {guest.eligible ? "Eligible" : guest.eligibilityCode}</li>
                          ))}
                    </ul>
                  </div>
                </div>
                {permissions.edit && (working?.status === "DRAFT" || working?.status === "WORKING") ? (
                  <ProtectionMutationForm action={applySeatingChangeAction.bind(null, event.id)} className="atelier-form seating-form" testId="seating-edit-form">
                    <div
                      hidden
                      data-testid="seating-edit-identity"
                      data-edition-id={working.id ?? ""}
                      data-edition-version={String(working.version ?? 0)}
                      data-edition-hash={working.contentHash ?? ""}
                    />
                    <Envelope
                      fields={{
                        ...envelopeFields,
                        editionId: working.id ?? "",
                        expectedVersion: working.version ?? 0,
                        expectedContentHash: working.contentHash ?? "",
                      }}
                    />
                    <IdempotencyField />
                    <fieldset>
                      <legend>Apply a seating change</legend>
                      <label>
                        Guest
                        <select name="guestId" required>
                          {workspace.workingAssignments.map((item) => (
                            <option key={item.guestId} value={item.guestId}>{item.guestLabel}</option>
                          ))}
                        </select>
                      </label>
                      <label>
                        Action
                        <select name="command" required>
                          <option value="ASSIGN_UNSEATED">Assign unseated</option>
                          <option value="MOVE">Move</option>
                          <option value="SWAP">Swap</option>
                          <option value="UNSEAT">Unseat</option>
                          <option value="LOCK">Lock</option>
                          <option value="UNLOCK">Unlock</option>
                        </select>
                      </label>
                      <label>
                        Target seat
                        <select name="targetPositionId">
                          <option value="">Choose seat</option>
                          {workspace.positions.map((position) => {
                            const occupied = workspace.workingAssignments.some(
                              (item) => item.state === "SEATED" && item.positionId === position.positionToken,
                            );
                            return (
                              <option key={position.id} value={position.positionToken} data-occupied={occupied ? "true" : "false"}>
                                {position.positionToken}
                              </option>
                            );
                          })}
                        </select>
                      </label>
                      <label>
                        Other guest
                        <select name="otherGuestId">
                          <option value="">None</option>
                          {workspace.workingAssignments.map((item) => (
                            <option key={`other-${item.guestId}`} value={item.guestId}>{item.guestLabel}</option>
                          ))}
                        </select>
                      </label>
                    </fieldset>
                    <label>
                      Unseat reason
                      <select name="reasonCode" defaultValue="MANUAL_UNSEAT">
                        <option value="MANUAL_UNSEAT">Manual unseat</option>
                        <option value="GOVERNED_UNSEATED">Governed exception</option>
                      </select>
                    </label>
                    <PendingSubmit
                      locked={
                        Boolean(resultId) &&
                        (presented.mutationLocked ||
                          presented.retryLock?.actionScope === "seating.plan.edit" ||
                          retryLockApplies(presented.retryLock, {
                            actionScope: "seating.plan.edit",
                            subjectId: working.id ?? "",
                            attemptedVersion: working.version,
                          }))
                      }
                    >
                      Apply seating change
                    </PendingSubmit>
                  </ProtectionMutationForm>
                ) : (
                  <p>Adopt a run before editing seats.</p>
                )}
              </section>
        
              <section id="review" className="atelier-panel" data-testid="seating-review">
                <h2>Review</h2>
                <p data-testid="seating-review-lineage">Package → Run → Validation → Plan edition</p>
                {cpsatReview ? (
                  <>
                    <CpsatCandidateReviewPanel
                      review={cpsatReview}
                      runHref={`#runs`}
                      showMutationControls={Boolean(permissions.submit || permissions.approve || permissions.publish)}
                    />
                    {/* Role-accurate mutation controls: Auditor never sees these forms. */}
                    {permissions.submit && cpsatReview.actions.canSubmit && cpsatReview.freshness !== "STALE" ? (
                      <ProtectionMutationForm
                        action={submitCpsatCandidateAction.bind(null, event.id)}
                        className="actions"
                        testId="cpsat-submit-for-approval"
                      >
                        <Envelope
                          fields={{
                            ...envelopeFields,
                            runId: cpsatReview.runId,
                            candidateId: cpsatReview.candidateId,
                            assignmentHash: cpsatReview.assignmentHash,
                          }}
                        />
                        <IdempotencyField />
                        <button type="submit" className="button primary" style={{ cursor: "pointer" }}>
                          Submit for approval
                        </button>
                      </ProtectionMutationForm>
                    ) : null}
                    {permissions.approve && cpsatReview.actions.canApprove && cpsatReview.approvalHistory.proposalId && cpsatReview.freshness !== "STALE" ? (
                      <ProtectionMutationForm
                        action={decideCpsatCandidateAction.bind(null, event.id)}
                        className="actions"
                        testId="cpsat-approve-candidate"
                      >
                        <Envelope
                          fields={{
                            ...envelopeFields,
                            runId: cpsatReview.runId,
                            candidateId: cpsatReview.candidateId,
                            assignmentHash: cpsatReview.assignmentHash,
                            proposalId: cpsatReview.approvalHistory.proposalId,
                            decision: "APPROVED",
                          }}
                        />
                        <IdempotencyField />
                        <button type="submit" className="button primary" style={{ cursor: "pointer" }}>
                          Approve seating plan
                        </button>
                      </ProtectionMutationForm>
                    ) : null}
                    {permissions.approve && cpsatReview.actions.canReject && cpsatReview.approvalHistory.proposalId ? (
                      <ProtectionMutationForm
                        action={decideCpsatCandidateAction.bind(null, event.id)}
                        className="actions"
                        testId="cpsat-reject-candidate"
                      >
                        <Envelope
                          fields={{
                            ...envelopeFields,
                            runId: cpsatReview.runId,
                            candidateId: cpsatReview.candidateId,
                            assignmentHash: cpsatReview.assignmentHash,
                            proposalId: cpsatReview.approvalHistory.proposalId,
                            decision: "REJECTED",
                          }}
                        />
                        <IdempotencyField />
                        <label>
                          Rejection reason
                          <input name="reason" required minLength={3} data-testid="cpsat-reject-reason" />
                        </label>
                        <button type="submit" className="button secondary" style={{ cursor: "pointer" }}>
                          Reject seating plan
                        </button>
                      </ProtectionMutationForm>
                    ) : null}
                    {permissions.publish && cpsatReview.actions.canAdopt && cpsatReview.approvalHistory.proposalId && cpsatReview.freshness !== "STALE" ? (
                      <ProtectionMutationForm
                        action={adoptCpsatCandidateAction.bind(null, event.id)}
                        className="actions"
                        testId="cpsat-adopt-candidate"
                      >
                        <Envelope
                          fields={{
                            ...envelopeFields,
                            runId: cpsatReview.runId,
                            candidateId: cpsatReview.candidateId,
                            assignmentHash: cpsatReview.assignmentHash,
                            approvalId: cpsatReview.approvalHistory.proposalId,
                          }}
                        />
                        <IdempotencyField />
                        <button type="submit" className="button primary" style={{ cursor: "pointer" }}>
                          Adopt seating plan
                        </button>
                      </ProtectionMutationForm>
                    ) : null}
                  </>
                ) : null}
                <p data-testid="seating-review-event">
                  {event.name}
                  {" · "}
                  {publication
                    ? `Current operational publication: adoption ${(publication.adoptionId ?? publication.id ?? "").slice(0, 8)} · ${seatedDisplay}/${eligibleDisplay} seated`
                    : "No current operational publication"}
                  {" · "}
                  {working ? `Current working edition: ${working.status}${working.status === "PUBLISHED" ? "" : " / unpublished"}` : "No working edition"}
                </p>
                <p data-testid="seating-review-requirement">{workspace.reviewRequirementCopy}</p>
                <ul data-testid="seating-review-evidence">
                  {workspace.constraints.filter((item) => item.reviewDomain && workspace.implicatedReviewDomains.includes(item.reviewDomain as "PROTOCOL" | "ACCESSIBILITY" | "SECURITY")).map((item) => (
                    <li key={item.id}>{item.reviewDomain} · {item.preview} · {item.status}</li>
                  ))}
                </ul>
                <details>
                  <summary>Plan hash provenance</summary>
                  <p data-testid="seating-plan-hash">Working edition hash: {working?.contentHash ?? "None"}</p>
                </details>
                <h3>Manual decisions</h3>
                <ul>{workspace.decisions.map((item) => <li key={item.id}>{item.command} · {item.reasonCode}</li>)}</ul>
                <h3>Specialist reviews</h3>
                <ul>{workspace.reviews.map((item) => <li key={item.id}>{item.domain} · {item.reviewerLabel} · {item.decision} · {item.reason}</li>)}</ul>
                {permissions.submit && (working?.status === "DRAFT" || working?.status === "WORKING") ? (
                  <ProtectionMutationForm action={submitSeatingPlanAction.bind(null, event.id)} className="actions">
                    <Envelope
                      fields={{
                        ...envelopeFields,
                        editionId: working.id ?? "",
                        expectedVersion: working.version ?? 0,
                        expectedContentHash: working.contentHash ?? "",
                      }}
                    />
                    <IdempotencyField />
                    <button type="submit" className="button">Submit seating plan</button>
                  </ProtectionMutationForm>
                ) : null}
                {permissions.submit && working?.status === "SUBMITTED" ? (
                  <ProtectionMutationForm action={recallSeatingPlanAction.bind(null, event.id)} className="actions" testId="seating-recall">
                    <Envelope fields={{ ...envelopeFields, editionId: working.id ?? "", expectedVersion: working.version ?? 0, expectedContentHash: working.contentHash ?? "" }} />
                    <IdempotencyField />
                    <button type="submit" className="button secondary">Recall submitted plan</button>
                  </ProtectionMutationForm>
                ) : null}
                {(permissions.reviewProtocol || permissions.reviewAccessibility || permissions.reviewSecurity) && working?.status === "SUBMITTED" && workspace.implicatedReviewDomains.length ? (
                  <ProtectionMutationForm action={decideSeatingReviewAction.bind(null, event.id)} className="atelier-form seating-form" testId="seating-review-form">
                    <Envelope fields={{ ...envelopeFields, editionId: working.id ?? "", editionHash: working.contentHash ?? "", expectedVersion: working.version ?? 0, expectedContentHash: working.contentHash ?? "" }} />
                    <IdempotencyField />
                    <fieldset>
                      <legend>Record specialist review</legend>
                      <label>
                        Domain
                        <select name="domain" required>
                          {workspace.implicatedReviewDomains.includes("PROTOCOL") && permissions.reviewProtocol ? <option value="PROTOCOL">Protocol</option> : null}
                          {workspace.implicatedReviewDomains.includes("ACCESSIBILITY") && permissions.reviewAccessibility ? <option value="ACCESSIBILITY">Accessibility</option> : null}
                          {workspace.implicatedReviewDomains.includes("SECURITY") && permissions.reviewSecurity ? <option value="SECURITY">Security</option> : null}
                        </select>
                      </label>
                      <label>
                        Decision
                        <select name="decision" required>
                          <option value="APPROVED">Approve review</option>
                          <option value="REJECTED">Request correction</option>
                        </select>
                      </label>
                      <label>
                        Reason
                        <input name="reason" required defaultValue="Reviewed against coded constraints" />
                      </label>
                    </fieldset>
                    <button type="submit" className="button">Record review</button>
                  </ProtectionMutationForm>
                ) : null}
                {permissions.approve && working?.status === "SUBMITTED" ? (
                  <ProtectionMutationForm action={decideSeatingApprovalAction.bind(null, event.id)} className="actions" testId="seating-approve">
                    <Envelope fields={{ ...envelopeFields, editionId: working.id ?? "", editionHash: working.contentHash ?? "", expectedVersion: working.version ?? 0, expectedContentHash: working.contentHash ?? "" }} />
                    <IdempotencyField />
                    <input type="hidden" name="decision" value="APPROVED" />
                    <button type="submit" className="button">Approve seating plan</button>
                  </ProtectionMutationForm>
                ) : null}
              </section>
        
              <section id="publication" className="atelier-panel" data-testid="seating-publication">
                <h2>Publication</h2>
                <p>Published without sending messages, issuing credentials or changing check-in.</p>
                <article data-testid="seating-current-publication">
                  <h3>Current operational publication</h3>
                  <p>
                    {publication
                      ? `CP-SAT adoption ${publication.adoptionId ?? publication.id} is the operational seating (${seatedDisplay}/${eligibleDisplay} seated). Solver result: ${publication.productResult ?? "n/a"}. Evidence: ${publication.evidenceGrade ?? "n/a"}.`
                      : "No current operational publication."}
                  </p>
                  {publication?.runId ? (
                    <p data-testid="seating-publication-run">
                      Adopted run <code>{publication.runId}</code>
                    </p>
                  ) : null}
                </article>
                <article data-testid="seating-working-edition">
                  <h3>Current working edition</h3>
                  <p>
                    {working
                      ? working.status === "PUBLISHED"
                        ? `PUBLISHED · ${working.contentHash}`
                        : `${working.status} / unpublished · ${working.contentHash}`
                      : "No working edition."}
                  </p>
                  {publication && working && working.status !== "PUBLISHED" ? (
                    <p data-testid="seating-publication-dual-truth">
                      Operational publication remains current while this working edition stays unpublished.
                    </p>
                  ) : null}
                </article>
                <article>
                  <h3>Operational approval</h3>
                  <ul>{workspace.approvals.map((item) => <li key={item.id}>{item.decision}</li>)}</ul>
                </article>
                <article>
                  <h3>History</h3>
                  <ul>{workspace.publications.map((item) => <li key={item.id}>{item.status} · {item.publicationNumber} · {item.editionHash}</li>)}</ul>
                </article>
                {permissions.publish && !seatingV2ReplacementEnabled() && working?.status === "APPROVED" ? (
                  <ProtectionMutationForm action={publishSeatingPlanAction.bind(null, event.id)} className="actions" testId="seating-publish">
                    <Envelope fields={{ ...envelopeFields, editionId: working.id ?? "", editionHash: working.contentHash ?? "", expectedVersion: working.version ?? 0, expectedContentHash: working.contentHash ?? "" }} />
                    <IdempotencyField />
                    <p>Published without sending messages, issuing credentials or changing check-in.</p>
                    <button type="submit" className="button">Publish seating plan</button>
                  </ProtectionMutationForm>
                ) : null}
                {permissions.exportJob ? (
                  <ProtectionMutationForm
                    key={`seating-export-${presented.correlationId ?? "idle"}`}
                    action={requestSeatingExportAction.bind(null, event.id)}
                    className="atelier-form seating-form"
                    testId="seating-export"
                  >
                    <Envelope
                      fields={{
                        ...envelopeFields,
                        publicationId: publication?.id ?? "",
                        editionId: working?.id ?? "",
                        // Server-minted key: avoids client IdempotencyField SSR/reuse mismatch on
                        // identical READY-export replay navigations (React #418).
                        idempotencyKey: crypto.randomUUID(),
                      }}
                    />
                    <fieldset>
                      <legend>Request a seating export</legend>
                      <label>
                        Format
                        <select name="format" required>
                          <option value="PDF">PDF</option>
                          <option value="PNG">PNG</option>
                          <option value="JSON">JSON</option>
                        </select>
                      </label>
                      <label>
                        Projection
                        <select name="projectionClass" required>
                          <option value="PLANNER">Planner</option>
                          <option value="DIRECTOR">Director</option>
                          <option value="CEO">CEO</option>
                          <option value="AUDITOR">Auditor</option>
                          <option value="DOWNSTREAM">Downstream</option>
                        </select>
                      </label>
                    </fieldset>
                    <button type="submit" className="button">Request export</button>
                  </ProtectionMutationForm>
                ) : null}
                <ul data-testid="seating-export-list">
                  {workspace.exports.map((item) => (
                    <li key={item.id} data-testid="seating-export-item" data-export-id={item.id}>
                      {item.format} · {item.status} · {item.projectionClass === "FULL" ? "CEO" : item.projectionClass}
                    </li>
                  ))}
                </ul>
              </section>
            </AppShell>
          );
        }
        
      operator: 'match'
      stack: |-
        TestContext.<anonymous> (/Users/maisondoclar/Desktop/atelier-doclar/apps/event-os/test/seating-v2-replacement-fail-closed.test.ts:50:12)
        Test.runInAsyncScope (node:async_hooks:206:9)
        Test.run (node:internal/test_runner/test:796:25)
        Suite.processPendingSubtests (node:internal/test_runner/test:526:18)
        Test.postRun (node:internal/test_runner/test:889:19)
        Test.run (node:internal/test_runner/test:835:12)
        async Suite.processPendingSubtests (node:internal/test_runner/test:526:7)
      ...
    1..4
not ok 42 - MD-PR-S075 V2 replacement fail-closed mutation surface
  ---
  duration_ms: 5.476
  type: 'suite'
  location: '/Users/maisondoclar/Desktop/atelier-doclar/apps/event-os/test/seating-v2-replacement-fail-closed.test.ts:17:1'
  failureType: 'subtestsFailed'
  error: '1 subtest failed'
  code: 'ERR_TEST_FAILURE'
  ...
# Subtest: sign-in form pending UX
    # Subtest: uses PendingSubmit, pending status and disables inputs while pending
    ok 1 - uses PendingSubmit, pending status and disables inputs while pending
      ---
      duration_ms: 0.956291
      ...
    1..1
ok 43 - sign-in form pending UX
  ---
  duration_ms: 1.826083
  type: 'suite'
  ...
# Subtest: staff identity presentation
    # Subtest: shows the Event Director name and event role without raw keys
    ok 1 - shows the Event Director name and event role without raw keys
      ---
      duration_ms: 418.931166
      ...
    # Subtest: shows CEO without substituting another identity
    ok 2 - shows CEO without substituting another identity
      ---
      duration_ms: 355.825417
      ...
    # Subtest: joins multiple same-scope roles instead of choosing CEO
    ok 3 - joins multiple same-scope roles instead of choosing CEO
      ---
      duration_ms: 155.106833
      ...
    # Subtest: uses a dignified fallback when no event assignment applies
    ok 4 - uses a dignified fallback when no event assignment applies
      ---
      duration_ms: 242.103917
      ...
    1..4
ok 44 - staff identity presentation
  ---
  duration_ms: 1174.255875
  type: 'suite'
  ...
# Subtest: sign-in status copy
    # Subtest: maps known session states without echoing unknown values
    ok 1 - maps known session states without echoing unknown values
      ---
      duration_ms: 0.248416
      ...
    1..1
ok 45 - sign-in status copy
  ---
  duration_ms: 0.611833
  type: 'suite'
  ...
# Subtest: canonical staff cookie policy
    # Subtest: uses one attribute set for form and API issuance
    ok 1 - uses one attribute set for form and API issuance
      ---
      duration_ms: 0.756291
      ...
    # Subtest: deletes an HTTPS-issued Secure cookie with the matching Secure policy
    ok 2 - deletes an HTTPS-issued Secure cookie with the matching Secure policy
      ---
      duration_ms: 0.108666
      ...
    # Subtest: keeps HttpOnly, SameSite and Secure aligned for insecure issuance
    ok 3 - keeps HttpOnly, SameSite and Secure aligned for insecure issuance
      ---
      duration_ms: 0.0665
      ...
    1..3
ok 46 - canonical staff cookie policy
  ---
  duration_ms: 1.686167
  type: 'suite'
  ...
# Subtest: MD-PR-UX001 event-create authority
    # Subtest: denies Read-Only Auditor and Planner event.create while CEO and Event Director remain allowed
    ok 1 - denies Read-Only Auditor and Planner event.create while CEO and Event Director remain allowed
      ---
      duration_ms: 333.721125
      ...
    # Subtest: server-denies Auditor createEvent even when the form is not shown
    ok 2 - server-denies Auditor createEvent even when the form is not shown
      ---
      duration_ms: 257.762125
      ...
    # Subtest: keeps authorised Event Director creation functional
    ok 3 - keeps authorised Event Director creation functional
      ---
      duration_ms: 247.907083
      ...
    # Subtest: presents Ready as unavailable and not selectable from Prepare
    ok 4 - presents Ready as unavailable and not selectable from Prepare
      ---
      duration_ms: 0.690917
      ...
    1..4
ok 47 - MD-PR-UX001 event-create authority
  ---
  duration_ms: 840.854333
  type: 'suite'
  ...
# Subtest: MD-PR-UX001 role-safe identity resolution
    # Subtest: resolves authorised staff and event labels without using UUIDs as the primary label
    ok 1 - resolves authorised staff and event labels without using UUIDs as the primary label
      ---
      duration_ms: 369.885417
      ...
    # Subtest: does not leak other-organisation or out-of-scope event names to a Planner
    ok 2 - does not leak other-organisation or out-of-scope event names to a Planner
      ---
      duration_ms: 269.924666
      ...
    # Subtest: uses Event scope restricted for Admin when CEO sees Alpha One
    ok 3 - uses Event scope restricted for Admin when CEO sees Alpha One
      ---
      duration_ms: 305.577792
      ...
    1..3
ok 48 - MD-PR-UX001 role-safe identity resolution
  ---
  duration_ms: 946.078416
  type: 'suite'
  ...
# Subtest: MD-PR-UX001 layout action readiness
    # Subtest: blocks submit without a current validation run
    ok 1 - blocks submit without a current validation run
      ---
      duration_ms: 1.124292
      ...
    # Subtest: blocks publish until the exact current hash is approved
    ok 2 - blocks publish until the exact current hash is approved
      ---
      duration_ms: 0.464125
      ...
    1..2
ok 49 - MD-PR-UX001 layout action readiness
  ---
  duration_ms: 2.844459
  type: 'suite'
  ...
1..49
# tests 161
# suites 49
# pass 158
# fail 3
# cancelled 0
# skipped 0
# todo 0
# duration_ms 30150.021667
/Users/maisondoclar/Desktop/atelier-doclar/apps/event-os:
 ERR_PNPM_RECURSIVE_RUN_FIRST_FAIL  @maison-doclar/event-os@0.1.0 test: `tsx --test test/*.test.ts`
Exit status 1

---
exit_code: 0
elapsed_ms: 32207
ended_at: 2026-09-18T08:24:45.126Z
---
